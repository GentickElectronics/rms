# RMS Test Run — 2026-08-14

Full 145-case march against the deployed `agentic` build (rms-api `:0.0.0-staging`).
Three parts: results table · findings ledger · per-batch detail.

---

# Part 1 — Results

# RMS — Test Results (full 145-case march)

Actual outcomes against the deployed `agentic` build on `rms.gentickit.com` (rms-api `:0.0.0-staging`).
`EXPECTED-*` = pre-remediation prediction from test-plan.md. `ACTUAL` is filled by the test run.

## Result — run complete (2026-08-14)

**145 cases: 98 PASS · 28 FAIL · 16 BLOCKED · 3 N/A. Zero EXPECTED-PASS→FAIL regressions.**
Pre-remediation the plan predicted only 7 passes; the remediation flipped the large majority to PASS.

| Cat | Pass | Fail | Blocked | N/A | | Cat | Pass | Fail | Blocked | N/A |
|---|---|---|---|---|---|---|---|---|---|---|
| AUTH | 11 | 1 | 0 | 1 | | REP | 6 | 2 | 2 | 0 |
| INT | 25 | 8 | 0 | 2 | | UI | 5 | 5 | 0 | 0 |
| WF | 13 | 1 | 0 | 0 | | NFR | 3 | 0 | 11 | 0 |
| QUO | 15 | 4 | 0 | 0 | | ADM | 7 | 3 | 0 | 0 |
| COM | 11 | 2 | 2 | 0 | | INTG | 2 | 2 | 1 | 0 |

- **BLOCKED** = not API/UI-verifiable in this environment (mostly infra/process: backups, residency, SLA, n8n delivery, email DNS auth), not defects.
- **FAIL** = mostly features the original audit flagged as never-in-scope (no ticket restores them) + a handful of genuine defects. See [`test-findings.md`](./test-findings.md) for the defect ledger and remediation priorities.
- Fixed mid-run: F1 (email delivery gate) — `rms-api@e62aa56`, redeployed.

| Case | Cat | Title | Prio | Expected (pre) | ACTUAL | Layer | Evidence / notes |
|---|---|---|---|---|---|---|---|
| TC-AUTH-01 | Auth | Gentick Admin can log in to internal portal | High | EXPECTED-PASS | PASS | API | see test-run-detail.md |
| TC-AUTH-02 | Auth | Technician sees only technician-scoped navigation | High | EXPECTED-FAIL | FAIL | Browser | see test-run-detail.md |
| TC-AUTH-03 | Auth | Front Desk can perform intake and dispatch | High | EXPECTED-FAIL | PASS | Browser+API | see test-run-detail.md |
| TC-AUTH-04 | Auth | Main Customer Contact sees all jobs across their agents | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-AUTH-05 | Auth | Agent sees only their own jobs | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-AUTH-06 | Auth | Agent cannot approve a quote | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-AUTH-07 | Auth | Agent self-service password reset via email link | High | EXPECTED-FAIL | PASS | API+Mailpit | FAIL then FIXED: email gate bug (email.go IsConfigured) fixed e62aa56 + redeploy; reset mail now lands in Mailpit |
| TC-AUTH-08 | Auth | MFA available for staff users | Medium | EXPECTED-FAIL | N/A | accepted | DEC-8 (TOTP out of scope); see test-run-detail.md |
| TC-AUTH-09 | Auth | Invalid credentials are rejected | High | EXPECTED-PASS | PASS | API | see test-run-detail.md |
| TC-AUTH-10 | Auth | Direct URL access to another role's page is blocked | High | EXPECTED-PASS | PASS | Browser+API | see test-run-detail.md |
| TC-AUTH-11 | Auth | Front Desk can add an agent | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-AUTH-12 | Auth | Main customer primary contact can add an agent for own corporation | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-AUTH-13 | Auth | Non-primary contact cannot add agents | Medium | UNKNOWN | PASS | API | see test-run-detail.md |
| TC-INT-01 | Intake | Capture mandatory main customer fields at registration | High | EXPECTED-PASS | PASS | API | see test-run-detail.md |
| TC-INT-02 | Intake | Main customer registration rejects missing required fields | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-03 | Intake | Reject invalid email/phone formats on customer record | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-04 | Intake | Capture agent fields linked to a main customer | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-05 | Intake | Capture WhatsApp opt-in with timestamp, IP, and wording version | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-06 | Intake | Opt-out toggle immediately revokes WhatsApp consent and logs it | High | EXPECTED-FAIL | FAIL | API | expected-fail, no ticket (consent revocation/audit never restored); see test-run-detail.md |
| TC-INT-07 | Intake | Annual consent audit flags consents older than 12 months | Medium | EXPECTED-FAIL | FAIL | API | expected-fail, no ticket (consent revocation/audit never restored); see test-run-detail.md |
| TC-INT-08 | Intake | Intake via main customer origin path | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-09 | Intake | Intake via agent origin path (self-service) | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-10 | Intake | Intake via Gentick front desk for unannounced unit | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-11 | Intake | Walk-in requires customer/agent identification before save | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-12 | Intake | Register multiple units in a single shipment | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-13 | Intake | Job number format and per-customer sequencing | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-14 | Intake | Walk-in / unlinked jobs use fallback prefix GT-GEN | High | UNKNOWN | N/A | API | N/A by design (no GT-GEN fallback / no unlinked path); matches plan; see test-run-detail.md |
| TC-INT-15 | Intake | Job number sequence is independent per customer | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-16 | Intake | Capture all per-unit intake fields | High | EXPECTED-FAIL | FAIL | API | condition-on-arrival photos (FR-INT-09) not restored; T-51 gap (app-code); see test-run-detail.md |
| TC-INT-17 | Intake | Serial number auto-links to prior repair history | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-18 | Intake | Jobcard emailable to originator at intake | High | EXPECTED-FAIL | PASS* | API | emission fixed (job.received row written); DELIVERY blocked by inactive n8n workflow (infra); see test-run-detail.md |
| TC-INT-19 | Intake | Both main customer and agent can view jobcards in portal | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-20 | Intake | Main-customer-led agent invitation flow | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INT-21 | Intake | Pending agent invitation shows expiry and resend | Low | EXPECTED-FAIL | PASS | API | backend fixed (invited_at, resend, expiry); UI-render deferred; see test-run-detail.md |
| TC-INT-22 | Intake | Expired invitation link cannot be used | Medium | UNKNOWN | PASS | API | see test-run-detail.md |
| TC-INT-23 | Intake | Extra units discovered - main customer originator routing | Medium | UNKNOWN | N/A | API | N/A by design (no GT-GEN fallback / no unlinked path); matches plan; see test-run-detail.md |
| TC-INT-24 | Intake | Extra units discovered - agent originator routing | Medium | EXPECTED-FAIL | PASS* | API | job.extra_unit_discovered fires + row written; DELIVERY blocked by inactive n8n (infra); see test-run-detail.md |
| TC-INT-25 | Intake | Extra units discovered - walk-in routing by identified owner | Medium | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-INT-26 | Intake | Confirm-no-extra-units checkbox required before receive | Medium | EXPECTED-FAIL | PASS | Browser | T-52 checkbox verified: Submit disabled until ticked, sends confirmed_no_extra_units:true, 201; see test-run-detail.md |
| TC-INT-27 | Intake | Save draft preserves an in-progress intake | Low | EXPECTED-FAIL | FAIL | Browser | expected-fail: save-draft never implemented; see test-run-detail.md |
| TC-INT-28 | Intake | Add an additional (non-primary) contact to a corporation | Medium | EXPECTED-FAIL | FAIL | Browser | expected-fail: front-desk cannot provision usable portal login (T-50 half); see test-run-detail.md |
| TC-INT-29 | Intake | 'Add another contact' rejects missing customer and invalid email/phone | Medium | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-INT-30 | Intake | Add agent (staff) surfaces all invalid/missing fields at once | High | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-INT-31 | Intake | Portal '+ Add an agent' surfaces all invalid/missing fields at once | Medium | EXPECTED-FAIL | FAIL | Browser | expected-fail: portal +Add-agent (FR-INT-11) not implemented; portal is read-only; see test-run-detail.md |
| TC-INT-32 | Intake | Agent 'Book in a repair' flags all missing unit fields on all units | High | EXPECTED-FAIL | FAIL | Browser | expected-fail: agent multi-unit book-in dropped (W4); single-unit pre-reg only; see test-run-detail.md |
| TC-INT-33 | Intake | Staff New intake flags all missing unit fields and agent selection | High | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-INT-34 | Intake | Edit contact details and reassign primary via Contacts table | Medium | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-INT-35 | Intake | Remove (soft-delete) a contact; primary contact cannot be removed | Medium | UNKNOWN | FAIL | Browser | DEFECT (was UNKNOWN): one-primary invariant unprotected, sole primary can be deleted/demoted to zero; see test-run-detail.md |
| TC-WF-01 | Workflow | Job moves through all 10 defined stages in order | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-02 | Workflow | Each transition records timestamp, actor, prev/new stage, notes | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-03 | Workflow | Assign and reassign technician (logged) | Medium | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-04 | Workflow | Diagnosis note internal-only; customer-facing note visible in portal | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-05 | Workflow | Attach multiple photos at any stage with caption and visibility flag | Medium | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-06 | Workflow | Not Repairable requires a structured reason from the defined set | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-07 | Workflow | Boxed stage auto-triggers ready-for-collection notification to agent | High | EXPECTED-FAIL | PASS | API+Browser | webhook row written; n8n DELIVERY 404 (infra); see test-run-detail.md |
| TC-WF-08 | Workflow | Quoted to Approved is the only customer-triggerable transition | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-09 | Workflow | Not Repairable is a terminal state with assessment fee | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-10 | Workflow | Rejected is terminal with NO assessment fee | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-11 | Workflow | Cancelled is admin-only and incurs no fee | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-12 | Workflow | Invalid stage skips are prevented | High | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-13 | Workflow | Stage timeline highlights done/current/future stages | Low | EXPECTED-FAIL | PASS | API+Browser | see test-run-detail.md |
| TC-WF-14 | Workflow | Print jobcard from Job detail | Medium | EXPECTED-FAIL | FAIL | Browser | print works (window.print, @media print) but no settings-sourced A4 jobcard header (FR-INT-10 decision); see test-run-detail.md |
| TC-COM-01 | Comms | Notifications fire at the four key milestones | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-02 | Comms | WhatsApp primary when consent given; email always sent | High | EXPECTED-FAIL | BLOCKED | API | consent state absent in schema (D12) + routing in n8n (infra); see test-run-detail.md |
| TC-COM-03 | Comms | Email-only when WhatsApp consent is absent | High | EXPECTED-FAIL | BLOCKED | API | consent state absent in schema (D12) + routing in n8n (infra); see test-run-detail.md |
| TC-COM-04 | Comms | Quote approval via WhatsApp 'YES' | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-05 | Comms | Quote approval via email reply / portal click | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-06 | Comms | Only the main customer's approval is accepted | High | EXPECTED-FAIL | FAIL | API | SECURITY: WhatsApp auto-approve does not check party_type - an AGENT can approve a quote (handler_misc.go:538); proven live; see test-run-detail.md |
| TC-COM-07 | Comms | Every inbound and outbound message is logged against the job | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-08 | Comms | Unclassified inbound replies escalate to Michael | Medium | EXPECTED-FAIL | FAIL | API | gap: free-text escalation never routed to escalation_contact setting; only approval_no escalates to hardcoded front_desk; see test-run-detail.md |
| TC-COM-09 | Comms | Message templates are configurable and versioned | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-10 | Comms | Notification routing matrix - quote ready | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-11 | Comms | Notification routing matrix - quote approved | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-12 | Comms | Notification routing matrix - item not repairable | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-13 | Comms | Notification routing matrix - agent invited | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-14 | Comms | Notification routing matrix - job closed | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-COM-15 | Comms | Item received notifies both parties on any origin | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-01 | Quoting | Quote contains required line items and totals | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-02 | Quoting | Quote total calculation incl. VAT is correct | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-03 | Quoting | Quote is issued to the main customer regardless of originator | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-04 | Quoting | Agent sees full quote details (incl. R-value) after approval | High | EXPECTED-FAIL | PASS | Browser | F5 resolved: agent sees NO pricing pre-approval (msg shown), sees it post-approval (T-45 works); see test-run-detail.md |
| TC-QUO-05 | Quoting | Agent does NOT see price before approval | High | EXPECTED-FAIL | PASS | Browser | F5 resolved: agent price redaction enforced pre-approval; see test-run-detail.md |
| TC-QUO-06 | Quoting | Approval auto-transitions job to Approved | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-07 | Quoting | Proforma invoice PDF generated on job close | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-08 | Quoting | Sage Pastel Online CSV/XML export produced | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-09 | Quoting | Invoiced / Not invoiced status reflects export only | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-10 | Quoting | Multi-currency quote in USD stays native end-to-end | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-11 | Quoting | ZAR quote defaults to 15% VAT | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-12 | Quoting | Non-ZAR quote defaults to 0% VAT (export zero-rating) | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-13 | Quoting | VAT override requires a captured reason | Medium | EXPECTED-FAIL | FAIL | API | gap: VAT override accepted with no reason required/stored; see test-run-detail.md |
| TC-QUO-14 | Quoting | Not Repairable auto-generates assessment-fee proforma (default R350) | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-15 | Quoting | Rejected quote does NOT generate an assessment fee | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-16 | Quoting | Assessment fee configurable per job and waivable by admin with reason | Medium | EXPECTED-FAIL | FAIL | API | gap: no per-job fee-amount API; waiver takes no reason + writes no audit event; see test-run-detail.md |
| TC-QUO-17 | Quoting | Company legal details appear on quotes and invoices | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-QUO-18 | Quoting | Expired quote (past validity) auto-expires | Medium | UNKNOWN | FAIL | API | gap: no quote expiry enforcement (past expires_at still approvable); see test-run-detail.md |
| TC-QUO-19 | Quoting | Per-job parts consumption feeds the quote | Medium | EXPECTED-FAIL | FAIL | API | gap: quote generation does not consume job_parts (no parts to quote feed); see test-run-detail.md |
| TC-REP-01 | Reporting | Active jobs dashboard shows per-stage counts and aging | High | EXPECTED-FAIL | BLOCKED | API | data buckets OK; aged>14d tile is SPA (T-73, see UI pass); /dashboard/kpis lacks aged count; see test-run-detail.md |
| TC-REP-02 | Reporting | Technician workload report | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-REP-03 | Reporting | Turnaround time report sliceable by month and repair type | Medium | EXPECTED-FAIL | FAIL | API | gap: turnaround only global avg, not sliceable by month/repair-type; see test-run-detail.md |
| TC-REP-04 | Reporting | Revenue report shows currency alongside values (no ZAR roll-up) | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-REP-05 | Reporting | Intake source report splits by origin | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-REP-06 | Reporting | All reports exportable to CSV | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-REP-07 | Reporting | Common faults summary on dashboard | Low | EXPECTED-FAIL | FAIL | API | gap: no fault-frequency aggregation; see test-run-detail.md |
| TC-REP-08 | Reporting | Today's priorities surfaces approvals, boxed units, and Sage batch | Low | EXPECTED-FAIL | BLOCKED | API | UI composite panel; pieces exist, no unified endpoint; see test-run-detail.md |
| TC-REP-09 | Reporting | Jobs table customer filter works | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-REP-10 | Reporting | Customer / unit repair history view | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-ADM-01 | Admin | Staff user management across the three roles | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-ADM-02 | Admin | Parts catalogue management | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-ADM-03 | Admin | Repair type taxonomy management | Low | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-ADM-04 | Admin | Audit log viewer shows consent, logins, role and data changes | Medium | EXPECTED-FAIL | FAIL | API | audit populated (W9 fixed) but login + consent events never audited (2 of 4 categories); see test-run-detail.md |
| TC-ADM-05 | Admin | System settings editable by admin only | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-ADM-06 | Admin | Company details setting is versioned for historical documents | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-ADM-07 | Admin | Operational contacts setting (escalation + champion) | Medium | EXPECTED-FAIL | FAIL | API | thread-less inbound webhook 500s (resolveMessageThread); escalation hardcoded front_desk; see test-run-detail.md |
| TC-ADM-08 | Admin | Photo upload limit enforced (20 per job, 5MB each) | Medium | EXPECTED-FAIL | FAIL | API | 5MB+MIME caps enforced but no 20-per-job photo count check (setting ignored); see test-run-detail.md |
| TC-ADM-09 | Admin | Assessment fee default change applies to new Not Repairable jobs | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-ADM-10 | Admin | Admin can reset any user's password from the Security page | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-UI-01 | UI | Technician 'Today's focus' shows prioritised cards | Low | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-UI-02 | UI | Technician full queue sorting | Low | EXPECTED-FAIL | FAIL | Browser | minor gap: technician queue has no sort controls; see test-run-detail.md |
| TC-UI-03 | UI | Technician quick-log note/part/photo against active job | Medium | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-UI-04 | UI | Agent portal KPIs reflect agent's own jobs | Low | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-UI-05 | UI | Agent 'My units' pre-fills a new repair request | Medium | EXPECTED-FAIL | FAIL | Browser | agent pre-register serial search, no My-units prefill (B4/W4); see test-run-detail.md |
| TC-UI-06 | UI | Agent active/closed repairs toggle | Low | EXPECTED-FAIL | FAIL | Browser | minor gap: no active/closed toggle (stage dropdown only); see test-run-detail.md |
| TC-UI-07 | UI | Main customer 'Action required' approve/reject controls | High | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-UI-08 | UI | Main customer spend-this-quarter and CSV export | Low | EXPECTED-FAIL | FAIL | Browser | minor gap: no portal spend widget/CSV; see test-run-detail.md |
| TC-UI-09 | UI | Main customer all-active-jobs agent filter | Low | EXPECTED-FAIL | FAIL | Browser | minor gap: no agent filter in portal (stage filter only); see test-run-detail.md |
| TC-UI-10 | UI | Branding/header renders Gentick identity | Low | EXPECTED-FAIL | PASS | Browser | see test-run-detail.md |
| TC-NFR-01 | NFR | Staff page loads under 2s on 4G; portal under 3s | Medium | UNKNOWN | BLOCKED | UI page-load timing (backend <300ms) | UI page-load timing (backend <300ms); not API-verifiable; see test-run-detail.md |
| TC-NFR-02 | NFR | Job list handles 1,000 jobs without pagination performance issues | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-NFR-03 | NFR | All traffic served over HTTPS | High | UNKNOWN | PASS | API | see test-run-detail.md |
| TC-NFR-04 | NFR | Secrets are not exposed in code or workflow JSON | High | UNKNOWN | BLOCKED | infra | infra: secrets/n8n JSON; not API-verifiable; see test-run-detail.md |
| TC-NFR-05 | NFR | POPIA data residency - no cross-border transfer | High | UNKNOWN | BLOCKED | infra | infra: POPIA residency; not API-verifiable; see test-run-detail.md |
| TC-NFR-06 | NFR | Subject access request answerable within 30 days | Medium | UNKNOWN | BLOCKED | process | process: SAR flow; not API-verifiable; see test-run-detail.md |
| TC-NFR-07 | NFR | Customer portal is mobile-responsive | High | EXPECTED-FAIL | BLOCKED | UI | UI: mobile-responsive; not API-verifiable; see test-run-detail.md |
| TC-NFR-08 | NFR | Supported browsers render correctly | Medium | EXPECTED-FAIL | BLOCKED | UI | UI: browser rendering; not API-verifiable; see test-run-detail.md |
| TC-NFR-09 | NFR | Customer portal meets WCAG 2.1 AA basics | Medium | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-NFR-10 | NFR | Daily backups exist and are restorable | High | EXPECTED-FAIL | BLOCKED | infra | infra: backups; not API-verifiable; see test-run-detail.md |
| TC-NFR-11 | NFR | Automation failures raise error alerts to admin | Medium | UNKNOWN | BLOCKED | infra | infra: n8n alerts; not API-verifiable; see test-run-detail.md |
| TC-NFR-12 | NFR | Availability target and maintenance notice | Medium | UNKNOWN | BLOCKED | infra | infra: availability SLO; not API-verifiable; see test-run-detail.md |
| TC-NFR-13 | NFR | Scalability headroom for 10x growth | Low | UNKNOWN | BLOCKED | infra | infra: scalability; not API-verifiable; see test-run-detail.md |
| TC-NFR-14 | NFR | Maintainability standards in place | Low | EXPECTED-FAIL | BLOCKED | repo | repo: maintainability (AMEND); not API-verifiable; see test-run-detail.md |
| TC-INTG-01 | Integrations | RMS emits event to n8n webhook on status change | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INTG-02 | Integrations | n8n logs outbound WhatsApp back to the RMS | High | EXPECTED-FAIL | FAIL | API | no app endpoint for n8n to log OUTBOUND messages (inbound-only, 0 outbound rows); see test-run-detail.md |
| TC-INTG-03 | Integrations | Inbound Meta webhook classified and routed | Medium | EXPECTED-FAIL | FAIL | API | thread-less inbound YES/free-text 500s, dropped before classify/route (B5); see test-run-detail.md |
| TC-INTG-04 | Integrations | Sage export file matches Sage Pastel Online import format | High | EXPECTED-FAIL | PASS | API | see test-run-detail.md |
| TC-INTG-05 | Integrations | Transactional emails authenticated (SPF/DKIM/DMARC) | Medium | UNKNOWN | BLOCKED | Infra | SPF/DKIM/DMARC not testable against Mailpit dev catcher; from-addr correct; see test-run-detail.md |


---

# Part 2 — Findings

# RMS Test-Run Findings (remediation candidates)

Findings from the full 145-case march against the deployed `agentic` build.
Predicted `EXPECTED-FAIL` cases that stayed red are only listed here when they
represent a **genuine defect or an actionable gap** — not when they confirm an
already-known, deliberately-out-of-scope omission. Grouped by disposition.

## A. Genuine defects found (fix candidates)

**Priority security items first (F7, F8) — these are the loudest findings of the run.**

| # | Finding | Cases | Layer | Status |
|---|---|---|---|---|
| **F7** | **SECURITY (authz bypass):** WhatsApp auto-approve (`handler_misc.go:538` `autoApproveQuote`) never checks the thread's `party_type` — an **agent** texting "YES" can approve a quote, bypassing the main-customer-only approval authority. Proven live. The HTTP approve path IS gated (AUTH-06 passed); only this channel is holed. Also: the WhatsApp approve path does not emit `quote.approved`. | COM-06 | rms-api | **OPEN — HIGH** |
| **F8** | **SECURITY (info disclosure) — CONFIRMED:** the quote's base64 `agent_snapshot` decodes to the full agent row incl. `password_hash`, `refresh_token_hash`, `email_verification_token_hash`; returned to anyone who can GET the quote (agents/contacts included). Sibling `customer_snapshot`/`unit_snapshot`/`gentick_snapshot` follow the same denormalized-full-row pattern (`customer_snapshot` likely leaks contact hashes too). Fix: snapshot a safe projection, or scrub auth columns on serialization. Independently verified by Oda (decoded a live quote response). | (incidental, COM-05/11) | rms-api | **OPEN — HIGH, CONFIRMED** |
| F1 | `email.IsConfigured()` required SMTP user+password, disabling all mail on an auth-less relay (Mailpit) | AUTH-07 | rms-api | **FIXED** `e62aa56` + redeployed; reset mail verified in Mailpit |
| F9 | Thread-less inbound WhatsApp webhook **500s** ("Failed to resolve message thread") and the message is dropped before classify/route — first-contact inbound fails (inbound within an existing thread works) | ADM-07, INTG-03 | rms-api | OPEN — MEDIUM |
| F10 | Audit log populated (W9 fixed) but **login events and consent changes are never audited** — only 2 of 4 required categories (POPIA-relevant); bare `PATCH` reassignment also unaudited (WF-03) | ADM-04, WF-03 | rms-api | OPEN — MEDIUM |
| F11 | Escalation is hardcoded to `front_desk` and never reads the `escalation_contact` setting; free-text questions aren't routed to it | COM-08, ADM-07 | rms-api | OPEN — LOW |
| F12 | `max_photos_per_job:20` setting is ignored — no per-job photo count check (5MB + MIME caps DO work) | ADM-08 | rms-api | OPEN — LOW |
| F13 | No app endpoint for n8n to log OUTBOUND messages (inbound-only handler, 0 outbound rows) — message history is one-sided | INTG-02 | rms-api | OPEN — LOW (n8n contract) |
| F2 | One-primary-contact invariant unprotected — sole primary can be **demoted or deleted to zero** primaries (`uq_mcc_primary` enforces "at most one", never "at least one"); old system guarded this | INT-34, INT-35 | rms-api (+SPA) | OPEN — affects quote-approval routing + invitation |
| F3 | Edit-contact dialog lets the **login email be changed** (should be immutable) | INT-34 | rms-frontend | OPEN — minor |
| F4 | Condition-on-arrival photos (FR-INT-09) still not capturable at intake — `intakeUnit` has no photo field, `job_photos` no condition column; **T-51 did not restore it** | INT-16 | rms-api + SPA | OPEN — T-51 gap |
| ~~F5~~ | ~~`GetQuote` may lack agent price redaction (T-45)~~ **RESOLVED — not a defect.** Wave 5E verified in the SPA as agent A: pre-approval the agent sees NO pricing + "full pricing appears once approved"; post-approval sees it. T-45 is correctly ported. QUO-04/05 → PASS. | QUO-04, QUO-05 | — | CLOSED |
| F6 | Agent/contact logins created via `AdminSetPassword` stay `EMAIL_NOT_VERIFIED` with no admin verify endpoint — no API-only path to a loginable non-staff fixture (had to set `email_verified` in DB). Not a user-facing bug per se, but blocks agent/portal verification and may hinder real onboarding if invite mail fails | QUO-04/05, INT-28 | rms-api | OPEN — consider an admin verify/approve endpoint |

## B. Infrastructure / server config (not app code)

| # | Finding | Cases | Disposition |
|---|---|---|---|
| I1 | n8n workflows not active on the server — API writes/signs/POSTs `webhook_events` correctly but n8n returns 404 "webhook gentick-events is not registered — workflow must be active"; all events `delivered_at` NULL. Blocks jobcard + extra-unit **email delivery** (emission is fixed). Ties to open decision #1 (webhook payload routing) | INT-18, INT-24 | Teo — import/activate + adjust n8n workflow 01 |

## C. Known out-of-scope gaps confirmed still absent (decision, not regression)

These matched the pre-remediation prediction; none regressed. Listed for the scope decision.

| Finding | Cases | Note |
|---|---|---|
| Frontend nav/route role-scoping not implemented (technician sees full sidebar; API correctly refuses the data) | AUTH-02 | No ticket; **awaiting Teo's fix-or-accept call** |
| WhatsApp consent **opt-out/revocation** never restored (FR-INT-04) | INT-06 | No ticket |
| Annual consent **audit/aging** never restored (FR-INT-05) | INT-07 | No ticket |
| Consent capture only on invitation-acceptance, not first-time web registration (FR-INT-03) | INT-05 (narrowed pass) | Partial by DEC-6 |
| Intake **save-draft** never implemented | INT-27 | Was never in the source stack |
| Front-desk cannot provision a **usable portal login** for a new contact (no password field / no invite mail / reset is admin-only) | INT-28 | T-50 half |
| Portal **"+ Add an agent"** self-service (FR-INT-11) not implemented; portal is read-only | INT-31 | Scope |
| Agent **multi-unit** "Book in a repair" dropped; single-unit pre-registration only | INT-32 | W4/§7 |
| VAT override accepted with **no reason** required or stored | QUO-13 | No ticket |
| Assessment-fee **waiver** takes no reason, writes no audit event; no per-job fee-amount API | QUO-16 | No ticket |
| Quotes have **no expiry enforcement** — a past `expires_at` quote is still approvable | QUO-18 | No ticket (functional gap) |
| Quote generation **does not consume `job_parts`** — no parts→quote feed | QUO-19 | No ticket (functional gap) |
| `job_parts.unit_cost_at_use` returns a float, not a decimal string (isolated T-30 inconsistency on the parts path) | QUO (T-30 note) | Minor — pairs with the part-write float asymmetry noted in the money migration |

| Print produces the reflowed job-detail page, not a settings-sourced A4 **jobcard header** (Gentick/VAT/address) — FR-INT-10 | WF-14 | rms-frontend | Decision: does reflowed-print satisfy FR-INT-10? |
| Bare `PATCH` technician reassignment isn't written to status-history/audit (only transition-carried assignment is logged); no handler writes `audit_events` for reassignment | WF-03 | rms-api | Minor |

_Updated through Waves 2 + 3 + 4 (AUTH, INT, QUO, WF). Wave 5 appends here._


---

# Part 3 — Per-batch detail

# RMS Test Run — Per-Batch Detail (archive)

Consolidated detailed evidence from the 145-case run (2026-08-14). The
summary + per-case verdicts live in [test-results.md](./test-results.md);
the defect ledger in [test-findings.md](./test-findings.md). This file is
the fuller per-case evidence, kept for the remediation phase.


---

## Source: test-results-auth.md

# RMS Test Results — Auth Module (Wave 1)

Executed against the deployed `agentic` build on the live server
(`rms-api :0.0.0-staging`, image `ghcr.io/gentickelectronics/rms-api:0.0.0-staging`).

- **API base:** `https://rms.gentickit.com/api/v1`  (health → `{"db":"ok","status":"ok"}` HTTP 200)
- **SPA:** `https://rms.gentickit.com`
- **Date:** 2026-08-14
- **Scope:** TC-AUTH-01 … TC-AUTH-13 (Batch B-01 auth slice)
- **Predictions source:** `_docs/test-plan.md` Section 1 (pre-remediation `EXPECTED-*`) + Section 2 AMEND/KEEP notes.

## Summary

| Result | Count | Cases |
|---|---|---|
| PASS | 10 | AUTH-01, 03, 04, 05, 06, 09, 10, 11, 12, 13 |
| FAIL | 2 | AUTH-02, AUTH-07 |
| N/A (accepted) | 1 | AUTH-08 |
| BLOCKED | 0 | — |

- **EXPECTED-PASS → FAIL regressions: NONE.** All three predicted passes (01, 09, 10) held.
- **EXPECTED-FAIL → PASS flips (remediation confirmed): AUTH-03, 04, 05, 06, 11, 12** (six).
- **UNKNOWN → PASS: AUTH-13.**
- **Still FAIL as predicted:** AUTH-02 (frontend nav scoping — known plan hole, no ticket per test-plan §4b) and AUTH-07 (email delivery — now blocked by a deployment SMTP-config gate, see notes).

## Results table

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-AUTH-01 | PASS | API | `POST /auth/login` admin `teo.jordaan@gentick.co.za` → HTTP 200, body `{access_token, refresh_token, expires_in:900, user:{role:"admin", entity_type:"staff", staff_role:"admin"}}`. | EXPECTED-PASS → PASS. Seed admin logs in, `email_verified=true`. |
| TC-AUTH-02 | FAIL | Browser (SPA) | Logged in as technician fixture → lands `/queue`. Sidebar nav renders **Dashboard, Technician Queue, Jobs, Parts, Reports**. Direct nav to `/reports` renders "Operational Reports" (not blocked). | EXPECTED-FAIL → FAIL (unchanged). Technician sees the full staff nav and can reach non-technician pages — frontend route/nav role scoping was never restored (test-plan §4b: T-21 restored the **API** policy only; no ticket for FE scoping). Not a regression; a known uncovered gap. |
| TC-AUTH-03 | PASS | Browser (SPA) + API | Front_desk fixture → lands `/intake`. Nav: Dashboard, Job Intake, Jobs, Customers, Agents, Reports (no admin-only Staff/Settings). Intake screen renders working multi-unit form (customer/agent/"Walk-in" origin, Add Unit, repair type, Submit Intake). Job detail exposes a **Transition Job** (dispatch) control + Print jobcard. Job creation works (`POST /jobs` → 201, see AUTH-05 seed). | EXPECTED-FAIL → **PASS** flip. Pre-remediation basis was B3 (intake could not create a job); B3 is remediated — jobs create with `GT-`-prefixed numbers. Front-desk intake+dispatch capability present and functional. Caveat: I did not submit a full intake through the UI (job creation verified via API `POST /jobs`); assign-technician UI not separately exercised (dispatch = Transition Job control present). |
| TC-AUTH-04 | PASS | API | Seeded 2 jobs under one customer (RMSTEST) across agent A and agent B. Primary contact `GET /jobs` → 200, count 2 (both `GT-RMSTEST-0001` agentA + `GT-RMSTEST-0002` agentB). `GET /jobs/{agentB job}` as contact → 200 (own customer). | EXPECTED-FAIL → **PASS** flip. B3 remediated (jobs now creatable); main-customer contact sees all jobs across their agents, scoped to `main_customer_id`. |
| TC-AUTH-05 | PASS | API | Agent A `GET /jobs` → 200, count 1 (only own `GT-RMSTEST-0001`); agent B's job absent. Object-level: agent A `GET /jobs/{own}` → 200; agent A `GET /jobs/{agentB job}` → **403** `{"code":"FORBIDDEN"}`; agent B `GET /jobs/{agentA job}` → 403. | EXPECTED-FAIL → **PASS** flip. List scoping (`agent_id = self`, fail-closed on nil) and object-level `RequireJobAccess` both enforce agent isolation. |
| TC-AUTH-06 | PASS | API | On issued quote `Q-GT-RMSTEST-0001-1` (job A, `approval_authority:main_customer`): agent A `POST /quotes/{id}/approve` → **403** "you are not authorised to approve or reject quotes"; agent A `POST /quotes/{id}/reject` → 403. **Positive control** (AMEND): primary contact `POST /approve` → 200, quote `status:"approved"`. | EXPECTED-FAIL → **PASS** flip. Addresses the AMEND note: agent is refused AND the approve control is present/functional for the main customer (W5/T-45 remediated). |
| TC-AUTH-07 | FAIL | API + Mailpit | `POST /auth/forgot-password` for a verified contact and a verified staff account → both HTTP 200 `{"message":"If the email exists, a reset link has been sent"}` (enumeration-safe). **But no email is delivered** — Mailpit total stays at 1; API logs `WARN "SMTP not configured — skipping email"` for subject "Password Reset — Gentick RMS". | EXPECTED-FAIL → FAIL (unchanged, but the cause moved to config). Root cause: `email.Config.IsConfigured()` requires **non-empty SMTP username AND password**, but the deploy sets `SMTP_USERNAME=`/`SMTP_PASSWORD=` empty (Mailpit needs no auth) with `SMTP_HOST=mailpit`, `SMTP_PORT=1025`. So **all** transactional mail (reset, invitation, verification) is silently skipped. The reset *code path* (token issue, enumeration-safe 200, `POST /reset-password` with length check) is present; only delivery is broken. This is a **deployment/config defect**, not application logic — feeds a remediation pass. |
| TC-AUTH-08 | N/A (accepted) | — | TOTP/MFA not implemented. | Accepted by DEC-8 (TOTP out of scope); NFR-04 amended by T-59c. Not a failure. |
| TC-AUTH-09 | PASS | API | `POST /auth/login` wrong password (real account) → 401 `{"code":"UNAUTHENTICATED","message":"Invalid email or password"}`; unknown email → **identical** 401 body. AMEND timing probe (5x each): known-email+wrong-pw avg 477 ms vs unknown-email avg 462 ms — indistinguishable (within network noise). | EXPECTED-PASS → PASS. No enumeration via wording; the S14 timing oracle (predicted 1 ms vs 250 ms) is **not observable** on the deployed system. |
| TC-AUTH-10 | PASS | Browser (SPA) + API | Logged in as agent → `/portal`. Direct nav to `/reports`, `/staff`, `/dashboard` each redirect to `/portal` (blocked). API (AMEND): agent bearer token → **403** on `GET /staff`, `/parts`, `/reports/revenue`, `/units`, `/admin/audit`. | EXPECTED-PASS → PASS. SPA route guard AND the direct-API control (the AMEND concern, S1/S4) both refuse. |
| TC-AUTH-11 | PASS | API | Front_desk `POST /agents` (login-provisioned) → **201**. Negative control: technician `POST /agents` → **403**. | EXPECTED-FAIL → **PASS** flip. T-50 remediated: creation provisions a login (optional `password`); DEC-5 restores `deskMw` (admin+front_desk) write gate — technician correctly refused. |
| TC-AUTH-12 | PASS | API | Primary contact `POST /agents/invite` with own `main_customer_id` → **201**. Negative control: same contact invites for a different customer id → **403**. | EXPECTED-FAIL → **PASS** flip. S2 remediated: `authz.CanInviteAgent` gates on the session's `main_customer_id` + `is_primary`, not the request body. |
| TC-AUTH-13 | PASS | API | Non-primary (secondary) contact `POST /agents/invite` for own customer → **403** `{"code":"FORBIDDEN","message":"You are not authorized to invite agents for this customer"}`. | UNKNOWN → **PASS**. Primary-vs-non-primary gating exists and denies non-primary contacts (`is_primary` check in `CanInviteAgent`). |

## Fixture accounts created (namespaced; reusable by later waves)

All emails `@rmstest.local`; all `email_verified=true`. Passwords below (min policy ≥8).
Staff/contact/agent **email verification had to be set directly in the DB** on the
namespaced rows only, because the deployment's SMTP gate skips verification mail
(see AUTH-07). `AdminSetPassword` does **not** set `email_verified`, so there is no
API-only path to a loginable fixture on this deploy.

| Role | Email | Password | Entity id / notes |
|---|---|---|---|
| technician (staff) | `rmstest.technician@rmstest.local` | `RmstestTech123` | staff id `5e9cb17e-0574-4306-b217-c0a26e58ffab` |
| front_desk (staff) | `rmstest.frontdesk@rmstest.local` | `RmstestDesk123` | staff id `a04cab8f-af51-4a39-ba6a-89e120a167d4` |
| main_customer_contact (PRIMARY) | `rmstest.primary@rmstest.local` | `RmstestPrimary123` | customer `8182524f-f350-4df4-b121-8e27f15ce7de` (prefix RMSTEST) |
| main_customer_contact (non-primary) | `rmstest.secondary@rmstest.local` | `RmstestSecondary123` | same customer |
| agent A | `rmstest.agenta@rmstest.local` | `RmstestAgentA123` | agent `81046cda-ad50-4f65-8c5a-131cda8a336d`, customer RMSTEST |
| agent B | `rmstest.agentb@rmstest.local` | `RmstestAgentB123` | agent `0ff4a54a-95a1-40b8-8977-197fabb5ab10`, customer RMSTEST |

Supporting seed data (also namespaced):
- Main customer: **RMSTEST Corporation**, prefix `RMSTEST`, id `8182524f-f350-4df4-b121-8e27f15ce7de`.
- Unit: serial `RMSTEST-SN-0001`, id `b20da3bb-0e11-4edc-b8e4-5a64d8240021` (inserted via psql — no unit-create API endpoint exists outside intake).
- Job A: `GT-RMSTEST-0001`, id `50065296-d7ba-4288-a5ae-25c413017eee` (agent A) — now stage **Approved** (AUTH-06 positive control approved its quote).
- Job B: `GT-RMSTEST-0002`, id `13de6ba0-e54d-42fd-880d-ddc5f573fb6c` (agent B) — stage received.
- Quote: `Q-GT-RMSTEST-0001-1` on job A, ZAR, subtotal 500.00 / VAT 15% 75.00 / total 575.00, status approved.
- Extra agents created by AUTH-11/12 role checks: `rmstest.agentfd@`, `rmstest.agentfd2@` (front_desk-created, no login), and agent-invitations for `rmstest.invprimary@`/`rmstest.invprimary2@` (no login).

## Failures — actual vs expected

- **TC-AUTH-02 (FAIL):** Expected a technician to see only technician-scoped navigation. Actual: technician sidebar shows Dashboard, Technician Queue, Jobs, Parts, Reports and can directly open `/reports`. Frontend nav/route role scoping is not implemented (API gate only). Known gap — test-plan §4b lists "Frontend route/nav role scoping" as having no ticket; will remain red until FE scoping is added. Not a regression.
- **TC-AUTH-07 (FAIL):** Expected an agent/user to receive a self-service password-reset email and reset via the link. Actual: `forgot-password` returns 200 but no mail is sent. Root cause is a deployment config gate (`IsConfigured()` demands non-empty SMTP user+pass; Mailpit runs auth-less with empty creds), so reset/invitation/verification mail is all skipped. Application reset logic itself is intact. Recommend either (a) relax `IsConfigured()` to `Host != "" && Port > 0` (send when a host is set even without auth), or (b) configure dummy SMTP creds so the gate passes. This same defect blocks email-based invitation acceptance (relevant to later AUTH-07/INT-20/COM-13 waves).

## Method notes
- API assertions via `curl`; browser assertions via the built-in Browser pane against `rms.gentickit.com` (the Chrome extension was not connected; the pane does not composite frames so screenshots were unavailable — DOM/accessibility reads and network/console inspection were used instead, and login forms were driven via React-compatible input events).
- Admin password was never logged or echoed.
- No application code changed; nothing committed/pushed. The only DB writes were: creating the namespaced fixtures above, and setting `email_verified=true` on those same `rmstest.` rows (required to make fixtures loginable given the SMTP gate). No real/existing accounts were mutated beyond logging in as the admin.

---

## Source: test-results-int-a.md

# Test Results — Wave 2A (TC-INT-01 … TC-INT-12)

- **System under test:** deployed `agentic` build, rms-api reports `version 0.1.0`, `db:ok`. API base `https://rms.gentickit.com/api/v1`.
- **Layer:** API-only (no browser). Verified behaviourally via HTTP status codes and records fetched back via GET. Where a DB-only column cannot be read back through any endpoint, that is stated explicitly.
- **Executed:** 2026-08-14 by Pegasus (Wave 2A).
- **Namespace created:** `RMSTESTA` — customer `RMSTESTA Alpha Corporation` (prefix `RMSTESTA`, id `ce927b2d-…bd4e1`); primary contact `rmstesta.primary@rmstest.local`; agent `RMSTESTA Agent One` (`rmstesta.agent1@rmstest.local`, id `78cf61da-…09177`); invited+verified agent `RMSTESTA Self Agent` (`rmstesta.agentself@rmstest.local`, agent id `c555b460-…df05c`); jobs `GT-RMSTESTA-0001 … 0005`.

## Summary counts

| Result | Count |
|---|---|
| PASS | 10 |
| FAIL | 2 |
| BLOCKED | 0 |
| N/A | 0 |

**Regressions (EXPECTED-PASS → FAIL): none.** The one EXPECTED-PASS case (INT-01) passed. Every other PASS is a healthy **EXPECTED-FAIL → PASS flip** — the Sprint remediation landed. INT-06/07 remain FAIL exactly as predicted (no ticket).

## Results table

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-INT-01 | PASS | API | `POST /customers` → 201 (`RMSTESTA Alpha Corporation`); `POST /customers/{id}/contacts` → 201 (person/email/phone); `GET /customers/{id}` returns all four fields incl. contact under `contacts[]`. | Matches EXPECTED-PASS. All four mandatory fields (Corporation Name on customer; Contact Person/Email/Phone on contact) persist and are retrievable. Contact phone pinned to valid E.164 `+27821110001` (per Section 2 AMEND). |
| TC-INT-02 | PASS | API | `POST /customers` with blank prefix+corporation_name → **400** `VALIDATION_ERROR "customer_prefix and corporation_name are required"`; `POST …/contacts` blank person+email → **400** `"contact_person and contact_email are required"`. | Flip vs EXPECTED-FAIL. Core assertion "save blocked on missing required fields" holds at the API. Caveat: the missing-required response is a single combined message, not a per-field `details[]` array (that array **is** populated for format errors — see INT-03). Inline per-field rendering is a frontend concern not observable via API. |
| TC-INT-03 | PASS | API | Contact `contact_email:"notanemail"` → 400 with `details:[{field:contact_email,issue:invalid email address}]`; valid email + `contact_phone:"abc"` → 400 with `details:[{field:contact_phone,issue:must be E.164 …}]`; agent bad email+phone → 400 field detail. | Flip vs EXPECTED-FAIL. Email + E.164 validation restored on both customer-contact and agent paths, with field-level `details[]`. Record not saved. |
| TC-INT-04 | PASS | API | Agent created 201 linked to `main_customer_id=ce927b2d…`. `GET /agents/{id}` → `corporation_name:"RMSTESTA Alpha Corporation"`; `GET /agents?customer_id=…` list row carries the same `corporation_name`. | Flip vs EXPECTED-FAIL. **T-47 join confirmed** — `ListAgents` now `JOIN main_customers`, so the Customer column is populated on the list endpoint (previously permanently blank). Agent associated to correct customer. |
| TC-INT-05 | PASS | API (write verified indirectly; field values DB-only) | `POST /agents/invite` → 201 (token in body); `POST /auth/accept-invitation` with `consent_whatsapp:true` → **200** + token pair. | Flip vs EXPECTED-FAIL, **on the DEC-6 invitation-acceptance path only** (Section 2 AMEND narrowing). `recordAgentConsent` runs inside `AcceptInvitation` and aborts the whole request on INSERT failure (`service.go:360`), so the 200 proves a `consent_records` row was written: `entity_type='agent'`, `consent_type='whatsapp'`, `action='granted'`, `wording_version='1.0'`, `source='invitation_acceptance'`, `ip_address` from nginx `X-Real-IP`, timestamp DB default. **Loud caveat:** the strict FR-INT-03 ("consent at first-time *web registration*") is NOT implemented — no WhatsApp consent field on customer/contact creation. Consent row field values are not readable via any endpoint (no consent GET; `/admin/audit` reads `audit_events`, not `consent_records`). |
| TC-INT-06 | FAIL | API + route/handler inspection | No consent opt-out / revocation route exists in `internal/router/router.go`; only consent write site is `recordAgentConsent` (invitation acceptance). `GET /auth/me` exposes no consent toggle. | Matches EXPECTED-FAIL. **No ticket** — revocation (FR-INT-04) was never restored. No endpoint writes a revocation event to `consent_records`. |
| TC-INT-07 | FAIL | API + route/handler inspection | No annual-consent-audit route or workflow anywhere in the router; `consent_records` has no aging/flagging reader. | Matches EXPECTED-FAIL. **No ticket** — FR-INT-05 annual audit not implemented. |
| TC-INT-08 | PASS | API | `POST /jobs/intake` `originator_type=main_customer` → **201**; `shipment.origin="main_customer"`, `originator_main_customer_id` set, `originator_agent_id=null`; job `GT-RMSTESTA-0001`, `originator_type=main_customer`. | Flip vs EXPECTED-FAIL. **B6 fixed** — `shipmentOrigin("main_customer")` now yields the enum label `main_customer` and `chk_shipment_originator` is satisfied. |
| TC-INT-09 | PASS | API | Verified agent token → `POST /jobs/pre-registrations` → **201**; job `GT-RMSTESTA-0005`, `originator_type=agent`, `agent_id`+`main_customer_id` derived from session, stage `received`, `shipment_id=null` (pre-registration). | Flip vs EXPECTED-FAIL. **W4 / DEC-11 fixed** — agent portal is intake-capable, not read-only; ownership derived server-side. Note: agent token required email verification first (middleware `EMAIL_NOT_VERIFIED` gate) — verified via Mailpit link before this call. |
| TC-INT-10 | PASS | API | `POST /jobs/intake` `originator_type=front_desk`, `originator_id=<staff>` → **201**; `shipment.origin="walk_in"`, **both** `originator_main_customer_id` and `originator_agent_id` = null; job `GT-RMSTESTA-0002`. | Flip vs EXPECTED-FAIL. **B6 fixed** — `chk_shipment_originator` (both originator IDs NULL for walk_in) now satisfied; front-desk intake no longer 500s. |
| TC-INT-11 | PASS | API | `POST /jobs/intake` with originator/owner omitted → **400** `VALIDATION_ERROR "main_customer_id is required"`. | Flip vs EXPECTED-FAIL. Save is blocked pending identification. API backstop: intake requires `main_customer_id`, `agent_id`, `originator_type`, `originator_id` — a walk-in cannot be saved without identifying the owner. (The "inline prompt" wording is a UI concern; the gate itself is enforced server-side.) |
| TC-INT-12 | PASS | API | `POST /jobs/intake` with 2 units → **201**; one shipment `9536a98f-…`, two jobs `GT-RMSTESTA-0003`/`GT-RMSTESTA-0004`, both sharing that `shipment_id`, job numbers unique. | Flip vs EXPECTED-FAIL. Multi-unit booking restored (§7 loss reversed) and B3 numbering fixed — each unit gets its own per-customer-sequenced job number under one shipment. |

## Notable observations

- **Job numbering (B3/T-12) works end-to-end.** `GT-RMSTESTA-0001` through `0005` were issued in the correct `GT-{PREFIX}-{NNNN}` format, per-customer sequenced (this new `RMSTESTA` customer started at 0001, independent of the shared `RMSTEST` sequence). This is corroborating evidence for the B-02 batch.
- **`email_verified` gate on invited agents.** `accept-invitation` returns a token pair, but the auth middleware rejects subsequent API calls with `EMAIL_NOT_VERIFIED` until the emailed verification link is used. This is stricter than the audit's gap-9/S12 description ("returns a working token pair for email_verified=false") — the token exists but is not usable pre-verification. Flagged for the auth batch (B-01/B-05), not scored here.
- **INT-05 residual gap.** Strict FR-INT-03 web-registration consent capture remains unimplemented; only the invitation-acceptance path writes consent. This is the AMEND narrowing in test-plan.md Section 2, not a regression.

## Method notes

- Admin auth via `POST /auth/login` (staff/admin). Agent auth via invitation acceptance → email verification (Mailpit) → `POST /auth/login`.
- All intake calls used `confirmed_no_extra_units:true` (T-52 receive gate) and E.164 phone literals.
- Nothing outside the `RMSTESTA` namespace was mutated. No app code changed, no commits.

---

## Source: test-results-int-b.md

# Test Results — Wave 2B (TC-INT-13 … TC-INT-24)

- **System under test:** deployed `agentic` build, `https://rms.gentickit.com/api/v1` (health 200; API reports `version: 0.1.0`).
- **Layer:** API + Mailpit + direct DB observation (`webhook_events`, `consent_records`, `agent_invitations`). No browser (parallel batch owns it).
- **Namespace:** `RMSTESTB` (isolated from `RMSTEST` / `RMSTESTA`).
- **Run date:** 2026-08-14.
- All 12 pre-remediation expectations were `EXPECTED-FAIL` or `UNKNOWN`; remediation tickets are marked DONE. This run confirms actual behaviour. **No `EXPECTED-PASS → FAIL` regressions in this batch.** Nine cases flipped from FAIL/UNKNOWN to PASS.

## Fixtures created (RMSTESTB)

| Entity | Prefix / email | ID |
|---|---|---|
| Main customer 1 | `RMSTESTB1` (RMSTESTB One Corporation) | `5a2ae8d1-4fe7-47d9-b2db-63299f90b0fe` |
| Main customer 2 | `RMSTESTB2` (RMSTESTB Two Corporation) | `53e72bcd-19a2-405e-b094-365f768e7cb6` |
| Agent (under C1, login) | `rmstestb.agent1@rmstest.local` / `RmstestbAgent1123` | `6b0a0853-627e-4f11-b28a-4218bdfc5588` |
| Agent (under C2, login) | `rmstestb.agent2@rmstest.local` / `RmstestbAgent2123` | `201ca3b8-8d8b-43b5-8dcd-5a4dc90c97bc` |
| Primary contact (under C1, login) | `rmstestb.primary1@rmstest.local` / `RmstestbPrimary1123` | `cce7dc1c-72a2-4df1-bd96-b5c5b70260ee` |
| Agent (invited+accepted, TC-INT-20) | `rmstestb.invitee@rmstest.local` / `RmstestbInvitee123` | `24a76459-fa63-4b19-8746-d8a0d0bbc0d8` |
| Invitations (pending/expired tests) | `rmstestb.pending@…`, `rmstestb.expired@…` | — |

`email_verified` was set `true` on the three provisioned RMSTESTB logins via DB (same treatment as Wave 1 fixtures) so they could authenticate. Jobs created: `GT-RMSTESTB1-0001…0005`, `GT-RMSTESTB2-0001`.

## Results

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-INT-13 | PASS | API | Intake under `RMSTESTB1` → `GT-RMSTESTB1-0001`; matches `^GT-[A-Z0-9]{2,10}-[0-9]{4,}$` (verified in code path `nextJobNumber`: `GT-%s-%04d`, per-customer `MAX(substring…)` under `SELECT … FOR UPDATE` on `main_customers`). | Pre: `EXPECTED-FAIL` (B3, `ACME-00001`, no `GT-`). **Flip → PASS.** Remediation T-12 confirmed: `GT-`+customer-prefix+zero-padded sequence, format constraint satisfied. |
| TC-INT-14 | N/A | API/code | No `GT-GEN`, no generic/`UNKNOWN`/`GEN` customer in `main_customers` (prefixes present: RMSTEST, RMSTESTA, RMSTESTB1, RMSTESTB2); code grep finds no `GT-GEN` / GEN-prefix fallback. `POST /jobs/intake` **requires** both `main_customer_id` and `agent_id` (handler rejects nil), so no unlinked-job path exists — a walk-in is still filed under a `main_customer` (shipment `origin='walk_in'`, but the job number derives from the customer prefix). | Pre: `UNKNOWN`. Matches plan revisit note (line 226): "N/A by design; T-12 may not keep a GT-GEN fallback." Confirmed there is no GT-GEN fallback and no unlinked-job numbering path → **N/A by design.** |
| TC-INT-15 | PASS | API | Sequence proven independent per customer: C1#1 = `GT-RMSTESTB1-0001`, C2#1 = `GT-RMSTESTB2-0001`, C1#2 = `GT-RMSTESTB1-0002`. Two freshly-created customers used to prove independence. | Pre: `EXPECTED-FAIL` (B2/B3, single global `job_number_seq`). **Flip → PASS.** Numbering is per-prefix `MAX`, not a global sequence. |
| TC-INT-16 | FAIL | API/schema | All textual per-unit fields ARE captured & persisted (`GetJob` on `GT-RMSTESTB1-0001`: `fault_description`, `accessories`="cable", `unit_type`/`serial` via unit upsert, `repair_type_id`). BUT condition-on-arrival photos (FR-INT-09) are **not** capturable at intake: `intakeUnit` struct has no photo field, and `job_photos` has no photo-type/condition column (only `caption`, `visibility`). Photos are a separate post-intake endpoint `POST /jobs/{id}/photos` with no condition-on-arrival semantics. | Pre: `EXPECTED-FAIL` (§7, FR-INT-09 photos dropped). **Still FAIL — no regression, remediation incomplete for the photo requirement.** Text fields restored; the condition-on-arrival photo capture the case names remains absent. T-51 did not restore it. |
| TC-INT-17 | PASS | API | Same serial `RMSTESTB-SN-B1-0001` intaken twice → both jobs (`GT-RMSTESTB1-0001` and `-0003`) resolve to the **same** `unit_id` `298b2165-…`. `GET /units/{id}/history` returns both jobs (prior repair history auto-linked by serial, case-insensitive upsert on `lower(serial_number)`). | Pre: `EXPECTED-FAIL` (§7/T-54, banner dropped; B4 units upsert 42P10). **Flip → PASS.** Serial→unit linkage and history endpoint both work; the units upsert (B4) no longer raises 42P10. (Banner rendering itself is UI, but the data linkage FR is satisfied.) |
| TC-INT-18 | PASS* | API + DB + Mailpit | `job.received` webhook event emitted and a durable `webhook_events` row written for every intake job (`GT-RMSTESTB1-0001…0005` all present). W1 (0 rows) is remediated — `emitEventAsync` now uses `context.WithoutCancel`, so rows persist. **Caveat:** `delivered_at` is NULL for all events; n8n returns HTTP 404 "webhook gentick-events is not registered" — the n8n workflow is not active. No jobcard email reaches Mailpit (Mailpit holds only direct-SMTP invitation/verification/reset mail). | Pre: `EXPECTED-FAIL` (W1/T-15, `job.received` never emitted). **API emission flipped → PASS** (the app-code defect these tickets covered is fixed). **\*End-to-end email NOT delivered** — blocked on n8n infra (batch B-11a precondition "n8n workflows imported and active" is unmet on this server), not an app-code defect. Jobcard email is DB-observable only. |
| TC-INT-19 | PASS | API | Job `GT-RMSTESTB1-0001` (customer C1, agent A1): primary contact (`main_customer_contact`) `GET /jobs/{id}` → 200, sees job_number; agent A1 → 200, sees job_number. Negative control: agent A2 (different customer) → **403**. Portal scoping enforced by `RequireJobAccess`. | Pre: `EXPECTED-FAIL` (B3). **Flip → PASS.** Both parties view; cross-customer access denied. |
| TC-INT-20 | PASS | API + DB + Mailpit | Main-customer-led: logged in as primary contact `rmstestb.primary1`, `POST /agents/invite` for `rmstestb.invitee` → 201 with token + `expires_at` (now+7d). Invite email landed in Mailpit ("You've been invited to Gentick RMS", to invitee). Accepted with `consent_whatsapp:true` → agent created, `invited_at` + `invitation_accepted_at` stamped, invitation `accepted_at`/`accepted_agent_id` set. `consent_records` row written: `subject_type=agent, channel=whatsapp, action=granted, wording_version=1.0, recorded_via=invitation_acceptance`, with IP + user_agent + timestamp. | Pre: `EXPECTED-FAIL` (W3 no email, W6 no `invited_at`, D12 no consent). **Flip → PASS on all three.** `authz.CanInviteAgent` correctly authorizes the primary contact. |
| TC-INT-21 | PASS* | API + DB + Mailpit | `ListAgents` returns `invited_at` for every agent (W6 root cause fixed — Invite-vs-Edit toggle now has its key; was "Not invited" forever). Fresh pending invitation stores `expires_at` = now+7d. Resend = re-invite same email: mints a **new** token and sends a **new** email (Mailpit 10→11). | Pre: `EXPECTED-FAIL` (W6). **Flip → PASS** on the backing behaviour. **\*The literal Agents-page rendering** of the expiry countdown and a "Resend" button is UI — deferred to a browser pass. No API endpoint lists pending-invitation `expires_at`; the value lives in `agent_invitations`. |
| TC-INT-22 | PASS | API + DB | Created invitation, force-expired it (`UPDATE agent_invitations SET expires_at = now()-'1 day'` on my RMSTESTB token), then `POST /auth/accept-invitation` → **HTTP 400 `BUSINESS_RULE_VIOLATION` "invitation expired"**. Enforced in `Service.AcceptInvitation` (`time.Now().After(inv.ExpiresAt)`). | Pre: `UNKNOWN`. **Confirmed implemented → PASS.** Token expiry is enforced server-side (7-day TTL). |
| TC-INT-23 | N/A | API/code | No main-customer originator pre-registration path exists (DEC-11: agent-only intake variant). `POST /jobs/pre-registrations` is gated to the `agent` role; a primary contact calling it → **403 "Insufficient permissions"**. Main customers cannot pre-register units, so "extra units discovered for a main-customer originator" cannot arise. | Pre: `UNKNOWN` / N/A by design. **Confirmed N/A by design** — the N/A trigger ("revisit if built") is unchanged; DEC-11 built an agent-only variant. |
| TC-INT-24 | PASS* | API + DB | Agent A1 pre-registered `RMSTESTB-PREREG-24A` → `GT-RMSTESTB1-0004`, `shipment_id=NULL`. Admin intake for C1/A1 with `[PREREG-24A (announced), EXTRA-24B]`: the announced unit was attached to the shipment (0004, `job.received`); the unannounced `EXTRA-24B` → new job `GT-RMSTESTB1-0005` flagged `isExtra` → **`job.extra_unit_discovered` webhook_events row written** for 0005 (plus `job.received`). `job.extra_unit_discovered` now has a live call site (was a zero-call-site constant). | Pre: `EXPECTED-FAIL` (W1; T-15 constant unused). **API emission flipped → PASS** — extra-unit detection + event emission verified. **\*Delivery to n8n fails** (`delivered_at` NULL, HTTP 404 workflow-not-registered) — same n8n infra gap as TC-INT-18; downstream notification not actually sent. |

## Summary counts

- **PASS: 8** (TC-INT-13, 15, 17, 19, 20, 22 clean; TC-INT-18, 24 PASS on API emission with n8n-delivery caveat)
- **PASS-with-UI-deferral: 1** (TC-INT-21 — backing behaviour PASS; visual expiry/resend rendering deferred to browser pass)
- **FAIL: 1** (TC-INT-16 — condition-on-arrival photos FR-INT-09 still not capturable at intake)
- **N/A: 2** (TC-INT-14 no GT-GEN/unlinked-job path by design; TC-INT-23 no main-customer originator path by design)
- **BLOCKED: 0**
- **Regressions (EXPECTED-PASS→FAIL): 0**

## Cross-cutting findings

1. **n8n delivery is broken cluster-wide (affects TC-INT-18, TC-INT-24 end-to-end, and every stage-transition notification).** `webhook_events` rows are written and signed correctly, but every delivery attempt gets HTTP 404 from `http://rms-n8n:5678/webhook/gentick-events` — "The requested webhook POST gentick-events is not registered. The workflow must be active…". `delivered_at` is NULL on all 14 events in the table (job.received, agent.invited, quote.ready, quote.approved, invoice.created, job.extra_unit_discovered). **The n8n workflow needs importing/activating** for any webhook-driven email/notification to reach a recipient. This is infra (batch B-11a precondition), not an app-code defect — the API side of the transport is verified working.
2. **Direct-SMTP emails DO work** (Mailpit receives invitation, verification, and password-reset mail from the API directly — bypassing n8n). So invitation delivery (TC-INT-20/21) is genuinely end-to-end; only the n8n-mediated jobcard/notification mail (TC-INT-18/24) is not.
3. **TC-INT-16** is the one genuine remaining app-code gap in this batch: condition-on-arrival photos (FR-INT-09) have no representation in the intake payload or the `job_photos` schema (no photo-type/condition column). Everything else the case names is captured.

---

## Source: test-results-int-c.md

# Test Results — Wave 2C (TC-INT-25 … TC-INT-35)

- **System under test:** deployed `agentic` build. SPA `https://rms.gentickit.com`, API `…/api/v1`. rms-api image `ghcr.io/gentickelectronics/rms-api:0.0.0-staging`.
- **Layer:** UI/validation-focused. Driven through the in-app Browser tools (screenshots unavailable in this pane — assertions taken from the DOM, `localStorage`, live network reads, and one `webhook_events` DB read over SSH). Cross-checks and setup via the API. Where a behaviour is only observable in source, that is stated.
- **Executed:** 2026-08-14 by Pegasus (Wave 2C).
- **Session mechanics:** the SPA keeps the JWT in `localStorage` (`rms_access_token` / `rms_refresh_token`, 15-min access TTL) and bootstraps via `/auth/me`. Sessions were established by injecting valid API-issued tokens for each fixture and reloading (the pre-existing browser tab's login-form submit did not persist tokens; not in this batch's scope — flagged for the auth batch).
- **`RMSTESTC` entities created:**
  - Job `GT-RMSTEST-0003` — front-desk walk-in intake, unit serial `RMSTESTC-INT26-01`, under `RMSTEST Corporation`, owning agent `RMSTEST Agent A` (INT-26/INT-25).
  - Contact **RMSTESTC Contact One** (`rmstestc.contact1@rmstest.local`, id `8651d1a5-…d29842`) added to `RMSTEST Corporation`, non-primary; an admin-set password was applied but login is blocked by `EMAIL_NOT_VERIFIED` (INT-28). Left in place.
  - Customer **RMSTESTC Corporation** (prefix `RMSTESTC`, id `6ad434d9-…a4630be`) with two contacts (`rmstestc.primary2@`, `rmstestc.nonprimary@`); used for INT-34/35 and both contacts were then soft-deleted during INT-35 — the corporation now has zero contacts. All `RMSTESTC` namespace; no shared fixtures mutated (RMSTEST Corporation's own primary/secondary contacts left intact).

## Summary counts

| Result | Count |
|---|---|
| PASS | 6 |
| FAIL | 5 |
| BLOCKED | 0 |
| N/A | 0 |

**Regressions vs the plan (EXPECTED-PASS → FAIL): none** — every case in this batch was predicted EXPECTED-FAIL (INT-35 was UNKNOWN). Six cases flip EXPECTED-FAIL → PASS (the remediation landed). Five stay FAIL exactly as predicted.

**Defects found (loud — behaviour regressed vs the OLD source system even though the plan predicted EXPECTED-FAIL):**
1. **INT-35 — the primary contact CAN be removed.** No client guard (Delete button enabled on the primary) and no server rejection: deleting the sole primary succeeded and left the corporation with zero contacts. The one-primary invariant is unprotected on the delete path.
2. **INT-34 — the one-primary invariant is not enforced on the demote path.** Unticking "primary" on the sole primary and saving succeeded, leaving the corporation with **zero** primaries (`uq_mcc_primary` only blocks two primaries, not zero). The current-primary checkbox is not disabled and shows no hint. Additionally the login **email is editable** in the edit-contact dialog (the requirement says it must not be).

**INT-26 (the T-52 checkbox) works** — confirmed end to end (see below).

## Results table

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-INT-25 | PASS | UI intake + DB (`webhook_events`) | Front-desk walk-in intake → `POST /jobs/intake` **201**, job `GT-RMSTEST-0003`, `shipment.origin=walk_in`, `originator_type=front_desk`, agent identified. `webhook_events` rows for the job: `job.received` **and** `job.extra_unit_discovered`; the `job.received` payload carries `agent_id=81046cda…` (RMSTEST Agent A), `main_customer_id=8182524f…` (RMSTEST Corporation), `originator_type=front_desk`. `delivered_at` NULL. | Flip vs EXPECTED-FAIL. Core criterion (owner routing on `job.received`) holds — payload routes to the front-desk-identified agent + main customer. `delivered_at` NULL = queued pending n8n import (agreed local criterion, per TC-INT-18/register). `job.extra_unit_discovered` also fired because Agent A had pending pre-registrations, so the walk-in unit was a baseline mismatch — consistent with the design (not a failure of this case). |
| TC-INT-26 | PASS | UI + network | Checkbox present (`/intake`). With customer + agent + all unit fields valid but the checkbox **unticked**, Submit Intake is `disabled=true`; ticking it flips Submit to `disabled=false`. Submitting → `POST /jobs/intake` **201**, job `GT-RMSTEST-0003`. Source `IntakePage.tsx:805-811` gates Submit on `!confirmedNoExtraUnits`; line 341 sends `confirmed_no_extra_units:true`; server T-52 gate would 400 without it. | **KEY T-52 case — works.** Flip vs EXPECTED-FAIL. Submit is blocked until the confirmation is ticked (disabled-button gate + a `handleSubmit` toast backstop + the server backstop). Ticked submit sends the attestation and the API accepts (201). |
| TC-INT-27 | FAIL | UI + source | `/intake` exposes only **Cancel** and **Submit Intake** — no "Save draft" control. No draft route, state, or persistence anywhere in `IntakePage.tsx`. | Matches EXPECTED-FAIL. Feature never implemented in the migrated stack (as in the source stack, defect D-10). Correctly still absent. |
| TC-INT-28 | FAIL | UI + API + email sink | Part A works: `POST /customers/{id}/contacts` **201**; new contact **RMSTESTC Contact One** lists under RMSTEST Corporation **without** a Primary badge, contact count 2→3, existing primary unchanged. Part B (T-50 login) fails: staff "Add Contact" has **no password field** and sends **no** invite/verification email (Mailpit shows nothing for the address; the reset pipeline works — `rmstest.primary` got a reset mail at 10:20). Admin `set-password` succeeds (an auth row exists) but login then returns **403 `EMAIL_NOT_VERIFIED`**, and no verification mail is ever dispatched; `UserResetActions` (Send Reset / Set Password) is admin-only so front desk cannot provision at all. | Matches EXPECTED-FAIL on the T-50 half. Front desk can create a contact that cannot access the portal: no password entry, no invitation/verification email at creation, and the email-verified gate blocks login even after an admin sets a password. Creation-and-listing half is healthy. |
| TC-INT-29 | PASS | UI (DOM) | "Add Contact" (scoped to the expanded customer). Blank submit → **2 inline errors at once**: "Contact person is required", "Email is required". `email=notanemail`, `phone=abc` → **3 inline errors at once**: "Contact person is required", "Enter a valid email address", "Enter a valid phone number, e.g. +27111234567". No contact created. | Flip vs EXPECTED-FAIL. Multiple field-level errors render simultaneously (inline `<p class=text-red-500>`), not one-at-a-time. **"Missing customer" sub-assertion is N/A** — the migrated flow scopes contact creation to the expanded customer row; there is no customer selector to leave blank. Email + E.164 validation restored (`lib/validation.ts`, T-55/T-56). |
| TC-INT-30 | PASS | UI (DOM) | Agents → New Agent. Blank submit → **5 inline errors at once**: "Customer is required", "Agent name is required", "Email is required", "Address line 1 is required", "City is required". Then `email=notanemail`, `phone=abc` → email + phone format errors render **together** with the remaining required errors ("Enter a valid email address", "Enter a valid phone number, e.g. +27111234567"). No agent created. | Flip vs EXPECTED-FAIL. All invalid/missing fields surface simultaneously, inline per field. **Password sub-part N/A** — the migrated staff agent form has no password field (agents are provisioned via the separate Invite flow). |
| TC-INT-31 | FAIL | UI (DOM) + source | Logged in as the primary contact (`rmstest.primary`, `is_primary=true`). `/portal` renders only "My Jobs" (read-only job tracking); no "+ Add an agent" / "Your agents" control anywhere (DOM scan `hasAddAgentControl=false`). No add-agent component exists in the frontend. | Matches EXPECTED-FAIL. The portal self-service agent-management feature (FR-INT-11) was not migrated — the customer portal is read-only. The "surface all errors at once" assertion is therefore untestable (no form). (Bonus: my walk-in job `GT-RMSTEST-0003` is visible in the primary's portal — customer-scoped job visibility works.) |
| TC-INT-32 | FAIL | UI (DOM) + source | Logged in as agent A (`rmstest.agenta`). `/portal/intake` = "Pre-Register a Repair": a **single-unit** form — search an **existing** unit by serial, one Fault Description, Accessories. No multi-unit rows, no per-unit Serial/Unit-Type entry, no "Add unit". Submit Repair disabled until a unit is selected and a fault entered (`disabled=true` when empty). | Matches EXPECTED-FAIL. The multi-unit agent "Book in a repair" with per-unit field validation was dropped in migration (§7/W4 intake loss). The migrated agent flow only pre-registers one already-existing unit, so "flags all missing unit fields on all units / blank units not dropped" cannot apply. |
| TC-INT-33 | PASS | UI (DOM) | Staff `/intake`, customer + agent + checkbox set, Unit 1 complete, Unit 2 blank → Submit → **3 simultaneous toasts**: "Unit 2: Serial number is required", "Unit 2: Unit type is required", "Unit 2: Fault description is required". No navigation, no `POST /jobs/intake` (blank unit not dropped, submission blocked). Agent selection enforced via the disabled Submit gate (`!agentId`). | Flip vs EXPECTED-FAIL. All missing unit fields on the incomplete unit surface **at once** and the blank unit is not silently dropped. **UX differs from the old inline model:** unit errors render as toasts (not inline per-field), and the agent/customer/checkbox requirements are enforced by disabling Submit rather than by an inline "select the agent" error. Core assertion (block + surface-all + no-drop) holds. |
| TC-INT-34 | PASS | UI (DOM) + server probe | On RMSTESTC Corporation: (1) edited non-primary name + phone → "Contact updated", persisted. (2) ticked Primary on it → it gains the Primary badge and the old primary is demoted — exactly one primary (demote-in-transaction holds). (5) blank name + `phone=abc` → **2 inline errors at once**, dialog stays open, no save. | Flip vs EXPECTED-FAIL on the core (edit + reassign primary, promotion demotes the old primary). **Two flagged defects:** (a) the login **email field is editable** in the edit dialog (requirement: not editable); (b) the current-primary checkbox is **not** disabled and has **no hint**, and unticking it and saving **succeeded, leaving the corporation with ZERO primaries** — the "never left without a primary" invariant is not enforced client- or server-side (`uq_mcc_primary` only blocks a second primary). |
| TC-INT-35 | FAIL | UI (DOM) | Non-primary delete works: Trash → confirm dialog → "Contact deleted", row disappears, count decrements (soft delete per `deleteContact`). **Primary delete is NOT guarded:** the primary's Delete button is enabled (`disabled=false`), and clicking Delete → confirm → **"Contact deleted"**, leaving the corporation with **zero contacts** ("No contacts yet"). Server accepted it. | Resolves the plan's UNKNOWN to a **defect**: the "primary contact cannot be removed" invariant does not exist — no disabled Remove button, no hint, and the server permits deleting the last/primary contact. Also: the confirmation is a single-step modal dialog (not the old inline two-step), though still an in-app confirm (no browser dialog). |

## Notable observations

- **INT-34 + INT-35 expose the same underlying gap:** the migrated stack does not protect the one-primary-per-corporation invariant. A corporation can be driven to zero primaries either by demoting the sole primary (INT-34) or by deleting it (INT-35). `uq_mcc_primary` enforces "at most one primary", never "at least one". This is a real regression vs the source system (old register showed the primary Remove disabled and a no-zero-primary guard). Recommend a follow-up ticket — the primary drives quote-approval routing and agent invitation.
- **Validation model, by surface:** dialog forms (Customers contacts, Agents) use **inline per-field** errors collected on one submit (matches the old "all errors at once" intent). The staff **Intake** form is a hybrid — header fields (customer/agent/received-by) and the T-52 checkbox gate the **disabled** Submit button, while per-unit errors fire as **simultaneous toasts**. Both surface multiple errors at once; only the rendering differs.
- **Email-verification gate on new contacts (INT-28):** a `main_customer_contact` auth row is created at contact creation (admin set-password succeeds), but login is blocked by `EMAIL_NOT_VERIFIED` with no verification email ever sent through the staff flow — same class of gate seen on invited agents in Wave 2A.
- **Login-form session bug (out of scope, flagged):** submitting the SPA login form in the pre-existing tab returned `POST /auth/login 200` but did not persist tokens or redirect (a `GET /api/v1/staff` 403 for front_desk appears in the post-login bootstrap). Sessions here were established by injecting API-issued tokens. This belongs to the auth batch, not scored in 2C.

## Method notes

- Radix Select popups do not open under a synthetic CDP click (open-then-close toggle) and screenshots are unavailable in this pane; selects were opened with a dispatched `pointerdown` and options committed with a full pointer-event sequence, verified against the trigger's rendered value. Controlled inputs were set via the native value setter + `input` event.
- `webhook_events` was read directly (no read API exists) via `docker exec postgres psql` using the connection string sourced from the `rms-api` container env; the DB password was never printed.
- Nothing outside the `RMSTESTC` namespace was mutated. No application code changed, no commits.

---

## Source: test-results-wf.md

# Wave 3 (Workflow) — Test Execution Results

System under test: `https://rms.gentickit.com` (SPA) / `/api/v1` (API), image `rms-api:0.0.0-staging`.
Executed: 2026-08-14. Namespace: `RMSTESTWF` / `rmstestwf.*@rmstest.local`.
All 14 cases were predicted `EXPECTED-FAIL` in `test-plan.md` (chiefly because of B3 —
"no job can be created"). B3 is resolved: jobs create cleanly with correct
`GT-<PREFIX>-<NNNN>` numbers. **13 of 14 now PASS**; the remaining failure
(TC-WF-14) stays failing for a different, narrower reason than predicted.

Layer legend: **API** = curl against `/api/v1`; **UI** = in-app browser DOM read of the SPA; **DB** = direct `psql` on the server.

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-WF-01 | PASS | API | Drove `GT-RMSTESTWF-0001` received→diagnosed→quoted→approved→in_progress→qc→boxed→ready_for_collection→collected→closed; every forward transition returned 200 with the expected `current_stage`; `closed` set `side_effects:["closed_at_set"]`. | **Flip EXPECTED-FAIL→PASS.** All 10 stages accepted in order. Stage rail on Job detail renders the full stage set. |
| TC-WF-02 | PASS | API + UI | `GET /jobs/{id}/timeline` returns one row per transition with `from_stage`, `to_stage`, `transitioned_by_user_id` (real admin UUID), `notes`, `transition_at`. SPA Status History table renders the full 11-row real timeline (stage/notes/date), not one hardcoded row. | **Flip; W11 fixed** (real timeline renders). Two caveats: (1) the SPA table surfaces STAGE/NOTES/DATE but **not** the actor/user column nor an explicit from_stage column — the actor is recorded and returned by the API but not displayed. (2) Intake writes **two** `received` rows (one `"Initial stage on intake"` with null user, one with the receiving user) — this is at intake only, **not** a per-transition duplicate, so the D7 "duplicate `system` row per transition" defect is NOT present on transitions. |
| TC-WF-03 | PASS | API | Assigned tech via transition `fields.assigned_technician_id` → `side_effects:["technician_assigned"]`, persisted on job, transition row logged with actor+timestamp+notes. Reassigned to a 2nd technician the same way. | **Flip.** Caveat: a pure `PATCH /jobs/{id}` reassignment (no stage change) persists the field but is **not** written to `job_status_history` and no `audit_events` row is emitted (no handler writes audit_events at all). The logged path is transition-carried assignment. |
| TC-WF-04 | PASS | API | Staff created 1 internal + 1 customer note. Staff `GET notes` → both. Customer (`main_customer_contact`) `GET notes` → only the customer note. Customer `GET notes?visibility=internal` → still only the customer note. | **Flip; S15 fixed.** Scoping is **server-side** (portal caller forced to `visibility='customer'` in SQL); the internal note body never reaches the portal response, so it is not a client-side filter. |
| TC-WF-05 | PASS | API | Uploaded internal + customer photos (caption + visibility flag saved). Staff list → both, each with signed URL. Customer list (even `?visibility=internal`) → only customer photo. Customer signed URL served **200 image/png** (W2 fixed). Portal Bearer GET of the internal file path → **403 "Access denied — internal file"**. Signed-URL TTL: customer `exp` ≈ 24h, internal `exp` ≈ 15 min (Δ ≈ 85 500 s). | **Flip; W2 + S16 fixed.** Internal photos are never handed a public signed URL to a portal caller (filtered server-side), internal URLs carry a 15-min TTL vs 24h for customer, and `ServeFile` denies portal Bearer access to internal files. |
| TC-WF-06 | PASS | API | `not_repairable` without a reason → **400 VALIDATION_ERROR** "not_repairable requires a fault reason". With `fields.not_repairable_reason_id` (water_damage) → 200. `GET /fault-reasons` returns exactly the 7 defined reasons (lightning, secondary_lightning, water_damage, component_failure, power_supply_failure, chip_probe_communication, other). | **Flip; D8 fixed** (T-39b enforces the structured reason server-side). The plan noted D8 had *no covering ticket* — it is nonetheless remediated. |
| TC-WF-07 | PASS (app); n8n delivery (infra) blocked | DB | Boxing `GT-RMSTESTWF-0007` wrote a `webhook_events` row `event_type='job.boxed'`. Row present with `delivered_at=NULL, delivery_attempts=1, last_error='HTTP 404: The requested webhook ...'`. | **App responsibility met** — the durable `webhook_events` row is emitted on the Boxed transition. Delivery fails at n8n (HTTP 404, workflow inactive) exactly as the brief describes — infra, not scored against the app. WhatsApp/email fan-out not verifiable while n8n is down. Note the emitted event is `job.boxed` (the milestone); the "ready-for-collection" wording is a downstream n8n template concern. |
| TC-WF-08 | PASS | API | Staff drove `GT-RMSTESTWF-0004` to quoted, created+issued a quote; customer (`main_customer_contact`) `POST /quotes/{id}/approve {channel:portal}` → 200, job auto-transitioned **quoted→approved**. Customer `POST /jobs/{id}/transition` → **403 "Only staff can transition jobs"**. | **Flip.** Quote approval is the only customer-triggerable transition; the generic transition endpoint refuses agents and customers. `authorizeQuoteAction` allows admin + the job's own `main_customer_contact` (agents refused). **W5 (portal UI has no link to approve/reject) is a frontend-reachability concern NOT visually verified here** — only the API path was exercised. |
| TC-WF-09 | PASS | API | `not_repairable` on `GT-RMSTESTWF-0002` → `side_effects:["closed_at_set","assessment_fee_invoice_created"]`; `GET invoices` shows `INV-GT-RMSTESTWF-0002-AF`, `type=assessment_fee`, subtotal 350.00 + VAT 52.50 = **402.50**, status `issued`. Further transition attempt → **400 "terminal stage not_repairable"**. | **Flip; D3 fixed.** Assessment-fee proforma is now actually raised (the plan said it "does not exist"). Terminal state enforced. |
| TC-WF-10 | PASS | API | Fresh job `GT-RMSTESTWF-0009` driven to quoted + issued quote; customer `POST /quotes/{id}/reject` → 200, job → **rejected** (`closed_at` set). `GET invoices` → `[]` (no fee). Further transition → **400 terminal**. | **Flip; non-vacuous** — TC-WF-09 in the same run is the positive control proving a fee *can* be raised, so "no fee on reject" is a real negative, not the D3 vacuous pass. |
| TC-WF-11 | PASS | API | `GET`/cancel of `GT-RMSTESTWF-0005`: technician → **403**, front_desk → **403** ("Only admins can transition to cancelled"); admin → 200, stage `cancelled`, `closed_at` set, `GET invoices` → `[]`. Transition out of cancelled → 400 terminal. | **Flip.** Admin-only cancel is now enforced via `adminOnlyTargets` (the plan flagged this clause as "unevidenced"). No fee on cancel. Terminal. |
| TC-WF-12 | PASS | API | `GT-RMSTESTWF-0006` received→boxed → **400 BUSINESS_RULE_VIOLATION** "Cannot transition from received to boxed (allowed: diagnosed, cancelled, not_repairable)"; received→closed → same 400. Job stays `received`. | **Flip.** Illegal skips rejected; only declared next-states permitted. |
| TC-WF-13 | PASS | UI | Job detail for quoted `GT-RMSTESTWF-0008`: pip colours — Received/Diagnosed = `text-brand-green-deep` (green, "done"); Quoted = amber badge `rgb(166,102,0)` on `rgb(255,246,229)` ("now"); Approved…Closed = `text-grey-400` (grey, "future"). | **Flip.** Three distinct pip states (done/current/future) render correctly — contradicts the V1 "one hardcoded green tint" prediction for the timeline. |
| TC-WF-14 | FAIL | UI + source | "Print jobcard" button present; clicking invokes `window.print()` (verified via override). `@media print` rules exist (`.no-print{display:none}`, white body) and 7 `.no-print` elements are hidden for print (V12 print support restored). **BUT** no company/"Gentick" header text (legal_name, VAT `4840244414`, address) exists anywhere in the printed DOM — company-details settings are not rendered. Source: `PrintButton` just calls `window.print()`; `JobDetailPage.tsx:130-132` states the job detail page *is* the print surface (T-79), with no dedicated A4 jobcard template. | Remains `EXPECTED-FAIL` (**not a regression**). V12 (print affordance) is restored, but the T-32 half of the expected result — "single-page A4 jobcard with the Gentick header sourced from company-details settings" — is **unmet**: there is no purpose-built jobcard and no settings-sourced company header at all. **Needs a human decision** on whether printing the reflowed job-detail page satisfies FR-INT-10 / the wireframe jobcard. |

## Summary counts
- **PASS: 13** (TC-WF-01…13, with TC-WF-07 = app-PASS / n8n-delivery infra-blocked).
- **FAIL: 1** (TC-WF-14).
- BLOCKED: 0. N/A: 0.

## Regressions (EXPECTED-PASS→FAIL)
- **None.** No case that was expected to pass now fails.

## Flips worth flagging (EXPECTED-FAIL→PASS — remediation landed)
Twelve of thirteen passes are flips from the plan's `EXPECTED-FAIL` prediction. The
highest-signal ones (subject-matter defects, not just the B3 setup blocker):
- **TC-WF-06 / D8** — structured not-repairable reason enforced server-side, despite D8 having no covering ticket in the plan.
- **TC-WF-09 / D3** — assessment-fee proforma is actually raised on `not_repairable` (INV-…-AF, R402.50 incl VAT); the plan said this "does not exist".
- **TC-WF-04 / S15** and **TC-WF-05 / S16, W2** — note/photo visibility is enforced server-side (not a client filter); signed photo URLs serve (no longer 404); internal photos get a 15-min TTL and are never handed to a portal caller.
- **TC-WF-02 / W11** — Status History renders the real timeline, not one hardcoded row.

## TC-WF-14 — the one still failing
Print support (V12) is back, but the case's expected header "sourced from
company-details settings" (T-32) does not render — the printed output is the
job-detail page reflowed, with no company header. Predicted FAIL, still FAIL,
for the T-32 reason rather than the V12 reason.

## Caveats / partial gaps noted inside passes (not scored as failures)
- **TC-WF-02 (UI):** Status History table omits the actor/user column and the explicit from_stage column; both are recorded and returned by the API.
- **TC-WF-03:** technician reassignment via bare `PATCH /jobs/{id}` is persisted but not written to `job_status_history`/`audit_events`; only transition-carried assignment is logged.
- **TC-WF-07:** WhatsApp/email delivery unverifiable (n8n inactive, HTTP 404) — infra, per brief.
- **TC-WF-08 (W5):** only the API approve/reject path was exercised; portal-UI reachability of the approve/reject control was not visually verified.

## RMSTESTWF entities created (for cleanup / traceability)
- **Customer:** `RMSTESTWF Workflow Corporation`, id `2cfb9d71-ed2b-44bb-a046-d0beed85f636`, prefix `RMSTESTWF`.
- **Agent:** `RMSTESTWF Agent One`, id `bf4ebce8-e9c6-4c9b-9380-7998b5b040f0`, `rmstestwf.agent1@rmstest.local`.
- **Primary contact:** `RMSTESTWF Primary`, id `1c3d4bde-efed-405c-826b-f9caaba119f6`, `rmstestwf.primary@rmstest.local` (email verified via Mailpit token to enable customer-triggered approve/reject).
- **Staff (technician):** `RMSTESTWF Tech Two`, id `76735860-0368-4e3f-8b9f-0bcb3d791ac4`, `rmstestwf.tech2@rmstest.local` (for reassign test).
- **Jobs:** `GT-RMSTESTWF-0001`…`0008` (one intake shipment `62f6b852-…`) plus `GT-RMSTESTWF-0009` (`d9208d7a-72d0-40e9-a99b-4783542dbdcd`, reject path). Terminal states reached: 0001 closed, 0002 not_repairable, 0005 cancelled, 0009 rejected.
- **Derived records:** quotes on 0004/0008/0009, assessment-fee invoice `INV-GT-RMSTESTWF-0002-AF`, 2 notes + 2 photos on 0008.
- No real-admin or other-namespace (`RMSTESTQA/QB`, `RMSTEST*`) data was mutated. No app code changed, no commits.

---

## Source: test-results-quo-a.md

# Test Results — Wave 4A (Quoting / Invoicing), API-only

- **Batch:** Wave 4A, namespace `RMSTESTQA` (emails `rmstestqa.*@rmstest.local`)
- **System under test:** `https://rms.gentickit.com/api/v1` (health 200)
- **Date:** 2026-08-14
- **Scope:** TC-QUO-01 … TC-QUO-10 (10 cases)
- **Method:** curl against the deployed API as admin. Money asserted as exact decimal strings; `total == subtotal + vat_amount` checked exactly. No app code changed, no commits.
- **Gating context:** All ten cases are **EXPECTED-FAIL** in `test-plan.md` (pre-remediation baseline). Remediation tickets T-30/T-31/T-32/T-33/T-34/T-36/T-37/T-38 are DONE, so most cases are now **flipped FAIL→PASS** (positive). No `EXPECTED-PASS→FAIL` regressions were found.

## Summary counts
| Result | Count |
|---|---|
| PASS | 8 |
| FAIL | 0 |
| BLOCKED | 2 |
| N/A | 0 |

**Flips vs EXPECTED-*:** 8 positive flips (EXPECTED-FAIL → actual PASS). 2 BLOCKED (agent-portal, could not be live-exercised via API). **Zero regressions.**

## Results
| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-QUO-01 | PASS | API | `POST /jobs/{id}/quotes` 201. Quote `Q-GT-RMSTESTQA-0001-1`. Line items present: L1 qty `0.500` × `800.00` = `400.00`, L2 qty `2.000` × `87.50` = `175.00`. Totals: subtotal `575.00`, vat_rate `15.00`, vat_amount `86.25`, total `661.25` — all decimal STRINGS. | EXPECTED-FAIL→**PASS** (flip). T-30/D1 remediated: money is shopspring/decimal strings, no float64. Required line items + totals present and internally consistent. |
| TC-QUO-02 | PASS | API | Same ZAR quote: `575.00` + `86.25` = `661.25` exactly; vat_rate `15.00` (ZAR 15%). | EXPECTED-FAIL→**PASS** (flip). `total == subtotal + vat_amount` holds exactly. |
| TC-QUO-03 | PASS | API | Job `GT-RMSTESTQA-0001` originated by **agent** (`originator_type=agent`). Its quote carries `approval_authority=main_customer`; invoice/PDF "Bill To" = `RMSTESTQA Corporation` (the main customer), agent shown as `c/o`. | EXPECTED-FAIL→**PASS at API layer**. Plan's EXPECTED-FAIL reason (W10 — quotes have no SPA entry point) is a **UI/SPA concern owned by the browser batch**, not verifiable here. At the API/data layer, the quote is correctly issued to the main customer regardless of the agent originator. |
| TC-QUO-04 | BLOCKED | API/Portal | Could not obtain an agent session: `POST /auth/login` as `rmstestqa.agenta` → **403 `EMAIL_NOT_VERIFIED`**. `AdminSetPassword` does not set `email_verified`; no admin email-verify endpoint exists; the email link is inaccessible in a headless API run. Code: `GetQuote` returns the full quote (incl `subtotal`/`vat_amount`/`total`) to any job-scoped caller with **no role-based redaction**. | Agent price-visibility is an agent-portal behaviour requiring an agent token. Not live-verified. Code indicates the R-value **is** available to a job-scoped agent (consistent with the plan's "vacuous pass" note). Deferred to a session with agent credentials / DB verify. |
| TC-QUO-05 | BLOCKED | API/Portal | Same agent-session limitation as QUO-04. Code: `GetQuote` (handler_quotes_invoices.go) applies **no** role-based price gating — a single path for all roles. | The T-45 price-gating control ("agent must not see price before approval") is **absent** in the API, so an agent with job access would see price both before and after approval — consistent with EXPECTED-FAIL. Not live-verified with an agent token, hence BLOCKED rather than FAIL. |
| TC-QUO-06 | PASS | API | Job stage `received` → **`approved`** immediately after `POST /quotes/{id}/approve` (HTTP 200, quote status `approved`). | EXPECTED-FAIL→**PASS** (flip). T-38 remediated: approval + job transition + status-history + audit + auto-invoice are one flow. |
| TC-QUO-07 | PASS | API | Approval auto-generated `repair` invoice `INV-GT-RMSTESTQA-0001`. `GET /invoices/{id}/pdf` → 200, `application/pdf`, 2127 B. PDF text: title **"PROFORMA INVOICE"** (no "TAX INVOICE"); footer **"This is a proforma invoice. The formal tax invoice is issued from Sage Pastel Online."**; **VAT Reg 4840244414** (settings value, not the old hardcoded 4420280275); qty column prints **"0.5"** (not truncated to 0); Subtotal ZAR 575.00 / VAT (15%) ZAR 86.25 / Total ZAR 661.25. | EXPECTED-FAIL→**PASS** (flip). T-32 fully remediated across all four checks (title, footer, settings-sourced VAT number, fractional-qty formatting). T-33/T-34: proforma raised idempotently on approval. |
| TC-QUO-08 | PASS | API | CSV: `POST /invoices/sage-export {format:csv, invoice_ids:[mine]}` → 200 `text/csv`, header `InvoiceNumber,Date,Customer,GentickVATNumber,Currency,Subtotal,VATRate,VATAmount,Total` with **CRLF**; row `INV-GT-RMSTESTQA-0001,2026-08-14,RMSTESTQA Corporation,4840244414,ZAR,575.00,15.00,86.25,661.25`. XML: same endpoint `{format:xml}` → **501 `NOT_IMPLEMENTED`**, and target invoice **unchanged** (status still `issued`, `sage_export_id` null). | EXPECTED-FAIL→**PASS** (flip). T-36 (CSV column layout + decimal money), T-37/D11 (XML rejected 501 **before** the transaction, no invoices marked exported). Scoped to my invoice_id to avoid system-wide export. |
| TC-QUO-09 | PASS | API | Invoice status `issued` (sage_export_id null) **before** export → after CSV export status = **`exported_to_sage`**, `sage_export_id=24dfc56a-2d1f-48ba-a43e-cee63bc317c6`. | EXPECTED-FAIL→**PASS** (flip). D5 remediated: export writes `status='exported_to_sage'` alongside `sage_export_id`, so the "Invoiced" state is now reachable and reflects export only. |
| TC-QUO-10 | PASS | API | USD quote `Q-GT-RMSTESTQA-0002-1`: currency `USD`, vat_rate `0.00`, vat_amount `0.00`, subtotal `300.00`, total `300.00` (qty `1.500` × `200.00`). Approved → USD `repair` invoice `INV-GT-RMSTESTQA-0002`: currency `USD`, vat_rate `0.00`, total `300.00`. EUR control quote also vat_rate `0.00`. | EXPECTED-FAIL→**PASS** (flip). T-31/D2 remediated: `DefaultVatRate` = ZAR 15%, all other currencies 0% (SA export zero-rating). Currency stays native quote→invoice end-to-end; no ZAR conversion, no 15% applied to foreign quotes. |

## Regressions
None. All ten cases were EXPECTED-FAIL; none regressed. The two BLOCKED cases are the agent-portal price-gating pair (QUO-04/05), blocked only by the inability to mint an agent session over the API — not by a code regression.

## VAT-by-currency (T-31) cross-check
- ZAR quote → vat_rate `15.00`, vat_amount `86.25` on `575.00`.
- USD quote → vat_rate `0.00`, vat_amount `0.00`.
- EUR quote → vat_rate `0.00`, vat_amount `0.00`.
Confirms currency-based defaults (15% ZAR / 0% export) rather than a hardcoded 15%.

## Fractional quantity (T-30) cross-check
0.5-qty labour line survived as `0.500` on the quote line item, `0.500` copied onto the invoice line item, and rendered as `0.5` in the PDF — with a correct `400.00` line total (0.5 × 800.00). The old `maxFloat(qty,1)` clamp (which would have billed 1.0 × 800.00 = 800.00 and a 975.00 subtotal) is gone.

## Entities created (namespace RMSTESTQA)
- **Customer:** `RMSTESTQA Corporation` — id `7c7bce6b-2011-437c-b43a-74d537e3603f`, prefix `RMSTESTQA`
- **Agent:** `RMSTESTQA Agent A` — id `80947b10-b802-4314-9dab-a576df3a6c50`, email `rmstestqa.agenta@rmstest.local` (portal login provisioned; email unverified → cannot log in)
- **Shipment + units:** one agent-origin shipment (tracking `RMSTESTQA-TRK-1`); units `RMSTESTQA-SN-ZAR`, `RMSTESTQA-SN-USD`
- **Jobs:** `GT-RMSTESTQA-0001` (id `f0226a8d-9d18-4806-bd13-5489108487ff`, ZAR chain, now `approved`); `GT-RMSTESTQA-0002` (id `49df7de4-4d25-4654-b699-045545608f13`, USD chain, now `approved`)
- **Quotes:** `Q-GT-RMSTESTQA-0001-1` (id `756343d3-8dc2-4b7e-aff0-8f3b56d97263`, ZAR, approved); `Q-GT-RMSTESTQA-0002-1` (id `ed726656-e5bf-4c72-9980-4820a5b9567a`, USD, approved); `Q-GT-RMSTESTQA-0002-2` (EUR, draft — control only)
- **Invoices:** `INV-GT-RMSTESTQA-0001` (id `dad6ba56-1e73-440e-b361-4ee5ce0a6532`, ZAR repair, `exported_to_sage`); `INV-GT-RMSTESTQA-0002` (USD repair, `issued`)
- **Sage export:** id `24dfc56a-2d1f-48ba-a43e-cee63bc317c6` (csv, 1 invoice)

## What was NOT verified
- **QUO-04 / QUO-05** live agent behaviour — no agent session obtainable via the API (email-verification gate). Findings are from code inspection of `GetQuote` (no role-based price redaction) plus the plan's T-45 note; not exercised with an agent token.
- **QUO-03 SPA entry point** (W10) — a UI concern owned by the parallel browser batch; only the API/data behaviour was checked.
- Bulk/system-wide sage export (empty `invoice_ids`) was deliberately NOT exercised — it would mutate other namespaces' invoices. All export tests were scoped to my own invoice_id.

---

## Source: test-results-quo-b.md

# Wave 4B — Quoting / Invoicing / Sage (TC-QUO-11 … TC-QUO-19)

API-only verification against the deployed system (`https://rms.gentickit.com/api/v1`),
run 2026-08-14. Namespace `RMSTESTQB`. No app code changed, no commits.

Baselines in the plan/register were all `EXPECTED-FAIL` (QUO-18 was `UNKNOWN`) —
those predate the Sprint-6 remediation tickets. This run records **actual**
post-remediation behaviour and flags every flip.

Money on quotes/invoices is a decimal **string** on the wire (T-30) and
`total == subtotal + vat_amount` held exactly on every quote and invoice checked.

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-QUO-11 | PASS | API | `POST /jobs/{id}/quotes` currency ZAR, no `vat_rate` → `vat_rate="15.00"`, `subtotal="100.00"`, `vat_amount="15.00"`, `total="115.00"`, `currency_code=ZAR`. Quote `Q-GT-RQB-0001-1`. | **FLIP EXPECTED-FAIL→PASS.** `model.DefaultVatRate` returns 15% for ZAR. Money as decimal strings; total = subtotal + vat exactly. |
| TC-QUO-12 | PASS | API | `POST /jobs/{id}/quotes` currency USD, no `vat_rate` → `vat_rate="0.00"`, `subtotal="100.00"`, `vat_amount="0.00"`, `total="100.00"`, `currency_code=USD`. | **FLIP EXPECTED-FAIL→PASS.** Export zero-rating restored (T-31). Non-ZAR → 0%. Contradicts plan note "VAT is hardcoded 15% regardless of currency". |
| TC-QUO-13 | FAIL | API | `POST /jobs/{id}/quotes` with `vat_rate="10.00"` and **no reason** → HTTP 201, quote stored `vat_rate="10.00"`. `createQuoteRequest` has no reason field; response carries no `*reason*` key. | Matches EXPECTED-FAIL, but **the reason differs from the plan.** Plan said "no override path exists" — an override path DOES now exist (explicit `vat_rate` is honoured). The failure is that the override is accepted **without requiring or storing a reason** (FR-QUO-08 unmet). |
| TC-QUO-14 | PASS | API | `POST /jobs/{id}/transition` received→`not_repairable` (fault reason supplied) → `side_effects=[closed_at_set, assessment_fee_invoice_created]`. Invoice `INV-GT-RQB-0004-AF` type `assessment_fee`, `subtotal="350.00"`, `vat_rate="15.00"`, `vat_amount="52.50"`, `total="402.50"`, status `issued`. | **FLIP EXPECTED-FAIL→PASS.** T-33/DEC-4 assessment-fee proforma works. Default R350 read from `assessment_fee` setting; ZAR 15% VAT applied. |
| TC-QUO-15 | PASS | API | Quote created→issued→`POST /quotes/{id}/reject` (channel portal) → quote `status=rejected`; `GET /jobs/{id}/invoices` count **0**; job `current_stage=rejected`; timeline shows single row `received→rejected`, `transitioned_by_user_id=6b21d3cf…` (real staff actor), notes stored. | **FLIP EXPECTED-FAIL→PASS.** No fee on reject (positive control TC-QUO-14 confirms a fee IS raisable). Also confirms **T-38**: one history row, real actor (not `system`). |
| TC-QUO-16 | FAIL | API | (a) **No API path** to set per-job `assessment_fee_amount`/`assessment_fee_currency` — `updateJobRequest` lacks them; `transition` fields only accept `assessment_fee_waived` + `assessment_fee_waiver_reason`. (b) Waive **without** a reason accepted → HTTP 200, `side_effects=[…,assessment_fee_waived_updated]`, invoices count 0. (c) The waiver transition writes **no** audit event. | Matches EXPECTED-FAIL — refined. Waiver correctly suppresses the fee, and audit infra works generally (see T-35), **but** per-job fee is not configurable via API, "waiver requires a reason" is not enforced, and the waiver action itself is not audited. Plan's W9 ("audit permanently empty") is **stale** — audit is populated. |
| TC-QUO-17 | PASS | API | Quote `gentick_snapshot` and invoice `gentick_snapshot` both decode to `{legal_name:"Gentick Electronics", vat_number:"4840244414", address_line1:"169 Haupt Street", address_line2:"Sidwell", city:"Port Elizabeth", postal_code:"6001"}`. Invoice PDF (`GET /invoices/{id}/pdf`, HTTP 200, `application/pdf`) decompressed → all six strings FOUND; document titled **"PROFORMA"**. | **FLIP EXPECTED-FAIL→PASS.** T-32 defect resolved — PDF no longer titled "TAX INVOICE"; company details come from the settings snapshot, not hardcoded literals. **Caveat:** there is no quote-PDF endpoint, so the "on quotes" half is verified at the data layer (`gentick_snapshot`), not a rendered quote document. |
| TC-QUO-18 | FAIL | API | Quote issued with `validity_days=-1` → `expires_at=2026-08-13` (past; "now"=2026-08-14), but `status=issued` (not auto-expired). `POST /quotes/{id}/approve` → **HTTP 200**, `status=approved`. `GET /quotes/{id}` still `status=approved`, past `expires_at`. | Plan baseline was UNKNOWN → **confirmed FAIL.** No auto-expiry mechanism exists and `ApproveQuote` gates only on `status='issued'`, never on `expires_at`. An expired quote is fully approvable. (Method: past-dated validity, functionally equivalent to a 15-day-old quote.) |
| TC-QUO-19 | FAIL | API | `POST /parts` (`unit_cost:125.50`, `active:true`) → HTTP 201 (part `5bb90b3e…`). `POST /jobs/{id}/parts` qty 2 → HTTP 201, `unit_cost_at_use=125.5`, currency ZAR. Then `POST /jobs/{id}/quotes` → quote has only the explicit labour line; logged part **absent**, no line item with `part_id`. | Matches EXPECTED-FAIL — **but the plan's blocker is stale.** `POST /parts` no longer 400s (W8/T-41 fixed); parts create and log fine. The case still fails because **quote generation does not consume `job_parts`** — no server-side parts→quote feed exists. Minor: `job_parts.unit_cost_at_use` is returned as a float (`125.5`), not a decimal string — a T-30 inconsistency isolated to the parts path. |

## Cross-cutting finance-ticket confirmations (task-flagged; support the cases above)

These are the finance back-half tickets named in the brief. Verified with my own
invoices only (explicit `invoice_ids` — never the "export all unexported" path,
to avoid touching other namespaces).

| Ticket | Result | Evidence |
|---|---|---|
| T-34 invoice numbering / idempotency | PASS | Approving the job1 quote auto-raised exactly **one** repair invoice `INV-GT-RQB-0001` (date-free number, `total="57.50"`). `GET /jobs/{id}/invoices` count = 1. `ON CONFLICT (job_id,type) DO NOTHING` + date-free number means a repeat approval or close reuses the row rather than duplicating; the old date-bearing number (which duplicated across days) is gone. |
| T-35 audit writers | PASS | `GET /admin/audit` shows `quote.approved` and `quote.rejected` events for my quotes, each `actor_type=staff`, real `actor_user_id`, `entity_type=quote`, valid `entity_id`. Audit table is **not** empty (plan W9 stale). |
| T-36 Sage CSV | PASS | `POST /invoices/sage-export {format:csv}` header row exactly: `InvoiceNumber,Date,Customer,GentickVATNumber,Currency,Subtotal,VATRate,VATAmount,Total` — `GentickVATNumber` present, correct order and casing, CRLF line endings. Rows carry Gentick VAT `4840244414`, decimal-string amounts. Both invoices then read `status=exported_to_sage` with `sage_export_id` set (the "Invoiced" state is reachable — D5 fixed). |
| T-37 XML export | PASS | `POST /invoices/sage-export {format:xml}` → **HTTP 501** `NOT_IMPLEMENTED` and the target invoice remained `status=issued`, `sage_export_id=null` — rejected before any DB mutation, no flag left set. |
| T-38 transactional history | PASS | Approve (job1) and reject (job5) each wrote exactly **one** `job_status_history` row for the transition, real actor `6b21d3cf…` (staff), not `system`. |
| T-39 template versioning | N/A | Not within TC-QUO-11…19 (maps to TC-COM-09). Not exercised. |

## Summary

- **9 cases:** 5 PASS (QUO-11, 12, 14, 15, 17), 4 FAIL (QUO-13, 16, 18, 19). 0 BLOCKED.
- **Flips (all favourable) EXPECTED-FAIL→PASS:** QUO-11, 12, 14, 15, 17. QUO-18 UNKNOWN→FAIL.
- **No EXPECTED-PASS→FAIL regressions.**
- Three of the four FAILs are genuine functionality gaps still open after Sprint 6
  (VAT-override reason capture, per-job fee config + audited waiver, quote auto-expiry,
  parts→quote feed). Several plan **notes are now stale** and are called out inline
  (QUO-12 "hardcoded 15%", QUO-13 "no override path", QUO-16 W9 "audit empty",
  QUO-19 W8 "POST /parts 400s").

## Entities created (RMSTESTQB namespace)

- Customer `RMSTESTQB QuoteBatch Corporation` `3df2bc76-e0be-475d-b806-95e532bfde88`
- Agent `RMSTESTQB Agent` `20e7adfb-1553-4e02-9533-6470602f1861` (`rmstestqb.agent@rmstest.local`)
- 1 shipment + 8 units + 8 jobs `GT-RQB-0001`…`GT-RQB-0008`
- Quotes on jobs 1 (×3, one approved), 2, 3, 5 (rejected), 8
- Invoices `INV-GT-RQB-0001` (repair, exported_to_sage), `INV-GT-RQB-0004-AF` (assessment fee, exported_to_sage)
- Part `RMSTESTQB-PSU-4G-A` `5bb90b3e-6d8c-4918-8f5a-b5d95ea61017`; one `job_parts` row on job 8
- Jobs transitioned: 4 → not_repairable, 5 → rejected, 6 → not_repairable (fee waived), 1 → approved

_No real-admin or other-namespace data mutated. Sage exports used explicit invoice_ids only._

---

## Source: test-results-com.md

# Test Results — Communications & Notifications (TC-COM-01…15)

**Wave:** 5B — API-only verification against deployed RMS
**Date:** 2026-08-14
**System under test:** `https://rms.gentickit.com/api/v1` (rms-api `version 0.1.0`, `db: ok`)
**Executor:** Pegasus (API + Mailpit + Postgres DB-surface; no app code changed)
**Namespace:** `RMSTEST5B`

## Standing infrastructure facts (apply to every notification case)

1. **Webhook emission works; n8n delivery is infra-blocked (as documented).**
   `webhook_events` holds 78+ durable rows across every event type. Every row has
   `delivered_at = NULL`, `delivery_attempts ≥ 1`, and
   `last_error = 'HTTP 404: … webhook "POST gentick-events" is not registered'`.
   The app's job — writing the durable event row with the correct type/payload/`job_id`
   and delivery-status columns — is done. n8n workflow is inactive server-side (404),
   so no WhatsApp/email fan-out happens. Per Wave-5B rules the app is **not** scored red
   for n8n delivery. The **routing matrix itself** (who gets notified, WhatsApp-vs-email
   selection) lives in n8n and is therefore unverifiable end-to-end.
2. **Direct-SMTP email DOES deliver** (invitation / verification / reset) — verified in Mailpit.
3. **D12 confirmed still open:** there is **no consent state anywhere in the schema**
   (no `consent`/`opt_in`/`whatsapp_consent` columns on any table). Nothing exists to
   branch channel selection on.

## Results

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-COM-01 | PASS | API/DB (app-emit) | Drove GT-RMSTEST5B-0001/0004/0005: `webhook_events` rows for `job.received`, `quote.ready`, `job.ready_for_collection`, `job.not_repairable` all present. | **FLIP EXPECTED-FAIL→PASS** (W1 fixed: `emitEventAsync` uses `context.WithoutCancel`, rows now durably written). All four milestone events fire. Actual delivery infra-blocked (n8n 404). |
| TC-COM-02 | BLOCKED | n8n + data-model | App emits one `job.received` event; there is **no consent column** to branch on (D12). | Channel selection (WhatsApp-primary + email-always) is entirely an n8n concern that is infra-blocked, and D12 means there is no consent state to select on even if n8n were up. Cannot be exercised app-only. Matches EXPECTED-FAIL. |
| TC-COM-03 | BLOCKED | n8n + data-model | Same as COM-02 — no consent/opt-out state exists (D12). | Email-only-when-opted-out is unimplementable app-side (no consent model) and routing is n8n (infra-blocked). Matches EXPECTED-FAIL. |
| TC-COM-04 | PASS | API/DB | Inbound `YES` from main customer (+27821110001) on GT-RMSTEST5B-0001 → `classified_intent=approval_yes`, `matched_quote_id` set, `action_taken=quote_approved`; job went `quoted→approved`; `job_status_history` row "Auto-approved via WhatsApp". Message row logged. | **FLIP EXPECTED-FAIL→PASS** (B5 fixed: inbound no longer 500s; auto-approve runs). **Caveat:** the WhatsApp auto-approve path does NOT emit a `quote.approved` webhook (confirmed: jobs 0001/0002 have no such row), so downstream "both-parties-notified" would not trigger from a WhatsApp approval. |
| TC-COM-05 | PASS | API/DB | `POST /quotes/{id}/approve {channel:portal}` on GT-RMSTEST5B-0003 → quote `approved`, job `quoted→approved`; `quote_approvals` row: `channel=portal, decision=approved, decided_by_actor_type=staff, decided_by_user_id` set, notes captured. All in one transaction (T-38/D10 fixed). | **FLIP EXPECTED-FAIL→PASS** at backend. Action logged with channel + actor as required. Portal-click *UI* reachability (W5) is a separate frontend batch's concern — the API fully supports it. |
| TC-COM-06 | **FAIL** | API/DB | Inbound `YES` from the **agent's** number (+27821110002, `party_type=agent`) on GT-RMSTEST5B-0002 → `action_taken=quote_approved`; job went to **approved**. | **DEFECT — actual-vs-expected mismatch loud.** Expected: agent approval must NOT be applied; job stays Quoted; message escalated as non-authoritative. Actual: the app **approves the quote from an agent's WhatsApp**. Root cause: `autoApproveQuote` (handler_misc.go:538) ignores `party_type` — it approves on any `approval_yes` with an issued quote regardless of sender. This is an authorization hole, not just a stalled feature. Matches EXPECTED-FAIL but the failure mode changed from "500s" to "wrongly approves". |
| TC-COM-07 | PASS | DB (app-persist) | Inbound: 4 `messages` rows with `direction=inbound, channel=whatsapp, classified_intent, from_phone, external_id, body, sent_at, received_at`, threads correctly keyed to `party_type` + `job`. Outbound: persisted as `webhook_events` rows carrying `job_id` + delivery status (`delivered_at`/`delivery_attempts`/`last_error`). | **B5 fixed** (inbound insert no longer omits NOT NULL `direction`/`channel`). **Caveat (read loudly):** the app does NOT write *rendered* outbound messages into the `messages` table — outbound content is composed and logged by n8n (infra-blocked), and there is no message-log **read** endpoint, so the unified "Communications log tab" is UI-batch/n8n territory. Scored PASS on the remediated app responsibility (durable per-direction logging against the job); the end-to-end rendered-outbound log is not app-only verifiable. |
| TC-COM-08 | **FAIL** | API/DB | Free-text question ("When will my radio be ready?") → `classified_intent=question`, message logged, **but response carries no escalation and no routing to the escalation contact**. `escalation_contact` setting exists (`Michael, +27823749842`) but is never read by the inbound handler. Only `approval_no` escalates, and even then to a hardcoded literal `"front_desk"`, not to the configured contact. | Expected: unclassified/question inbound routed to the WhatsApp escalation contact (Michael) and logged. Actual: logged only; **no escalation-to-Michael path exists in the app**. The `escalation_contact` setting is decorative for this flow. Matches EXPECTED-FAIL. |
| TC-COM-09 | PASS | API | Created `rmstest5b_quote_ready` (v1, `retired_at=null`). PATCH → new id, v2, `retired_at=null`; v1 now `retired_at` set. `GET` (default) returns only v2; `GET ?include_retired=true` returns v1(retired)+v2(live) with v1 body unchanged (immutable). PATCH of retired v1 → **404**. | **FLIP EXPECTED-FAIL→PASS** (D9 fixed: supersede-never-overwrite; `retired_at` written; historical wording immutable). The "historical *sent* message retains its version" clause can't be exercised because outbound sends aren't app-persisted (n8n), but the versioning machinery (immutable versions + `retired_at` + `messages.template_id` FK) is fully in place. |
| TC-COM-10 | PASS | API/DB (app-emit) | Issuing the quote on GT-RMSTEST5B-0001 emitted `quote.ready` (`webhook_events`). | **FLIP EXPECTED-FAIL→PASS** at app layer (W1 fixed). The matrix detail ("main customer only, agent not until approved") is n8n routing — infra-blocked, not app-verifiable. |
| TC-COM-11 | PASS | API/DB (app-emit) | `POST /quotes/{id}/approve` emitted `quote.approved` + `invoice.created` (jobs 0003, 0005). | **FLIP EXPECTED-FAIL→PASS** at app layer. **Caveats:** (a) the WhatsApp approval path does NOT emit `quote.approved` (COM-04 caveat) — a real gap for WhatsApp-approved quotes; (b) "agent now sees full price" is portal RBAC/UI, not API-verifiable here. |
| TC-COM-12 | PASS | API/DB | GT-RMSTEST5B-0004 `→ not_repairable` (with `fields.not_repairable_reason_id`) → side-effects `assessment_fee_invoice_created`; invoice `INV-GT-RMSTEST5B-0004-AF`, 402.50 ZAR, issued; `job.not_repairable` webhook emitted. | **FLIP EXPECTED-FAIL→PASS** (D3 fixed: assessment-fee invoice now raised; W1 fixed). Bonus finding: the API **enforces** a structured `not_repairable_reason_id` from `fault_reasons` (rejects free-text-only) — contradicts the D8/TC-WF-06 prediction that the structured-reason requirement was gone (at least on this API path). The reason-report + notification fan-out is n8n. |
| TC-COM-13 | PASS | API/Mailpit | `POST /agents/invite` for rmstest5b.invitee@rmstest.local → invitation persisted with token; **Mailpit shows** `from repairs@gentick.co.za → rmstest5b.invitee@rmstest.local`, subject "You've been invited to Gentick RMS". Main customer received no separate notification for the invite. | **FLIP EXPECTED-FAIL→PASS** (W3 fixed: `InviteAgent` now sends the email via direct SMTP; token no longer response-body-only). Only the agent is emailed, per the matrix. |
| TC-COM-14 | PASS | API/DB | GT-RMSTEST5B-0005 driven full lifecycle to `closed`; `job.closed` webhook emitted; repair/proforma invoice `INV-GT-RMSTEST5B-0005` (690.00 ZAR, issued) attached. | **FLIP EXPECTED-FAIL→PASS** (D4/W1). **Nuance:** the proforma is raised at *approval* (`invoice.created` event fires there), and closing is idempotent — the close side-effect was only `closed_at_set`, no *new* proforma minted at close. Net: the job has a proforma at close time. Email-with-proforma delivery is n8n. |
| TC-COM-15 | PASS | API/DB (app-emit) | `job.received` emitted for every RMSTEST5B job on intake, incl. agent-origin intakes. | **FLIP EXPECTED-FAIL→PASS** at app layer (W1 fixed). "Both parties WhatsApp + email" is n8n routing — infra-blocked, not app-verifiable. |

## Summary counts

- **PASS: 11** (COM-01, 04, 05, 07, 09, 10, 11, 12, 13, 14, 15)
- **FAIL: 2** (COM-06, COM-08)
- **BLOCKED: 2** (COM-02, COM-03)
- **N/A: 0**

All 15 were predicted **EXPECTED-FAIL**. **11 flipped to PASS** (W1/B5/D9/W3/D3/D4/T-38 remediations verified). 2 remain FAIL (genuine app defects). 2 are BLOCKED (n8n routing + absent consent model, D12).

## Regressions (EXPECTED-PASS → FAIL)

None. No COM case was EXPECTED-PASS, so no regressions in this suite.

## FAIL / BLOCKED detail (actual vs expected)

- **COM-06 (FAIL, top concern):** app auto-approves a quote from an **agent's** WhatsApp `YES`. `autoApproveQuote` never checks the thread's `party_type`, so any `approval_yes` on a job with an issued quote approves it — the agent, not just the main customer, can approve. Authority restriction ("only the main customer") is unenforced on the WhatsApp path. Expected: stays Quoted, escalated as non-authoritative.
- **COM-08 (FAIL):** unclassified/question inbound is classified + logged but **not routed to the escalation contact (Michael)**. The `escalation_contact` setting is never consulted by the inbound handler; only `approval_no` escalates, to a hardcoded `"front_desk"` string. Expected: routed to Michael + logged.
- **COM-02 / COM-03 (BLOCKED):** consent-based channel selection cannot be exercised — no consent state exists in the schema (D12), and the routing that would consume it lives in n8n (infra-blocked, HTTP 404).

## Cross-cutting note flagged for humans (out of COM scope)

The quote approve/issue API responses embed `agent_snapshot` as base64 that decodes to include `password_hash`, `refresh_token_hash`, and `email_verification_token_hash` for the agent. That is a credential-material exposure in an API response and warrants a separate look — noted here only because it surfaced while verifying COM-05/COM-11; not part of the COM test matrix.

## Entities created (all `RMSTEST5B`, safe to leave or clean up)

- Customer `RMSTEST5B Comms Corp` — `716c9867-8e59-4319-81f0-dfa825804fd4`
- Primary contact `rmstest5b.primary@rmstest.local` — `54cdd3db-96ea-4f79-bf47-2345286af9d2` (+27821110001)
- Agent `rmstest5b.agenta@rmstest.local` — `9721124b-4167-4050-9f33-4d10407247ff` (+27821110002)
- Agent invitation `rmstest5b.invitee@rmstest.local` — `6d580f60-c51a-4510-b21e-2753920dcc96`
- Jobs `GT-RMSTEST5B-0001` … `GT-RMSTEST5B-0005` (with quotes; 0001/0002 approved via WhatsApp, 0003 via endpoint, 0004 not_repairable, 0005 closed)
- Invoices: `INV-GT-RMSTEST5B-0004-AF` (assessment fee, 402.50 ZAR), `INV-GT-RMSTEST5B-0003`, `INV-GT-RMSTEST5B-0005` (repair/proforma)
- Message template `rmstest5b_quote_ready` (v1 retired, v2 live)
- 4 inbound WhatsApp `messages` + threads (external_ids `RMSTEST5B-WA-001/006/008/008b`)
- Mailpit: contact/agent verification emails + the invitee invitation email

No real-admin or other-namespace data was mutated.

---

## Source: test-results-rep.md

# Wave 5D — Reporting (TC-REP-01…10) — API verification results

Executed against deployed RMS: `https://rms.gentickit.com/api/v1` (curl, admin bearer).
API-only wave (browser owned by a parallel batch). Date: 2026-08-14.
Remediation tickets (T-42, T-73, T-30, T-31, T-33) recorded DONE; the plan's
`EXPECTED-FAIL` values predate those tickets, so most REP cases FLIP FAIL→PASS at
the data/aggregation layer. **No EXPECTED-PASS→FAIL regressions were found.**

No fixtures created — existing `GT-RMSTEST*` jobs/quotes/invoices already
populate revenue, invoice and aging data. Namespace `RMSTEST5D` unused (not needed).

| Case | Result | Layer | Evidence | Notes |
|------|--------|-------|----------|-------|
| TC-REP-01 | BLOCKED (UI-only, deferred) | API | `GET /dashboard/kpis` 200 → `jobs_by_stage {approved:6, boxed:1, quoted:1, received:18}`, `active_jobs:26`; `GET /reports/aging` 200 → `[{0-6 days:26}]` | Per-stage counts + aging **buckets** verified good at data layer. Tile/aged-escalation rendering is SPA (V4) → deferred. **Flag:** `/dashboard/kpis` exposes NO aged>14d count field — an aged tile must derive it from `/reports/aging` by summing `14-29 days`+`30+ days` (confirms the handoff undercount concern). Could NOT exercise the >14d bucket: all live fixtures were intaken today and there is no API path to backdate `intake_at`; bucket SQL (`interval '14 days'`/`'30 days'`) is present and correct. |
| TC-REP-02 | PASS | API | `GET /reports/technician-workload` 200 → `[{Unassigned:25},{RMSTEST Technician:1}]` | Per-technician open-job aggregation works. **Flip vs EXPECTED-FAIL** (that FAIL was W12 — the Reports SPA page crashing on empty `SelectItem`, a UI concern; unverified here, browser owned by other batch). Data layer sound. |
| TC-REP-03 | FAIL | API | `GET /reports/operational` 200 → single `average_turnaround_days: 0.000239...`; supports only `from/to/customer_id/agent_id` filters | **Matches EXPECTED-FAIL.** Turnaround exists only as one global average on the operational report. NOT sliceable by month and NOT by repair type — no month grouping, no `repair_type` param, no dedicated turnaround endpoint. The case's "sliceable by month and repair type" requirement is unmet. |
| TC-REP-04 | PASS | API | `GET /reports/revenue` 200 → `[{USD,Repair,total:"300.00",1},{ZAR,Repair,total:"1868.75",4},{ZAR,Assessment fee,total:"805.00",2}]` | **Key money case — PASS.** Money is decimal STRING (`"1868.75"`), not float (T-30/DEC-2 satisfied, `RevenueRow.total` is `model.Decimal`). NO ZAR roll-up — each currency reported natively and separately. Assessment-fee label branch (T-33) confirmed: `type:"assessment_fee"`→`"Assessment fee"`, else `"Repair"`. Aggregation correct (SUM per currency+type). **Flip vs EXPECTED-FAIL** (was W12 UI crash + D1 float money — both remediated). **Caveat:** revenue report ignores `from/to` (no date filtering — verified: `?from=2030-01-01` returns identical rows). Not required by this case but noted. |
| TC-REP-05 | PASS | API | `GET /reports/intake-source` 200 → `[{Front desk:4},{Main customer:3},{Agent:26}]` | Split is **non-degenerate**. **Flip vs EXPECTED-FAIL.** The plan's degeneracy concern (B6/T-53: "every staff intake records `walk_in`") does not apply — this endpoint groups by `jobs.originator_type` (main_customer/agent/front_desk), a meaningful field, not the `walk_in` intake channel. Three distinct sources returned. |
| TC-REP-06 | PASS | API | `GET /reports/export?type=` → revenue/aging/workload/intake-source all HTTP 200, `Content-Type: text/csv; charset=utf-8`, `Content-Disposition: attachment; filename="gentick-*.csv"`. Revenue CSV = 98 bytes, 3 data rows incl. `ZAR,Assessment fee,805.00,2`. Flat all-jobs export (no `type`) also 200 CSV. | All four typed report CSVs + flat export serve valid RFC-4180 CSV. Revenue CSV money as `805.00`/`1868.75` strings. **Flip vs EXPECTED-FAIL** (W12 UI). |
| TC-REP-07 | FAIL | API | `GET /fault-reasons` 200 → flat lookup list (`code`/`label`/`active`), no counts. No fault-frequency aggregation endpoint exists; `operational.jobs_by_repair_type` = `{"Unknown":33}` (jobs lack repair_type_id). | **Matches EXPECTED-FAIL (B3).** No data source aggregates "common faults" by frequency for a dashboard summary card. `/fault-reasons` is a taxonomy list only. |
| TC-REP-08 | BLOCKED (UI-only, deferred) | API | Component data available but no unified endpoint: `dashboard/kpis` gives `pending_quotes:1` (approvals) + `jobs_by_stage.boxed:1` (boxed units); `GET /sage-exports` 200 lists batches w/ `invoice_count` (Sage batch). | "Today's priorities" is an SPA composite panel; no single backend endpoint assembles approvals + boxed + Sage batch. Underlying pieces exist across three endpoints. Panel composition/links are UI (B3;W10;D5) → deferred. |
| TC-REP-09 | PASS | API | `GET /jobs?customer_id=8182524f-…` 200 → 3 jobs, all `main_customer_id` == filter value; unfiltered total 33; `?customer_id=not-a-uuid` → HTTP 400 | Customer filter narrows correctly and validates input. **Flip vs EXPECTED-FAIL** (B3). |
| TC-REP-10 | PASS | API | `GET /units/{id}/history` 200 → `[{GT-RMSTESTWF-0009, stage:rejected, intake_at, closed_at}]` for unit `03ca87ac-…`; unknown unit → 404 | Per-unit repair history across owners works. Customer-scoped history also reachable via `/jobs?customer_id=` (REP-09). **Flip vs EXPECTED-FAIL** (B3;B4). |

## Summary counts
- PASS: 6 (REP-02, 04, 05, 06, 09, 10)
- FAIL: 2 (REP-03, REP-07)
- BLOCKED (UI-only, deferred): 2 (REP-01, REP-08)
- N/A: 0

## Regressions (EXPECTED-PASS → FAIL)
None. Every REP case was `EXPECTED-FAIL` in the plan. Six flipped to PASS at the
data layer (remediation tickets landed); two remain FAIL at the data layer
(REP-03, REP-07 — genuinely missing aggregation); two are UI-only and deferred.

## FAIL / BLOCKED — actual vs expected
- **REP-03 FAIL (== EXPECTED-FAIL):** turnaround is a single global average on
  `/reports/operational`, not sliceable by month or repair type. No dedicated
  turnaround report exists.
- **REP-07 FAIL (== EXPECTED-FAIL):** no fault-frequency aggregation endpoint;
  `/fault-reasons` is a flat taxonomy list.
- **REP-01 BLOCKED (was EXPECTED-FAIL):** data aggregation (per-stage + aging
  buckets) verified good; aged>14d tile/escalation is UI and cannot be exercised
  on live data (no aged fixtures, no API backdate). `/dashboard/kpis` has no
  aged count field — handoff undercount concern confirmed.
- **REP-08 BLOCKED (was EXPECTED-FAIL):** composite dashboard panel; pieces exist
  across `/dashboard/kpis` and `/sage-exports`, no unified endpoint.

## Cross-cutting notes
- Money everywhere in reporting is a decimal STRING (T-30/DEC-2 honoured):
  revenue JSON `total:"1868.75"`, CSV `1868.75`. No float round-trip.
- Date-range filtering: works on `/reports/operational` and `/reports/export`
  (verified future-window → 0 jobs). Does NOT apply to `/reports/revenue`,
  `/reports/aging`, `/reports/intake-source`, `/reports/technician-workload`
  (these take no date params — verified revenue ignores `from/to`).
- All report endpoints are staff-gated and returned 200 to the admin bearer.

## Entities created
None.

---

## Source: test-results-nfr.md

# Test Results — NFR band (TC-NFR-01 … 14)

Wave 5C, API-only (`curl`) against the deployed RMS. VERIFY/RECORD only — no app
code changed, no commits. Executed 2026-08-14.

- System under test: `https://rms.gentickit.com` (frontend, static, behind Cloudflare)
  and `https://rms.gentickit.com/api/v1` (Go API, behind Cloudflare).
- Auth: admin `teo.jordaan@gentick.co.za` (password not recorded). Bearer token
  from `POST /auth/login`.
- Namespace for created entities: `RMSTEST5C`.
- Result legend: PASS / FAIL / BLOCKED / N/A. "EXPECTED-*" columns are the
  pre-run baseline from `test-plan.md` (lines 176–189) and its AMEND notes.

## Headline

- **No PASS→FAIL regressions found.**
- **Two positive flips** vs the plan's EXPECTED-FAIL baseline, both explained by
  the completed remediation/migration:
  - **TC-NFR-02** — server-side pagination now present and correct (jobs are
    creatable again post-migration). Old FAIL was B3 "1,000-job fixture cannot be
    built through the application". Mechanism PASSES; the 1,000-row *scale*
    assertion remains unmeasured (fixture not built — did not mass-create).
  - **TC-NFR-09** — re-baselined by DEC-13/T-76 from a hard contrast FAIL to an
    *asserted, documented accepted exception*, which is present in the docs → PASS
    on the amended assertion.
- All security remediation tickets in scope (T-22, T-23, T-24, T-25/S11, T-26,
  T-29) **confirmed live**. See the second table.
- One minor finding: **HSTS is not enabled** on the API or the edge (opt-in per
  T-26 design; not a regression — S13 baseline had none either).

## TC-NFR-01 … 14

| Case | Result | Layer | Evidence | Notes (vs EXPECTED / AMEND) |
|---|---|---|---|---|
| TC-NFR-01 — page load staff <2s / portal <3s | BLOCKED | UI | Backend latency (server round-trip via Cloudflare, 3 samples): jobs ~0.18s, staff ~0.17s, customers ~0.17–0.27s, agents ~0.16–0.19s | EXPECTED UNKNOWN. Full-page-load timing is a browser/render metric owned by the UI batch → BLOCKED (UI-only, deferred). API is not the bottleneck: backend answers well under 300 ms. |
| TC-NFR-02 — 1,000 jobs without pagination perf issues | PASS (mechanism) / BLOCKED (scale) | API | `GET /jobs?limit=5&offset=0` → `{"pagination":{"page":1,"limit":5,"total":33}}`, 5 items. `limit=500` → 33 items (all, capped by dataset). Server-side pagination + total count present. | EXPECTED-FAIL (B3 — fixture uncreatable in source app). **Positive flip**: Go migration restored job creation (33 exist) and server-side pagination is correct. The literal "1,000 jobs" performance assertion was NOT measured — did not mass-create a 1,000-row fixture (out of scope, avoid polluting prod). Not a regression. |
| TC-NFR-03 — HTTPS + security headers (T-26/S13) | PASS (HSTS caveat) | API/edge | HTTPS serves 200. `GET /api/v1/health -i`: `x-content-type-options: nosniff`, `content-security-policy: default-src 'none'; frame-ancestors 'none'; base-uri 'none'; form-action 'none'`, `referrer-policy: no-referrer`. CORS: `Origin: https://evil.example.com` → **no** `access-control-allow-origin` reflected (`Vary: Origin` present); locked allowlist, not `*`. **No `strict-transport-security` on API or frontend.** | EXPECTED UNKNOWN. T-26 (S13 fix) confirmed live: nosniff + CSP + Referrer-Policy present, CORS tightened off `*`. HSTS is "opt-in per T-26" and is **not enabled** — finding, not regression. App is same-origin so absent ACAO for its own origin is expected/secure. |
| TC-NFR-04 — secrets not in code / workflow JSON | BLOCKED | Infra | Not reachable by API. API responses inspected (`/health` etc.) leak no secrets. n8n workflow JSON + repo grep are the actual assertion. | EXPECTED UNKNOWN. Requires server/n8n/repo inspection (B-13) → out of API scope, BLOCKED. Adjacent: T-23 (signed-URL secret) confirmed — see second table. |
| TC-NFR-05 — POPIA residency, no cross-border transfer | BLOCKED | Infra | Stack fronted by **Cloudflare** (`Server: cloudflare`, `CF-RAY … -JNB`). No way to confirm origin-server region or data-at-rest location via API. | EXPECTED UNKNOWN. AMEND (plan L244): re-anchor to the actual prod target. Note for humans: Cloudflare is a global edge/CDN in front of the origin — a residency review must account for edge termination. Not API-verifiable → BLOCKED. |
| TC-NFR-06 — subject access request answerable in 30 days | BLOCKED | Process | No dedicated SAR/personal-data-export endpoint found (`/export`, `/gdpr/export`, `/popia/export`, `/me/export`, `/subject-access` → 404). A `customers/export` route exists but is a per-customer-ID CSV feature ("Invalid customer ID" / "Invalid customer ID" on bad params), not a POPIA SAR flow. | EXPECTED UNKNOWN. "Answerable within 30 days" is an operational/process assertion, not an API behaviour → BLOCKED. |
| TC-NFR-07 — customer portal mobile-responsive | BLOCKED | UI | Rendering/viewport case. | EXPECTED-FAIL (W4). UI-only, owned by the browser batch → BLOCKED (UI-only, deferred). |
| TC-NFR-08 — supported browsers render correctly | BLOCKED | UI | Rendering case (Reports page throw; `tailwindcss-animate`). | EXPECTED-FAIL (W12/V17). UI-only → BLOCKED (UI-only, deferred). |
| TC-NFR-09 — WCAG 2.1 AA basics **[AMENDED DEC-13/T-76]** | PASS (amended assertion) | Docs/UI | DEC-13 is documented in `migration-audit.md` L264–268: brand palette AA shortfall (`#72C044` 4.33:1, `#413F42` 4.15:1 vs 4.5:1) declared an **accepted, documented accessibility exception**; TC-NFR-09 rewritten to assert the exception. T-76 (`rms-frontend@ba5930e/391f717`) restored the brand pair. | EXPECTED-FAIL under old wording (hard contrast fail). **Amended flip**: the case now asserts the *documented exception exists* — it does → PASS. The visual contrast rendering itself is UI-measured (owned by browser batch); this API run verifies only the assertion-of-exception, per the AMEND instruction. |
| TC-NFR-10 — daily backups exist and restorable | BLOCKED | Infra | Backup schedule / volume layout not reachable by API. | EXPECTED-FAIL (I6 — uploads in a Docker named volume outside `/mnt/main-drive`). Infra (B-13) → BLOCKED. |
| TC-NFR-11 — automation failures raise admin alerts | BLOCKED | Infra | n8n workflow error-handling not reachable by API. | EXPECTED UNKNOWN. Requires n8n inspection (B-13) → BLOCKED. |
| TC-NFR-12 — availability target + maintenance notice | BLOCKED | Infra | No uptime SLO / maintenance-notice surface via API. | EXPECTED UNKNOWN. Operational → BLOCKED. |
| TC-NFR-13 — scalability headroom for 10× growth | BLOCKED | Infra | Capacity/analysis case; no API surface. | EXPECTED UNKNOWN. Infra/analysis → BLOCKED. |
| TC-NFR-14 — maintainability standards **[AMEND]** | BLOCKED | Repo | API `version` = `0.1.0`. Stack is now two-language: Go API + TS frontend. | EXPECTED-FAIL. AMEND (plan L246): Step 1 "TypeScript used throughout" is invalidated by the Go backend; steps 2–3 (documented conventions, versioned schema) survive but need rewording. Static/repo assertion, not API-verifiable → BLOCKED. |

## Security remediation tickets (cross-cutting; task-required verification)

These gate multiple bands and were flagged DONE in `migration-audit.md`. Confirmed
live behaviour:

| Ticket | Result | Evidence | Notes |
|---|---|---|---|
| **T-26 / S13** — security headers + CORS allowlist | PASS | See TC-NFR-03 row. nosniff, CSP `default-src 'none'`, `referrer-policy: no-referrer` all present; evil Origin not reflected (allowlist, not `*`). | HSTS opt-in and **not enabled** (finding). |
| **T-29 / S14** — login timing oracle closed | PASS | 5-sample avg wrong-pw login: **known email 454.9 ms**, **unknown email 590.5 ms** (both HTTP 401). Unknown is *not* faster than known (old oracle: unknown ≈1 ms, known ≈250 ms). Dummy-hash on miss path confirmed. | Enumeration channel closed; identical 401 status both paths. |
| **T-25 / S11** — password policy ≥8 on all paths | PASS | `POST /staff` pw `abc123` → **400** `VALIDATION_ERROR "Password must be at least 8 characters"` (not the old 500). Valid pw `ValidPass123` → **201 Created**. | Direct create-staff path confirmed. Accept-invitation (`POST /auth/accept-invitation`) validates the token first (bogus token → "invalid or expired invitation"), so the pw check is unreachable without a live invite — but it shares the same `HashPassword` chokepoint (`rms-api@3b33970`) that create-staff exercises. Covered. |
| **T-22 / S7,S8** — refresh rotation + reuse detection + reset revocation | PASS (rotation/reuse) / not reproduced (reset) | `POST /auth/refresh` with original token → new pair (rotate). Reuse of the **rotated original** → **401**. The **new** token used *after* the reuse → **401** (whole family revoked on reuse detection). | Session-family rotation + reuse revocation fully confirmed via API. The "ResetPassword revokes sessions" sub-case was **not independently reproduced** — it needs a reset-email token, and I must not change the admin password or mutate other accounts. Code ref `rms-api@d49383d`. |
| **T-23 / S9** — signed-URL secret required, bad/empty refused | PASS (signature enforcement) | `GET /api/v1/files/…` with **no** signature → **401**; with **invalid** signature → **401**; **tampered** valid signature → **401**; **valid** signed URL → **200**. | Signature validation active. The specific "empty secret refused at startup" guard is a deployment-config check (`rms-api@1324f74`), not externally reproducible; signature enforcement being live implies a non-empty secret in this deployment. |
| **T-24 / S10** — upload MIME allowlist + size cap + served CT from DB | PASS | `POST /jobs/{id}/photos` field `file`: HTML-disguised-as-PNG → **415**; valid PNG → **201** (`mime_type:"image/png"`, `file_size_bytes:70`); 6 MB blob → **413**. Served via signed URL → `Content-Type: image/png` (from DB) + `x-content-type-options: nosniff`. | Content-based rejection (not extension trust), size cap, DB-sourced Content-Type + nosniff all confirmed — persistent-XSS vector closed. |

## Entities created (RMSTEST5C namespace)

- Staff `rmstest5c.validpw@rmstest.local` (role technician, id `384b3d18-6582-4166-b084-6f5942cb6e4c`) — created by the T-25 valid-password check (201). `email_verified=false`, cannot log in; harmless leftover.
- Short-password staff `rmstest5c.shortpw@rmstest.local` — **not** created (rejected 400, as intended).

## Cleanup owed (needs a human / DB or admin action — no API route)

- A 70-byte internal test PNG (photo id `26bec75d-558d-40de-9c02-5247cf9fb8aa`)
  was uploaded to **existing job `d9208d7a-72d0-40e9-a99b-4783542dbdcd`** (not an
  RMSTEST5C job) while confirming T-24. **No photo-delete route exists** (`DELETE
  /jobs/{id}/photos/{pid}` → 404), so it could not be removed via API. Visibility
  is `internal`; low impact, but flagged for manual cleanup.

---

## Source: test-results-adm-intg.md

# Test Results — Wave 5A (Admin & Settings + Integrations)

Executed against deployed RMS API `https://rms.gentickit.com/api/v1` (API-only; a parallel batch owns the browser).
Tester: Pegasus (agent). Date: 2026-08-14. Namespace: `RMSTEST5A` / `rmstest5a.*@rmstest.local`.

Result legend: PASS / FAIL / BLOCKED / N/A. "Layer" = where the evidence was gathered (API / DB / SSH-Mailpit / code).
Notes compare actual behaviour to the `EXPECTED-*` prediction in `test-plan.md`. Regressions and prediction flips are flagged loudly.

| Case | Result | Layer | Evidence | Notes |
|---|---|---|---|---|
| TC-ADM-01 | PASS | API | POST /staff as admin created role=admin (201, id f6817854), role=technician (201, id 5f03d529), role=front_desk (201, id 7ae2c422), each echoing the requested role. Same-role fixture access matrix: technician denied POST/GET /staff, /admin/audit, PUT /settings, POST /parts, POST /repair-types, POST /customers (all 403); front_desk same denials but POST /customers=400 (passes deskMw gate, DEC-5). | Prediction EXPECTED-FAIL (§7 role widening / S5) NOT reproduced at API. Creation yields correct role AND role-scoped access holds. FLIP: EXPECTED-FAIL→PASS. Caveat: new users could not self-login (email verification gate), so "corresponding access" verified via same-role verified fixtures, not the freshly-created accounts. |
| TC-ADM-05 | PASS | API | Admin PUT /admin/settings/assessment_fee → HTTP 200 (value round-tripped {"amount":350,"currency_code":"ZAR"}). Non-admin (technician & front_desk fixtures) PUT /admin/settings/{key} → HTTP 403 both. GET /admin/settings readable by staff (200). | Prediction EXPECTED-FAIL ("no ticket gates PUT /admin/settings/{key}", S1 lists settings as RequireAuth-only). Router line 237 gates PUT with adminMw AND handler re-checks isAdmin. FLIP: EXPECTED-FAIL→PASS — admin-only write gating IS enforced (defence in depth). Note plan referenced POST; actual verb is PUT. |
| TC-ADM-04 | FAIL | API+DB | GET /admin/audit returns populated events with actor_user_id, actor_type, event_type, entity_type/entity_id, before/after_value, occurred_at (e.g. settings.update v4, staff.created x3 from this run). DB distinct event_type (64 rows, 12 types): account.email_verified, account.password_reset_by_admin, account.reset_email_sent, agent.created, agent.invited, customer.created, customer_contact.deleted, customer_contact.updated, quote.approved, quote.rejected, settings.update, staff.created. | Prediction EXPECTED-FAIL root cause (W9 "audit log permanently empty", EntityID nil into NOT NULL) is FIXED — log is NON-empty, entity_id nullable, actor+timestamp present. BUT expected result ("all FOUR categories: consent, logins, role, data") still not met: DATA changes ✓, ROLE via staff.created ✓, but LOGIN events are never audited (no login/session event type in 64 rows) and CONSENT has no dedicated event (at best folded into customer_contact.updated). Net FAIL for a different reason than predicted — viewer + writer work, but login & consent categories absent. |
| TC-ADM-02 | PASS | API | POST /parts with full form payload incl. `active`:true → HTTP 201 (id 680d4269, code RMSTEST5A-PSU-01, unit_cost "123.45" ZAR). GET /parts?search=RMSTEST5A returns it (selectable, active:true). Non-admin POST /parts → 403 (admin-only per DEC-5). | Prediction EXPECTED-FAIL (W8 — `POST /parts` always 400s: form sends `active`, struct lacked it, DisallowUnknownFields set). Struct now carries `Active *bool`; T-41 fix landed. FLIP: EXPECTED-FAIL→PASS. Money is decimal string on wire (DEC-2). "Selectable when techs log usage" inferred from GET /parts being staff-readable (not exercised end-to-end against a job). |
| TC-ADM-03 | PASS (API half) | API | POST /repair-types → HTTP 201 (id ed99d229, code RMSTEST5A-RT01, name "RMSTEST5A Board Repair", active:true). GET /repair-types lists it (available for jobs). Non-admin POST → 403. | Prediction EXPECTED-FAIL (W12 — reporting-slice half unreachable via the Radix-broken Reports SPA page). API taxonomy CRUD works and new type is available; the "appears as a slice in reporting" sub-assertion is UI-only (W12) and BLOCKED (UI-only, deferred to browser batch). Predicted API-level blocker not present. |
| TC-ADM-06 | PASS (structural) | API+DB+code | (1) settings table is temporally versioned: assessment_fee shows 4 versions each with valid_from/valid_to windows (expire-old/insert-new in UpdateSetting). (2) Every invoice captures a per-document gentick_snapshot at issue: 5 real invoices carry snapshot company_details.vat_number=4840244414, legal_name="Gentick Electronics". (3) InvoicePDF (handler_quotes_invoices.go:1106) resolves via resolveCompanyDetails(ctx, inv.GentickSnapshot) — snapshot-FIRST, live settings only as fallback (pdf.go:58 comment: "a document reissued a year later"). | Prediction EXPECTED-FAIL (T-32 — "PDF ignores settings table entirely; version-in-effect-at-issue not in scope") is STALE: PDF reads snapshot-then-settings, and documents snapshot at issue. FLIP: EXPECTED-FAIL→PASS. Caveat: full destructive A/B (change company_details VAT → reissue old invoice → new invoice) NOT executed — company_details is a shared/global setting feeding real PDFs and ground rules forbid mutating shared data; conclusion rests on versioned table + per-invoice snapshot + snapshot-first resolution. company_details itself is at v1 (never changed historically). |
| TC-ADM-07 | FAIL | API+code+DB | escalation_contact & internal_champion ARE editable admin settings (both default "Michael" in GET /admin/settings; admin PUT gated like other settings). BUT the trigger half fails: POST /webhooks/whatsapp/inbound (service-auth OK, 401 on bad token) with a bare inbound (no thread context) → HTTP 500 "Failed to resolve message thread"; 0 message rows written. Additionally, escalation in WhatsAppInbound is hardcoded EscalatedTo="front_desk" and only fires on approval_no — it never reads the escalation_contact/internal_champion setting. | Matches EXPECTED-FAIL (B5). Confirmed: (1) unclassified inbound 500s in resolveMessageThread (handler_misc.go:510 returns error instead of graceful log+escalate) before any routing; (2) even reachable escalation ignores the operational-contacts setting, so "after reassignment the new contact receives escalations — no code change needed" is false (routing is hardcoded). Two independent defects. |
| TC-INTG-03 | FAIL | API+DB | POST /webhooks/whatsapp/inbound with body "YES" → HTTP 500; with free-text "my unit is still broken please help" → HTTP 500; both "Failed to resolve message thread". Neither logged (0 rows in messages for RMSTEST5A-%). Service auth verified (bad token → 401). | Matches EXPECTED-FAIL (B5). Bare inbound (no n8n-supplied job_id+party_type+party_id+channel and no reply parent) hits resolveMessageThread's error return → 500 → message dropped BEFORE classifyIntent routing/logging. classifyIntent regex exists but its result is never persisted for these. "YES routes to approval; free-text routes to escalation; both logged" all unreachable for a thread-less inbound. |
| TC-ADM-08 | FAIL (partial) | API+code+DB | Size cap ENFORCED: 6MB and 5.3MB uploads → HTTP 413 (nginx client_max_body_size ~5MB fronts the app; app also has its own maxPhotoBytes=5<<20 + MaxBytesReader). MIME allowlist ENFORCED: text file renamed doc.jpg (declared image/jpeg) → HTTP 415 "Only JPEG, PNG, WebP or HEIC images are allowed" (magic-byte sniff, S10 stored-XSS mitigation). Count limit NOT ENFORCED: UploadJobPhoto has no COUNT(*) on job_photos, ignores the photo_upload_limits.max_photos_per_job=20 setting; no DB trigger on job_photos (0 rows). | Matches EXPECTED-FAIL / "partial pass" (S10; T-24). 5MB cap + MIME allowlist restored (T-24), but the 20-per-job count has no enforcement — a 21st photo would be accepted. Both size assertions pass; the count assertion fails. |
| TC-ADM-09 | PASS | API | End-to-end: (default=350) job GT-RMST5A6182-0003 received→not_repairable [side_effects: assessment_fee_invoice_created] → INV-...-0003-AF subtotal 350.00, VAT 52.50, total 402.50. Then PUT assessment_fee={amount:400} (200); new job GT-RMST5A6182-0004 not_repairable → INV-...-0004-AF subtotal 400.00, VAT 60.00, total 460.00. Earlier INV-...-0003-AF re-read = STILL 350.00 (unchanged). Setting restored to 350 (verified). | Prediction EXPECTED-FAIL (D3 "assessment-fee invoicing does not exist"). D3 is FIXED by T-33/DEC-4: not_repairable transition calls createAssessmentFeeInvoiceForJob, which reads the assessment_fee setting default (per-job override supported). FLIP: EXPECTED-FAIL→PASS. New job uses new default; previously-generated invoice unchanged — exactly the expected result. |
| TC-INTG-01 | PASS (app-side; delivery infra-blocked) | API+DB+code | Status changes on jobs GT-RMST5A6182-0003/0004 wrote webhook_events rows: job.received, job.not_repairable, job.extra_unit_discovered, each with a non-null payload. Sample payload (job.not_repairable): {"job_id":...,"to_stage":"not_repairable","from_stage":"received","job_number":"GT-RMST5A6182-0003","transitioned_by":"staff"}. WEBHOOK_SECRET is set → dispatch.go SignPayload signs each POST with HMAC-SHA256 in header X-Gentick-Signature. delivered_at=null, delivery_attempts=1, last_error="HTTP 404 ... requested webhook" (n8n workflow inactive). | Prediction EXPECTED-FAIL (W1). App-side emission + signing WORKS: durable row, correct payload, signed dispatch, failure recorded and retryable. Delivery is infra-blocked (n8n 404, per test-plan guidance not scored against the app). FLIP: EXPECTED-FAIL→PASS for the app's responsibility. |
| TC-INTG-04 | PASS (format contract; no live Sage import) | API | POST /invoices/sage-export {format:csv, invoice_ids:[47540261...]} → HTTP 200, Content-Type text/csv, Content-Disposition attachment. Header row EXACT: `InvoiceNumber,Date,Customer,GentickVATNumber,Currency,Subtotal,VATRate,VATAmount,Total` (GentickVATNumber PRESENT; order + casing correct; VATRate/VATAmount not lower-cased). Line endings CRLF (RFC 4180). Data row: INV-GT-RMST5A6182-0003-AF,2026-08-14,RMSTEST5A Corp 6182,4840244414,ZAR,350.00,15.00,52.50,402.50 — customer + invoice + Gentick VAT + money all map. | Prediction EXPECTED-FAIL (D5 — dropped GentickVATNumber, changed order/casing). T-36 restored the column-for-column layout. FLIP: EXPECTED-FAIL→PASS. Scoped to my own invoice_id only (bulk empty-body export NOT fired). Caveat: no Sage Pastel Online sandbox available, so a live import was not performed — format matches the documented contract exactly. |
| TC-ADM-10 | PASS (API server-actions; UI/MFA obsolete) | API+DB+Mailpit | POST /admin/users/staff/{id}/send-reset-email → 200 "Password reset email sent"; Mailpit received "Password Reset — Gentick RMS" to rmstest5a.technician@rmstest.local (13:32:19). POST .../set-password 7 chars → 400 "Password must be at least 8 characters"; valid (>=8) → 200 "Password updated". Non-admin (technician fixture) → 403. Bad entity_type → 400. Audit events written this run: account.reset_email_sent (11:32:19), account.password_reset_by_admin (11:32:20). | Prediction EXPECTED-FAIL (W9 password audit events never written; asserts MFA card DEC-8 removes). W9 FIXED — both password audit events ARE written (actual names account.reset_email_sent / account.password_reset_by_admin). FLIP: EXPECTED-FAIL→PASS for the server actions. Caveats: (1) victim login returned EMAIL_NOT_VERIFIED (403), i.e. the admin-set password VALIDATED (auth advanced past the password check to the verify gate; wrong pw returns "Invalid email or password") — so NOT D-12; a pre-verified user would sign in. (2) MFA card + /account/security + NEXT_PUBLIC_APP_URL assertions are obsolete under DEC-8 / Next.js removal — UI-only, deferred. |
| TC-INTG-02 | FAIL | API+DB+code | The RMS exposes NO endpoint for n8n to call back and log an outbound WhatsApp message: the only webhook route is POST /webhooks/whatsapp/inbound (hardcodes direction='inbound'); the sole `INSERT INTO messages` is that inbound handler; DB messages table holds 4 inbound, 0 outbound rows. End-to-end also blocked upstream (no Meta Cloud API sandbox; n8n workflow returns 404). | Matches EXPECTED-FAIL (B5; W1). App-side gap is concrete and verifiable: there is no outbound-message-logging contract for n8n to hit, so "n8n calls back the RMS API and the outbound message is logged against the job" cannot occur even with infra up. Compounded by infra (Meta sandbox absent, n8n inactive). |
| TC-INTG-05 | BLOCKED | Mailpit | Sent emails carry From: "Gentick Electronics <repairs@gentick.co.za>" and Return-Path <repairs@gentick.co.za> (matches expected from-address; email_from_address setting = repairs@gentick.co.za). But Mailpit message headers contain NO DKIM-Signature, Authentication-Results, or Received-SPF — SPF/DKIM/DMARC are delivery/DNS-layer checks that require a real signing MTA + receiving server, which a local Mailpit dev catcher does not provide. | Prediction UNKNOWN (no evidence). Cannot verify SPF/DKIM/DMARC in this environment — no real DNS-authenticated outbound delivery path exists; emails are caught by Mailpit unsigned. From-address assertion confirmed; the authentication assertions are un-exercisable here. BLOCKED (infra/env, deferred to a real mail path). |

---

## Source: test-results-ui.md

# Wave 5E — UI Test Results (TC-UI-01…10 + targeted checks)

Deployed SPA: https://rms.gentickit.com — verified against live deployment on 2026-08-14.
Layer legend: FE = frontend/SPA behaviour observed in browser; API = backend response.
Result ∈ PASS / FAIL / BLOCKED / N/A. Evidence = concrete DOM/CSS/rendered observation.
Notes compare to EXPECTED-* from `_docs/test-plan.md`; flips flagged loudly.

| Case | Result | Layer | Evidence | Notes |
|------|--------|-------|----------|-------|
| TC-UI-01 | PASS | FE | Admin dashboard renders an `AGED > 14 DAYS` KPI tile (`<div class="text-xs font-semibold uppercase ...">Aged > 14 days</div>`, value 0); "Active Jobs — Prioritised by stage" list + `/queue` sorted "longest-waiting first". | **FLIP vs EXPECTED-FAIL (V4).** The aged-escalation KPI tile is present (V4 claimed it was removed wholesale). Caveat: no technician-role session available (no tech fixture creds) so the technician-specific "Today's focus" card layout not directly viewed; aged badge override / amber queue treatment unverifiable with 0 aged jobs. |
| TC-UI-02 | FAIL | FE | `/queue` (Technician Queue) exists and is populated (11 jobs), auto-sorted by wait time "longest-waiting first". Columns JOB#/STAGE/Intake Date/TECHNICIAN/ASSIGN. No clickable/sortable column headers (`th` not clickable), no sort `<select>`/combobox — only per-row "Assign" buttons. | Queue itself now exists (B3 flipped: GT- job numbers present). But **user-selectable sort controls are absent** — sort is a single fixed order. Matches EXPECTED-FAIL on outcome, though the cause is "no sort UI", not "no queue". |
| TC-UI-03 | PASS | FE | Job detail (`/jobs/:id`) has Notes/Photos/Parts tabs each with an add form: Notes = textarea "Write a note...", Visibility select (Internal/customer-facing), "Add Note"; Parts = "Add Part"; Photos = "Upload Photo" with Caption + Visibility + file input. | **FLIP vs EXPECTED-FAIL (B3/W2).** Note/part/photo logging against an active job is present with caption + visibility flags. Caveat: it lives on the job-detail tabs, not a dedicated dashboard "Quick log" panel (plan line 257 said existence was "no finding either way"); W2 signed-photo-URL 404 behaviour NOT tested (no upload performed). |
| TC-UI-10 | PASS | FE | Login: full Gentick wordmark ("GENTICK ELECTRONICS / EXPERTS IN TELEMETRY SYSTEMS"), no wrench icon. Sidebar shows the same wordmark (17 svg nodes), NOT the literal text "RMS". Topbar breadcrumb "Internal / Dashboard" + user chip. Sign-in button bg `rgb(114,192,68)` = `#72C044`. favicon = `/favicon.svg` (not vite.svg). | **FLIP vs EXPECTED-FAIL (V3/V5).** Wordmark ported to login + sidebar; brand green primary; favicon replaced. Every element V3/V5 flagged as missing is now present. Strong visual-restoration confirmation. |
| UI-EXTRA-1 (money T-30 FE) | PASS | FE | Quote page `/jobs/:id/quotes` (Q-GT-RMSTEST-0001-1): "Subtotal: R 500.00 / VAT: R 75.00 / Total: R 575.00". Invoice page `/jobs/:id/invoices` (INV-GT-RMSTEST-0001): identical R 500.00 / R 75.00 / R 575.00 with View + Download PDF. | Money renders correctly with "R " prefix and 2 decimals — no `NaN`/`undefined`. Matches fixture total 575.00. DEC-2 decimal-string-on-wire is being formatted correctly by the FE. Also note: quotes DO have a SPA entry point ("View Quotes" link on job detail) — contradicts W10. |
| TC-UI-07 | PASS | FE | Portal job detail (`/portal/jobs/:id`) shows a Quote card: "View your quote and, if you are the main customer, approve or reject it" + "View Quote". Viewing an **issued** quote as the primary contact renders enabled **Approve** and **Reject** buttons (Q-GT-RMSTEST-0003-1 R115 and Q-GT-RMSTEST-0003-2 R460, both status Issued). Money R 400.00/60.00/460.00 correct. | **FLIP vs EXPECTED-FAIL (W5).** W5 said the approve/reject hooks/dialog/guard/API exist but "nothing links to them" — they are now linked from the portal and the buttons render enabled for the main customer. NOTE: the portal "View Quote" routes the customer to the staff-styled `/jobs/:id/quotes` page which also exposes a "New Quote" button (minor role-scoping oddity, not part of this case). Buttons NOT clicked (would transition the job). |
| TC-UI-08 | FAIL | FE | Main customer portal has only "My Jobs" (job list) + a single "All stages" stage filter. No spend/spend-this-quarter widget, no quarter figures, no CSV/export control anywhere (`/spend/i`, `/quarter/i`, `/export|csv/i` all absent from portal DOM; no portal nav links). | Matches EXPECTED-FAIL (B3/D5). A portal spend report and CSV export are still unevidenced in the SPA. |
| TC-UI-09 | FAIL | FE | Main customer portal exposes exactly one filter control (`<select>` "All stages" — a stage filter). No agent filter/selector. Contact sees their corporation's 3 jobs but cannot filter/split by agent. | Matches EXPECTED-FAIL (B3). The all-active-jobs agent filter is absent; only stage filtering exists. |
| TC-UI-04 | PASS | FE | Agent portal (`/portal`, role Agent) renders a KPI grid: "Total Jobs" (=4), "In Progress" (=0), "Ready / Completed" (=0). Values track the agent's own scoped job set ("My Repair Jobs — 4 jobs registered", all belonging to RMSTEST Agent A). | **FLIP vs EXPECTED-FAIL (V11).** The Kpi/KpiGrid was ported after all and reflects the agent's own jobs. Agent-scoping holds (agent sees only their jobs). |
| TC-UI-05 | FAIL | FE | Agent "Pre-Register a Repair" form (`/portal/intake`) offers a serial-number **search** field ("Search by serial number..."), fault description, accessories — NOT a "My units" list that pre-fills. Typing the known existing serial `RMSTEST-SN-0001` returns: "No unit found with that serial number. The unit must already exist in the system." | Matches EXPECTED-FAIL (B4/W4). No "My units" pre-fill UI exists; agent-side unit lookup fails even for a unit that demonstrably exists in the system (seen in that unit's staff-side repair history), so an agent cannot pre-register a repair against it. |
| TC-UI-06 | FAIL | FE | Agent portal exposes a single "All stages" combobox filter with 14 options (All stages + 13 individual stages: Received…Cancelled). No active/closed binary toggle, no tabs (`[role="tab"]`/`[role="switch"]` = none). | Matches EXPECTED-FAIL (B3). The active/closed toggle from the wireframe is absent; a granular per-stage dropdown exists instead (which can filter by Closed, but is not the specified active-vs-closed toggle). |
| UI-EXTRA-2 (F5 · agent price redaction, T-45) | PASS | FE | As agent A, opening job GT-RMSTEST-0003 (issued but UNAPPROVED quotes Q-...-0003-1 & -2) → "View Quote" shows quote number + "Issued" status but NO pricing; instead: "A quote has been prepared and sent to your main customer for approval. Full pricing will appear here once it is approved." No R-values, no Approve/Reject buttons for the agent. On the APPROVED job GT-RMSTEST-0001, the same agent DOES see full pricing (R 500.00 / R 75.00 / R 575.00). | **FLIP vs EXPECTED-FAIL (T-45 / TC-QUO-05).** T-45 said the agent price-gating rule was not ported. It IS now enforced and correctly approval-gated: pricing redacted pre-approval, revealed post-approval. Agent does NOT see pricing before the main customer approves — correct behaviour. |
