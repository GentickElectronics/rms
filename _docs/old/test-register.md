# Test Case Register

Source: `_docs/test-cases.xlsx`

Sheets:
- Summary (summary sheet, not a test case list)
- All Test Cases (145 test cases)
- Authentication and RBAC (13 test cases)
- Intake and Customer Mgmt (35 test cases)
- Workflow and State Machine (14 test cases)
- Communication and Notifications (15 test cases)
- Quoting and Billing (19 test cases)
- Reporting and Dashboard (10 test cases)
- Admin and Settings (10 test cases)
- UI - Wireframe (10 test cases)
- Non-Functional (14 test cases)
- Integrations (5 test cases)

---

## Summary

| Gentick RMS - Test Case Suite |  |  |  |  |
| Based on Requirements v1.0 and UI Wireframe v1.1  \|  Prepared 9 June 2026 |  |  |  |  |

| Module | Test Cases | High | Medium | Low |
|---|---|---|---|---|
| Authentication & RBAC | 10 | 9 | 1 | 0 |
| Intake & Customer Mgmt | 29 | 15 | 12 | 2 |
| Workflow & State Machine | 14 | 10 | 3 | 1 |
| Communication & Notifications | 15 | 10 | 5 | 0 |
| Quoting & Billing | 19 | 14 | 5 | 0 |
| Reporting & Dashboard | 10 | 1 | 7 | 2 |
| Admin & Settings | 9 | 3 | 5 | 1 |
| UI / Wireframe | 10 | 1 | 2 | 7 |
| Non-Functional | 14 | 5 | 7 | 2 |
| Integrations | 5 | 3 | 2 | 0 |
| TOTAL | 135 | 71 | 49 | 15 |

| Legend - Status values: Not Run / Pass / Fail / Blocked / N/A |  |  |  |  |

---

## All Test Cases

145 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-AUTH-01 | Authentication & RBAC | Gentick Admin can log in to internal portal | Sec 4.1, NFR-04 | Functional | High | Admin staff account exists in Supabase Auth. | 1. Navigate to the staff portal login page.<br>2. Enter valid admin email and password.<br>3. Submit. | Login succeeds over HTTPS and the Admin dashboard loads with full navigation (Admin dashboard, Technician queue, Job detail, New intake, settings). | Pass | Login succeeded; redirected to Admin dashboard. Full staff navigation present: Admin dashboard, Technician queue, New intake, Reports, Customers, Agents, Technicians & staff, Parts catalogue, Repair types, Message templates, Audit log, Settings. | Lucia |  |
| TC-AUTH-02 | Authentication & RBAC | Technician sees only technician-scoped navigation | Sec 4.1 | RBAC | High | Technician account exists. | 1. Log in as a Technician.<br>2. Inspect the landing page and sidebar navigation.<br>3. Attempt to open /dashboard and /reports directly. | Technician lands on the Technician queue ('My queue'). Sidebar under Internal-Staff shows ONLY Technician queue (no Admin dashboard, New intake or Reports). Repair initiation is unavailable (Can initiate repairs = No). Direct navigation to /dashboard and /reports redirects to /queue. No admin/management screens accessible. | Pass | RETEST after fix. Technician logs in and lands on '/queue' (My queue), not the Admin dashboard. Sidebar under 'Internal - Staff' shows only Technician queue - no Admin dashboard, no New intake, no Reports. Direct navigation to /dashboard and /reports both redirect to /queue (routes blocked, not just hidden). Admin/management screens remain hidden. Repair initiation still correctly blocked. | Lucia | PASS on retest. Original D-01 fix (queue landing + New intake hidden) confirmed. Additional fix applied during this retest: Admin dashboard + Reports links hidden for technicians AND routes blocked (redirect to /queue) - edits in components/ui/Sidebar.tsx, app/(staff)/dashboard/page.tsx, app/(staff)/reports/page.tsx. Closes the residual admin-data exposure. |
| TC-AUTH-03 | Authentication & RBAC | Front Desk can perform intake and dispatch | Sec 4.1 | RBAC | High | Front Desk account exists. | 1. Log in as Front Desk.<br>2. Open New intake and complete the required fields.<br>3. Open Customers and manage a record.<br>4. Open Agents and add an agent.<br>5. Attempt /settings, /dashboard, /reports and /queue directly. | Front Desk can create intake jobs, manage customer records, AND add agents. Sidebar shows New intake, Customers and Agents only; lands on New intake. Admin dashboard, Reports and Technician queue are hidden and their routes redirect to New intake. No access to admin-only Settings or staff management. | Pass | RETEST after fix. Front Desk (frontdesk@gentick.co.za) can now manage customer records: the Customers screen loads with an 'Add a customer' form and the customer list (6 customers). New intake works (form opens, required fields completable). Front Desk nav scoped to New intake + Customers only; Admin dashboard, Technician queue and Reports removed and their routes (/dashboard, /reports, /queue) redirect Front Desk to New intake. Settings and other admin-only screens remain blocked. Front Desk now lands on New intake after login. | Lucia | PASS on retest. D-02 fix confirmed (manage_customers permission + Customers UI). Additional scoping applied during retest at Lucia's request: removed Admin dashboard/Reports/Technician queue from Front Desk nav and blocked those routes; Front Desk home set to /intake. Edits in Sidebar.tsx, app/page.tsx, app/(staff)/dashboard\|reports\|queue/page.tsx. |
| TC-AUTH-04 | Authentication & RBAC | Main Customer Contact sees all jobs across their agents | Sec 4.1, FR-INT-10 | RBAC | High | Main customer with multiple agents and existing jobs. | 1. Log in as Main Customer Contact.<br>2. Open the repair overview. | All active jobs across all of the corporation's agents are visible; jobs belonging to other corporations are not visible. | Pass | Logged in as johan@acme.co.za - correct External Main customer portal ('Johan - repair overview'). Header cards: 'Active repairs across your agents' = 163 (1 agent), 'Awaiting your approval' = 6, 'Ready for collection' = 0. 'Action required' lists 6 quotes awaiting approval, all GT-ACME-* (Agent: Sipho Field) with View/Approve/Reject controls. All visible jobs belong to ACME; no other corporation's jobs shown. Correct portal (not staff dashboard). | Lucia | PASS on observable behaviour. Caveat: seed data has only ACME with a single agent (Sipho) and no second corporation, so 'all agents' and cross-corporation isolation are only partially exercised. Recommend adding a second ACME agent + a second corporation to fully prove TC-AUTH-04/05 isolation. |
| TC-AUTH-05 | Authentication & RBAC | Agent sees only their own jobs | Sec 4.1, FR-INT-10 | RBAC | High | Agent with own jobs; sibling agents under same main customer also have jobs. | 1. Log in as an Agent.<br>2. Open 'My repairs'. | Only the agent's own jobs are listed; jobs of sibling agents and other main customers are not shown. | Pass | Logged in as sipho@acme.co.za - correct External Agent portal ('My repairs - Sipho Field'). Cards: My active repairs 163, Awaiting approval 6 (quoted, pending main customer), Ready for collection 0, Completed YTD 37. 'My repairs' list shows only GT-ACME-* jobs with units/stages/submitted dates; 'My units' panel lists Sipho's units. No Approve/Reject controls present in the agent view. Correct agent portal (not staff/main-customer view). | Lucia | PASS on observable behaviour. Caveat: ACME has only one agent (Sipho) and no second corporation in seed data, so exclusion of sibling-agent / other-customer jobs cannot be fully demonstrated. Recommend adding a second ACME agent + second corporation to fully prove isolation. |
| TC-AUTH-06 | Authentication & RBAC | Agent cannot approve a quote | Sec 4.1, FR-COM-03, FR-QUO-02 | RBAC | High | Agent has a job in Quoted stage. | 1. Log in as the Agent.<br>2. Open the quoted job.<br>3. Attempt to find a quote approval control. | No approve/reject control is available to the agent for MVP; quote authority rests with the main customer only. | Pass | As agent Sipho, opened quoted job GT-ACME-0193 (Repair detail). Shows Quote Q-GT-ACME-0193-1 marked 'issued' with text: 'A quote has been prepared and sent to your main customer for approval. Full pricing will appear here once it is approved.' No approve/reject control present; pricing withheld from the agent. Progress timeline visible (Received -> Diagnosed -> Quoted). Quote authority correctly restricted to the main customer. | Lucia |  |
| TC-AUTH-07 | Authentication & RBAC | Agent self-service password reset via email link | FR-INT-13 | Functional | High | Registered agent who has forgotten password. | 1. On login, click 'Forgot password'.<br>2. Enter agent email.<br>3. Open the Supabase Auth reset email and follow the link.<br>4. Set a new password. | A clickable 'Forgot password' link opens a form to enter an email; submitting sends a Supabase reset email (generic confirmation, no account enumeration; in dev it lands in Mailpit :54324). The emailed link opens a 'set new password' page. After setting the password the recovery session is signed out and the user is redirected to the login page with a 'Password updated' confirmation, then signs in with the new password. Fully self-service, no staff intervention. | Pass | RETEST after fix. Login page now has a working 'Forgot your password?' link -> /auth/forgot-password. Submitting an email shows a generic 'if an account exists' confirmation (no enumeration) and sends a Supabase reset email (captured in Mailpit at 127.0.0.1:54324) with a 'Reset password' link and fallback code. The link opens /auth/reset-password ('Choose a new password'); setting a new password succeeds, signs out the one-time recovery session, and redirects to the login page showing 'Password updated - please sign in with your new password'. Login then succeeds with the new password. Fully self-service, no staff intervention. | Lucia | PASS on retest. D-03 fixed (forgot-password + reset pages, login link, Mailpit delivery). Post-reset UX improved during retest at Lucia's request: reset now signs out and redirects to login with a confirmation (edit in app/auth/actions.ts resetPassword + app/auth/login/page.tsx) instead of auto-entering the portal. |
| TC-AUTH-08 | Authentication & RBAC | MFA available for staff users | NFR-04 | Security | Medium | A staff account exists. Local Supabase has MFA TOTP enabled ([auth.mfa.totp] in supabase/config.toml; restart Supabase after enabling). An authenticator app (TOTP) is available. Enrolment UI lives at /account/security. | 1. Log in as staff.<br>2. Open security settings.<br>3. Enable multi-factor authentication.<br>4. Log out and back in. | MFA can be enabled and is enforced on subsequent staff logins. | Pass | RETEST after fix (Supabase restarted to apply [auth.mfa.totp]). Logged in as admin -> Security page (/account/security) offers MFA enrolment: TOTP QR scanned with an authenticator app and verified with a 6-digit code. After enrolment, signing out and back in as admin prompts for the MFA code (/auth/mfa) before granting access; entering a valid code completes login. MFA can be enabled and is enforced on subsequent staff logins. | Lucia | PASS on retest. D-04 fixed (TOTP enrolment at /account/security, login challenge at /auth/mfa, staff layout enforces AAL2). Requires local Supabase restart so config.toml [auth.mfa.totp] takes effect. Note: once enrolled, all subsequent admin logins require the authenticator code. |
| TC-AUTH-09 | Authentication & RBAC | Invalid credentials are rejected | NFR-04 | Negative | High | Any account exists. | 1. Go to login.<br>2. Enter a valid email with a wrong password.<br>3. Submit. | Login is rejected with a generic error; no indication whether email exists; no session created. | Pass | Entered valid email (admin@gentick.co.za) with an incorrect password and submitted. Login rejected with a generic error 'Invalid login credentials' - does not reveal whether the email exists. User remained on the login page; no session created. | Lucia | Generic error wording is correct (no account-enumeration). PASS. |
| TC-AUTH-10 | Authentication & RBAC | Direct URL access to another role's page is blocked | Sec 4.1, NFR-04 | RBAC | High | Logged in as Agent. | 1. While authenticated as an agent, request a staff-only URL (e.g., system settings) directly. | Access is denied (redirect or 403); no staff data is exposed. | Pass | Logged in as agent (sipho@acme.co.za). Entered the staff-only URL localhost:3000/settings directly in the address bar. Access denied - redirected to http://localhost:3000/portal (agent home). The Settings page and its staff/company data were not exposed. | Lucia | Role-based route protection works: agent requesting a staff URL is redirected to the agent portal, no staff data leaked. PASS. |
| TC-INT-01 | Intake & Customer Mgmt | Capture mandatory main customer fields at registration | FR-INT-01 | Functional | High | Admin/Front Desk creating a new main customer. | 1. Open new main customer form.<br>2. Enter Corporation Name, Contact Person, Contact Email, Contact Phone.<br>3. Save. | Record saved with all four fields persisted and retrievable. | Pass | RETEST after fix. The 'Add a customer' form now captures all four mandatory fields. Created prefix RETEST / 'Retest Customer One' with Contact person 'Pat Primary', Contact email retest-primary@example.com, Contact phone +27821119999 and a temporary password. Save succeeded: the corporation appears in the Customers list with 1 contact, and the Contacts list shows 'Pat Primary' (Primary) under Retest Customer One, Active (login provisioned). All four mandatory fields are persisted and retrievable from a single registration step. | Lucia | PASS on retest. Fix: customer registration now captures Contact Person/Email/Phone (required) + an optional temporary password and creates the customer AND its primary contact atomically (createCustomerWithContact, withTransaction). Prefix/Corporation alignment fixed by moving to FormGrid. Files: components/feature/CustomersManager.tsx, app/(staff)/customers/actions.ts, lib/people/people.ts, components/ui/Field.tsx. Note: contact email is format-validated only - no verification email is sent. |
| TC-INT-02 | Intake & Customer Mgmt | Main customer registration rejects missing required fields | FR-INT-01 | Negative | High | New main customer form open. | 1. Leave Corporation Name and Contact Email blank.<br>2. Attempt to save. | Save blocked with inline validation on the missing required fields. | Pass | RETEST after fix. Clicking Add customer with all fields blank now blocks the save and shows field-level inline validation on EVERY required field at once: Prefix 'must be 2-10 letters/numbers', Corporation name 'required', Contact person 'required', Contact email 'invalid email', Contact phone 'use E.164 e.g. +27821234567'. Errors render inline beneath each field (no longer a single top-of-form banner) and all missing fields are surfaced simultaneously. No record was created. | Lucia | PASS on retest. Fix: added client-side field-level validation surfacing all missing/invalid required fields together, rendered inline via a new optional 'error' prop on the shared Field component; server-side zod validation retained as backstop. Resolves both original gaps (banner-not-inline and only the first/prefix error being shown). |
| TC-INT-03 | Intake & Customer Mgmt | Reject invalid email/phone formats on customer record | FR-INT-01, FR-INT-02 | Negative | Medium | New customer/agent form open. | 1. Enter 'notanemail' in Contact Email and 'abc' in Phone.<br>2. Attempt to save. | Validation errors are shown for malformed email and phone; record not saved. | Pass | Entered 'notanemail' in Contact Email and 'abc' in Phone on the new customer form and attempted save. Inline validation errors shown on both fields; record not saved. | Lucia |  |
| TC-INT-04 | Intake & Customer Mgmt | Capture agent fields linked to a main customer | FR-INT-02 | Functional | High | An existing main customer. | 1. Create an agent with Agent Name, Phone, Email, Address.<br>2. Link to the main customer.<br>3. Save. | Agent saved and associated to the correct main customer. | Pass | Created an agent with name, phone, email and address, linked to a main customer; save succeeded and the agent appears in the Agents list under the correct customer. DEFECT found during this case (fixed same session): the Add agent form showed only one validation error at a time instead of all invalid fields at once. Fix mirrors the TC-INT-02 customer-form fix — client-side field-level validation surfacing all errors inline via the Field 'error' prop; applied to BOTH the staff Agents form and the portal '+ Add an agent' form. Files: components/feature/AgentsManager.tsx, components/feature/PortalAddAgent.tsx. Server-side zod validation (agentInput) unchanged as backstop. | Lucia |  |
| TC-INT-05 | Intake & Customer Mgmt | Capture WhatsApp opt-in with timestamp, IP, and wording version | FR-INT-03 | Functional | High | First-time registration of a contact/agent via web. | 1. Complete registration and tick the WhatsApp consent box.<br>2. Submit.<br>3. Open the resulting consent record / audit log. | A ConsentRecord is stored with granted timestamp, source IP (web), and the consent wording version shown to the user. | Fail | FAIL — feature not implemented. The consent_records table exists in the DB schema (001_initial_schema.sql) but no application code writes to it: no WhatsApp consent checkbox in any registration/invite flow, so no ConsentRecord (timestamp/IP/wording version) is captured. See defect D-06. | Lucia/Claude (code inspection) |  |
| TC-INT-06 | Intake & Customer Mgmt | Opt-out toggle immediately revokes WhatsApp consent and logs it | FR-INT-04 | Functional | High | Contact/agent with active WhatsApp consent. | 1. Open the profile.<br>2. Switch the WhatsApp opt-out toggle on.<br>3. Save.<br>4. Inspect consent/audit log. | Consent is revoked immediately, a revocation event with timestamp is logged, and no further WhatsApp messages are sent to that party. | Fail | FAIL — feature not implemented. No WhatsApp opt-out toggle exists on any profile screen and no code writes revocation events to consent_records. See defect D-06. | Lucia/Claude (code inspection) |  |
| TC-INT-07 | Intake & Customer Mgmt | Annual consent audit flags consents older than 12 months | FR-INT-05 | Functional | Medium | A consent record dated >12 months ago exists. | 1. Run / open the annual consent audit workflow. | The aged consent is flagged for renewal; consents under 12 months are not flagged. | Fail | FAIL — feature not implemented. No annual consent audit workflow exists in the app or n8n; consent_records is never populated so there is nothing to audit. See defect D-06. | Lucia/Claude (code inspection) |  |
| TC-INT-08 | Intake & Customer Mgmt | Intake via main customer origin path | FR-INT-06 | Functional | High | Logged in as Front Desk / main customer. | 1. Create a job with origin = Main customer (pre-registered).<br>2. Capture unit details.<br>3. Save. | Job created with shipment origin 'main_customer'; identical data fields captured as other paths. | Pass | Created a job via New intake with origin = Main customer (pre-registered), captured unit details and saved. Job created with shipment origin 'main_customer'; same data fields captured as the other intake paths. | Lucia |  |
| TC-INT-09 | Intake & Customer Mgmt | Intake via agent origin path (self-service) | FR-INT-06, Sec 2.1 | Functional | High | Logged in as Agent on mobile. | 1. Use 'Book a new repair'.<br>2. Register a unit and fault.<br>3. Submit. | Job created with origin 'agent'; agent portal is intake-capable, not read-only. | Pass | Logged in as agent (sipho@acme.co.za), used 'Book in a repair', registered a unit and fault, submitted. Job created with origin 'agent'; agent portal confirmed intake-capable. DEFECT found during this case (D-07, fixed same session): incomplete units showed only one banner error and blank unit rows were silently dropped before submit. Fixed in components/feature/AgentIntakeForm.tsx; retested Pass. | Lucia |  |
| TC-INT-10 | Intake & Customer Mgmt | Intake via Gentick front desk for unannounced unit | FR-INT-06 | Functional | High | Front Desk receives an unregistered unit. | 1. Open New intake.<br>2. Select origin = Walk-in / unannounced.<br>3. Identify the owning customer/agent.<br>4. Save. | Job created with origin 'walk_in'; the same data fields are captured as the other two paths. | Pass | Logged in as Front Desk, created a walk-in intake for an identified agent (ACME/Sipho Field) and saved. Job created with origin 'walk_in'; same data fields captured as the other paths. DEFECT found during this case (D-08, fixed same session): incomplete units showed only the first server error in a banner. Fixed in components/feature/IntakeForm.tsx; retested Pass. | Lucia |  |
| TC-INT-11 | Intake & Customer Mgmt | Walk-in requires customer/agent identification before save | FR-INT-06, Wireframe Step 1 | Negative | High | New intake, origin = Walk-in. | 1. Leave the originator/owner unidentified.<br>2. Attempt to save the shipment. | Save is blocked with a prompt to identify the customer/agent (per wireframe hint 'Walk-in needs you to identify customer/agent before save'). | Pass | As Front Desk, attempted to save a walk-in shipment with the agent/owner unidentified. Save blocked with an inline 'select the agent the unit(s) belong to' error on the Agent field (per D-08 fix). Enhancement added same session: selecting an agent now auto-backfills the Main customer field. | Lucia |  |
| TC-INT-12 | Intake & Customer Mgmt | Register multiple units in a single shipment | FR-INT-07, Wireframe Step 2 | Functional | High | New intake open. | 1. Add Unit 1 and Unit 2 via '+ Add another unit'.<br>2. Complete both units.<br>3. Receive shipment. | Each unit receives its own job number while sharing one shipment identifier (e.g., SHP-0438). | Pass | Added two units to one shipment via '+ Add unit', completed both and received the shipment. Each unit received its own job number while sharing a single shipment identifier. | Lucia |  |
| TC-INT-13 | Intake & Customer Mgmt | Job number format and per-customer sequencing | FR-INT-08, Sec 14 #1 | Functional | High | Main customer with prefix 'ACME' and last job GT-ACME-0184. | 1. Create a new job for ACME.<br>2. Inspect the generated job number. | Job number is GT-ACME-0185 - prefix from customer record, sequence per-customer, zero-padded to 4 digits. | Pass | Created a new job for ACME and inspected the generated job number: format GT-ACME-NNNN with the prefix from the customer record, per-customer sequence incremented by one from ACME's previous job, zero-padded to 4 digits. | Lucia |  |
| TC-INT-14 | Intake & Customer Mgmt | Walk-in / unlinked jobs use fallback prefix GT-GEN | FR-INT-08 | Functional | High | Design note: every job links to an agent (jobs.agent_id NOT NULL), so a customer prefix always exists. The GT-GEN fallback is a code-level safety net only (lib/jobs/numbering.ts) with no reachable UI path. | 1. Create a walk-in job without an assigned customer prefix.<br>2. Inspect the job number. | N/A by design — walk-ins require owner identification before save (verified by TC-INT-11), so all jobs are numbered GT-{CUSTOMER_PREFIX}-NNNN. The GT-GEN fallback remains in nextJobNumber() for the data-level edge case of a job with no main customer and would produce e.g. GT-GEN-0001 if ever invoked. | N/A | Marked N/A on 7 Jul 2026 — superseded by design. UI enforces agent identification (TC-INT-11 Pass), making an unlinked job impossible to create; fallback verified present in code (FALLBACK_PREFIX='GEN' in lib/jobs/numbering.ts). | Lucia/Claude |  |
| TC-INT-15 | Intake & Customer Mgmt | Job number sequence is independent per customer | FR-INT-08, Sec 14 #2 | Functional | Medium | Two main customers ACME and KAROO. | 1. Create a job for KAROO, then one for ACME.<br>2. Compare numbers. | KAROO uses its own sequence (e.g., GT-KAROO-0043) independent of ACME's sequence; no cross-customer collisions. | Pass | Created a job for a second main customer, then one for ACME, and compared job numbers: each customer's sequence advanced independently with its own prefix; no cross-customer collisions. | Lucia |  |
| TC-INT-16 | Intake & Customer Mgmt | Capture all per-unit intake fields | FR-INT-09 | Functional | High | New intake, a unit being added. | 1. Enter unit type, serial number, fault description, accessories shipped.<br>2. Attach intake photos.<br>3. Save. | All per-unit fields persist, including at least one intake photo reference. | Pass | New intake completed with all per-unit fields (unit type, serial, fault description, accessories) and a photo attached in the form via 'Photos of condition on arrival'. After receive, the job showed all fields and the photo persisted (customer-visible, caption 'Intake - condition on arrival'). | Lucia | Passed as originally written after the in-form intake photo enhancement (see Intake defect record, enhancement E: photos at intake). Previously would have required a two-step flow via Job detail Photos panel. |
| TC-INT-17 | Intake & Customer Mgmt | Serial number auto-links to prior repair history | FR-INT-09, Wireframe Step 2 | Functional | Medium | Serial SN-21A847 has 2 prior repairs. | 1. Begin a new unit and enter serial SN-21A847. | System detects the existing unit and shows 'This unit has been here before (2 prior repairs) - auto-linked.' | Pass | Entered serial GE1145 (2 prior repairs) on New intake: green banner appeared under the Serial field - 'This unit has been here before (2 prior repairs) - auto-linked' with last job number and last-seen date. Banner clears when the serial is edited. Job detail page shows the new 'Previous repairs for this unit' card. | Lucia | Passed after building the indicator in this session (see Intake defect record, enhancement 5: prior-repair-history indicator). Server-side auto-link by case-insensitive serial already existed; UI feedback and job-detail history panel were added. Staff-only lookup so counts don't leak to portal users. |
| TC-INT-18 | Intake & Customer Mgmt | Jobcard emailable to originator at intake | FR-INT-10, Wireframe Step 3 | Functional | High | Shipment ready to receive. | 1. Complete intake and confirm 'Receive shipment & notify'. | Jobcard(s) are emailed to the originator (e.g., the agent) at the point of intake. | Pass | Intake of GT-TE1-0003 emitted job.received with the full jobcard payload (job number/fault, unit serial/type, agent name+email+phone, main customer + primary contact, shipment origin/tracking) - verified in webhook_events. Delivery to local n8n returned 404 (workflow 01 not imported/active in local n8n) - environment config, not app code; event remains queued for retry. Required a dev-server restart to pick up the new code. | Lucia | Pass on the agreed app-side criterion (see Intake defect record, enhancement 6): createIntake now emits job.received per job post-commit; n8n workflow 01 turns it into the jobcard email. Actual email delivery requires the n8n workflow to be imported/activated and a mail provider (Postmark) configured - to be covered under the Communication module tests. |
| TC-INT-19 | Intake & Customer Mgmt | Both main customer and agent can view jobcards in portal | FR-INT-10 | Functional | Medium | A job exists for an agent under a main customer. | 1. Log in as the agent and open the job.<br>2. Log in as the main customer and open the same job. | Both parties can view the relevant jobcard (current and historical) through their portals. | Pass | Logged in as the agent (sipho@acme.co.za) and opened an ACME job from the agent portal, then as the main customer (johan@acme.co.za) and opened the same job. Both portals showed the jobcard with current status and history. | Lucia |  |
| TC-INT-20 | Intake & Customer Mgmt | Main-customer-led agent invitation flow | FR-INT-11, FR-INT-13 | Functional | High | Logged in as Main Customer Contact. | 1. Click 'Invite new agent'.<br>2. Enter the agent's details and send.<br>3. Open the invitation email as the agent and follow the link.<br>4. Complete the profile incl. WhatsApp opt-in on first login. | Invitation email is sent, agent self-completes profile and consent, and the agent account is linked to the inviting main customer. | Fail | No invitation email flow exists: the portal '+ Add an agent' creates the agent directly with a password set by the main customer contact (createAgent/provisionUnverifiedAuthUser). No email sent, no link, no agent self-completion, no WhatsApp opt-in (D-06). accept-invitation page and invitedAt/invitationAcceptedAt fields exist as scaffolding only. | Lucia | Fail - grouped into the 'agent invitations + WhatsApp consent' work package with D-06 (see defect record D-09). Build scheduled after the test run. |
| TC-INT-21 | Intake & Customer Mgmt | Pending agent invitation shows expiry and resend | FR-INT-11, Wireframe | Functional | Low | An invitation has been sent but not accepted. | 1. As main customer, open 'Your agents'. | Pending invite is shown with an 'Invited' badge and an expiry date plus a 'resend invite' action. | Fail | Not testable - no pending-invitation state exists because no invitation email flow is implemented (see TC-INT-20). No expiry or resend UI anywhere. | Lucia | Fail - grouped into the 'agent invitations + WhatsApp consent' work package with D-06 (see defect record D-09). Build scheduled after the test run. |
| TC-INT-22 | Intake & Customer Mgmt | Expired invitation link cannot be used | FR-INT-11 | Negative | Medium | An invitation token past its expiry. | 1. Open the expired invitation link. | Link is rejected as expired; agent is prompted to request a new invite; no account created. | Fail | Not testable - no invitation links are ever generated (see TC-INT-20), so an expired-link rejection path cannot exist. | Lucia | Fail - grouped into the 'agent invitations + WhatsApp consent' work package with D-06 (see defect record D-09). Build scheduled after the test run. |
| TC-INT-23 | Intake & Customer Mgmt | Extra units discovered - main customer originator routing | FR-INT-12(a) | Notification | Medium | Shipment originated by main customer; extra unit found at intake. | 1. At intake, add the extra unit beyond what was registered.<br>2. Save and notify. | Both the main customer AND the agent are notified about the extra unit. | N/A | N/A by design in this build: main customers cannot pre-register units (no booking form), so a main-customer-origin shipment has no announced baseline against which an 'extra' unit can be detected. Extra-unit detection exists only for agent pre-registrations (TC-INT-24). If main-customer pre-registration is added later, this case becomes testable. | Lucia | Reworded per design decision 8 Jul 2026; see defect record enhancement 7 (extra-unit discovery). Revisit if main-customer pre-registration is built. |
| TC-INT-24 | Intake & Customer Mgmt | Extra units discovered - agent originator routing | FR-INT-12(b) | Notification | Medium | Shipment originated by agent; extra unit found. | 1. Add the extra unit at intake.<br>2. Save and notify. | The agent is notified and the main customer is copied for visibility. | Pass | PASS. Front Desk (Pieter) received a shipment for Sipho (ACME): pre-registered unit GE55432-1 added via 'Add to shipment' (existing job GT-ACME-0212, no duplicate created) + hand-entered extra SN-TEST-24B (new job GT-ACME-0223). webhook_events verified in DB (22 Jul 09:51:17): job.extra_unit_discovered emitted for GT-ACME-0223 ONLY (not for GT-ACME-0212), before both jobs' job.received events, with agent + main customer + originator_type routing payload for n8n (agent notified, main customer copied). delivered_at NULL / 1 attempt = queued pending n8n workflow 01 import — agreed local pass criterion (as TC-INT-18). | Lucia | Evidence: webhook_events query 22 Jul 2026. Full delivery to be re-verified in the Communication module run once n8n workflow 01 is imported. |
| TC-INT-25 | Intake & Customer Mgmt | Extra units discovered - walk-in routing by identified owner | FR-INT-12(c) | Notification | Medium | No pre-registration; extra units found. | 1. Ensure the agent has NO pending pre-registrations.<br>2. Front desk creates the jobs at intake (origin = Walk-in / unannounced), identifying the owning agent.<br>3. Confirm the no-extra-units checkbox and save.<br>4. Inspect webhook_events for the created jobs. | Jobs are created and the job.received notification routes to whichever party the front desk identifies as the owner (payload carries the identified agent + main customer/primary contact). NOTE (agreed 22 Jul 2026): with no pre-registration baseline, job.extra_unit_discovered deliberately does NOT fire for walk-ins (same design reasoning as TC-INT-23 N/A); the pass criterion is correct owner routing on job.received. Local pass criterion is the webhook_events row per job, as agreed for TC-INT-18. | Pass | PASS (per reworded criterion agreed 22 Jul). Front Desk (Pieter) walk-in intake, agent FD Test Agent 0624 (ACME Industries) — agent had NO pending pre-registrations (no pending card shown, precondition held). Unit SN-TEST-25W -> new job GT-ACME-0224. webhook_events verified (10:01:02): single job.received, NO job.extra_unit_discovered (correct — no pre-reg baseline). Routing payload verified: agent='FD Test Agent 0624', main_customer='ACME Industries', originator_type='front_desk' — notification routes to the owner front desk identified. delivered_at NULL = queued pending n8n import (agreed local criterion). | Lucia |  |
| TC-INT-26 | Intake & Customer Mgmt | Confirm-no-extra-units checkbox required before receive | Wireframe Step 3 | UI | Medium | Intake at Step 3. | 1. Leave the 'no extra unannounced units' confirmation unticked.<br>2. Attempt 'Receive shipment & notify'. | Receiving is gated on the confirmation tick; instruction reminds user to add extras above first if any exist. | Pass | PASS. Executed as Front Desk (Pieter) on New intake, agent = Sipho (ACME). (1) Valid unit SN-TEST-26A entered with the 'no extra unannounced units' checkbox unticked -> 'Confirm & notify' blocked with inline red error under the checkbox and top banner; no job created. (2) Checkbox ticked, resubmitted -> intake succeeded, job GT-ACME-0222 created. Serial-history banner subsequently confirmed the job (re-confirms TC-INT-17). Server backstop (confirmedNoExtraUnits in CreateIntakeSchema/createIntake) verified in code review 22 Jul. | Lucia | Side effect noted: Sipho had 15 pending pre-registrations at the time, so SN-TEST-26A was treated as an unannounced extra — a job.extra_unit_discovered event is expected for GT-ACME-0222 in webhook_events; to be verified during the TC-INT-24/25 DB check. VERIFIED 22 Jul: webhook_events shows job.extra_unit_discovered + job.received for GT-ACME-0222 at 09:48:20. |
| TC-INT-27 | Intake & Customer Mgmt | Save draft preserves an in-progress intake | Wireframe Step 1 | Functional | Low | Partial intake entered. | 1. Click 'Save draft'.<br>2. Navigate away and return. | Draft is retained with previously entered data; no job numbers issued until receive. | Fail | FAIL — feature not implemented. No 'Save draft' control exists on staff New intake (only Cancel / Book in repair), and no draft route, state or persistence exists anywhere in the app (no match for 'draft' in application code). See defect D-10; build deferred to after the test run per the D-06/D-09 pattern (priority Low). | Lucia/Claude (code inspection) | Logged 22 Jul 2026 by code inspection. D-10 in Gentick_RMS_Intake_Defects_2026-07-07.md. |
| TC-INT-28 | Intake & Customer Mgmt | Add an additional (non-primary) contact to a corporation | FR-INT-01 | Functional | Medium | A corporation exists with a primary contact (captured at registration). Logged in as Admin or Front Desk. | 1. Open Customers.<br>2. In 'Add another contact', select the corporation.<br>3. Enter Contact person and Email (login); optionally a Phone and a temporary password; leave 'Mark as the primary contact' unchecked.<br>4. Click Add contact. | The additional contact is created and listed under the corporation in Contacts WITHOUT a Primary badge; the corporation's contact count increments. If a temporary password was supplied, the new contact can log in to the main-customer portal. | Pass | PASS (both parts). Front Desk (Pieter), Customers -> 'Add another contact', ACME Industries. (A) Thandi Test (thandi@acme.co.za, password set) added with 'Mark as primary' UNTICKED -> listed under ACME without Primary badge, Login Active, contact count incremented; Johan initially retained Primary. (B) D-11 retest: Marius Test (marius@acme.co.za) added with 'Mark as primary' TICKED -> Marius shows the Primary badge and Johan's badge is REMOVED — exactly one primary per corporation, demote-in-transaction fix verified in UI. Johan restored as primary via SQL immediately after (test data restored). Typecheck (npx tsc --noEmit) clean on the D-11 change. | Lucia | Adjacent defect D-11 found by code inspection 22 Jul 2026: creating a contact with 'Mark as primary' did not demote the existing primary (two primaries possible; primary drives quote-approval routing and invite_agent). Fixed same day in lib/people/people.ts (createContact demotes existing primary in-transaction). Retest during this case: also verify the primary-tick path demotes the old primary. |
| TC-INT-29 | Intake & Customer Mgmt | 'Add another contact' rejects missing customer and invalid email/phone | FR-INT-01, FR-INT-02 | Negative | Medium | Customers page open as Admin or Front Desk. | 1. In 'Add another contact', click Add contact with no customer selected.<br>2. Select a customer, then enter 'notanemail' as Email and 'abc' as Phone.<br>3. Attempt to save. | Save is blocked: 'Select a customer' is shown when none is chosen; validation errors are shown for the malformed email and phone; no contact is created. | Pass | PASS. Front Desk (Pieter), Customers -> 'Add another contact' submitted with Customer unselected, Contact person blank, email 'notanemail', phone 'abc': all four errors flagged inline simultaneously (select a customer / required / invalid email / E.164 format), no server call, no contact created. Validation from the 7 Jul proactive sweep confirmed in UI. | Lucia |  |
| TC-INT-30 | Intake & Customer Mgmt | Add agent (staff) surfaces all invalid/missing fields at once; no record saved | FR-INT-02 / D-05 | Negative | High | Logged in as Admin or Front Desk; Agents screen open. | 1. Leave all fields blank and click 'Add agent'.<br>2. Then enter 'notanemail' in Email, 'abc' in Phone, a 3-char password, leaving Address/City blank.<br>3. Click 'Add agent' again. | Step 1: inline errors shown simultaneously under Main customer ('select a customer'), Agent name ('required'), Email ('invalid email'), Address line 1 ('required') and City ('required'). Step 2: inline errors for invalid email, phone ('use E.164 e.g. +27821234567') and password ('min 8 characters') shown together. No agent record is created in either step. | Pass | Verified 7 Jul 2026 during D-05 retest: blank/invalid submits flag ALL missing/invalid fields at once, inline beneath each field; no record saved. | Lucia | Added after D-05 (agent form showed only one error at a time). Fix in components/feature/AgentsManager.tsx; server-side zod (agentInput) is the backstop. |
| TC-INT-31 | Intake & Customer Mgmt | Portal '+ Add an agent' surfaces all invalid/missing fields at once; no record saved | FR-INT-02, FR-INT-11 / D-05 | Negative | Medium | Logged in as a primary main-customer contact (e.g. johan@acme.co.za); 'Your agents' card open. | 1. Click '+ Add an agent', leave all fields blank and click 'Add agent'.<br>2. Then enter invalid email/phone and a short password.<br>3. Click 'Add agent' again. | All missing/invalid fields are flagged simultaneously with inline errors (name required, invalid email, E.164 phone if entered, address/city required, password min 8 if entered). No agent record is created. | Pass | PASS. Executed as Thandi Test (thandi@acme.co.za), promoted to ACME primary contact via the new edit-contact feature (Johan's login was blocked by an open auth issue — see defect log). Portal 'Your agents' -> '+ Add an agent' submitted with name/address/city blank and email 'notanemail': ALL invalid/missing fields flagged inline simultaneously (name required, invalid email, address required, city required); phone and temp password correctly optional; no server call made and no agent record created (agent list unchanged). Bonus verifications en route: non-primary contact saw NO '+ Add an agent' control before promotion (re-confirms TC-AUTH-13), and unverified-contact first login correctly demanded a 6-digit email OTP (delivered via Mailpit). | Lucia | Companion to TC-INT-30; same D-05 fix applied to components/feature/PortalAddAgent.tsx. |
| TC-INT-32 | Intake & Customer Mgmt | Agent 'Book in a repair' flags all missing unit fields on all units; blank units not silently dropped | FR-INT-06, FR-INT-09 / D-07 | Negative | High | Logged in as an Agent; 'Book in a repair' open with at least two unit rows, one incomplete/blank. | 1. Complete Unit 1 fully; leave Unit 2 entirely blank.<br>2. Click 'Book in repair' / 'Confirm & notify'.<br>3. Observe validation.<br>4. Remove or complete Unit 2 and resubmit. | Step 2: submission is blocked; ALL missing required fields (Serial number, Unit type, fault description) are flagged inline on EVERY incomplete unit simultaneously, with a banner instructing to complete highlighted fields or remove unused units. The blank unit is NOT silently dropped. Step 4: submission succeeds and each completed unit gets its own job with origin 'agent'. | Pass | Verified 7 Jul 2026 during D-07 retest: blank unit row flagged on all three required fields inline; nothing silently dropped; after completing/removing the row the booking succeeded. | Lucia | Added after D-07. Fix in components/feature/AgentIntakeForm.tsx (per-unit inline validation, no silent filtering, stale-error clearing on add/remove). Server-side IntakeUnitSchema is the backstop. |
| TC-INT-33 | Intake & Customer Mgmt | Staff New intake flags all missing unit fields on all units and agent selection inline | FR-INT-06, FR-INT-09 / D-08 | Negative | High | Logged in as Admin or Front Desk; New intake open with two unit rows, one complete and one blank; no agent selected. | 1. Complete Unit 1 fully; leave Unit 2 entirely blank; leave 'Agent (owner of the unit)' unselected.<br>2. Click 'Confirm & notify'.<br>3. Observe validation.<br>4. Select the agent, remove or complete Unit 2, resubmit. | Step 2: submission blocked; the agent field shows an inline 'select the agent' error AND all missing required fields (Serial number, Unit type, Fault description) are flagged inline on every incomplete unit simultaneously. Step 4: shipment saves; each unit gets its own job. | Pass | Verified 7 Jul 2026 during D-08 retest: blank unit flagged on all three required fields inline; after completing, the walk-in intake saved successfully. | Lucia | Added after D-08. Fix in components/feature/IntakeForm.tsx. The 'Add another contact' form (TC-INT-29) was proactively given the same fix in components/feature/CustomersManager.tsx during this session. |
| TC-INT-34 | Intake & Customer Mgmt | Edit contact details and reassign primary via Contacts table | FR-INT-01 / D-11 follow-up | Functional | Medium | A corporation with a primary and at least one non-primary contact. Logged in as Admin or Front Desk. | 1. Open Customers -> Contacts and click Edit on a non-primary contact.<br>2. Change the person name and phone (E.164) and Save.<br>3. Click Edit on the same contact, tick 'Primary contact', Save.<br>4. Click Edit on the current primary and inspect the Primary checkbox.<br>5. Attempt invalid input (blank name, malformed phone) and Save. | Edits persist (name/phone; login email not editable). Promoting a contact demotes the previous primary in the same transaction - exactly one Primary badge per corporation at all times (DB backstop uq_mcc_primary). The current primary's checkbox is disabled with a hint to promote another contact instead (a corporation can never be left with no primary). Invalid input is flagged inline, all errors at once; no server call is made. | Pass | PASS (all five steps). (1) Edited Thandi Test's phone to +27821110001 — persisted. (2) Ticked Primary on Thandi -> she gained the Primary badge and Johan was demoted (demote-in-transaction). (3) Reverse promotion verified (Johan re-promoted, later Thandi promoted again for TC-INT-31 after Johan's login issue surfaced). (4) Current primary's checkbox disabled with hint. (5) Negative: blank name + phone 'abc' -> both errors inline at once, nothing saved. Login email correctly not editable. | Lucia | Enhancement built 22 Jul 2026 (Lucia request during TC-INT-28): lib/people/people.ts (updateContact), app/(staff)/customers/actions.ts (updateContactAction), components/feature/CustomersManager.tsx (inline row edit). |
| TC-INT-35 | Intake & Customer Mgmt | Remove (soft-delete) a contact; primary contact cannot be removed | FR-INT-01 / D-11 follow-up | Functional | Medium | A corporation with a primary and at least one non-primary contact. Logged in as Admin or Front Desk. | 1. Open Customers -> Contacts and click Remove on a non-primary contact.<br>2. Confirm via the inline 'Confirm remove' prompt.<br>3. Inspect the Contacts list and the corporation's contact count.<br>4. Inspect the Remove button on the current primary contact.<br>5. (DB) select deleted_at for the removed contact. | Inline two-step confirmation shown (no browser dialog). On confirm, the contact disappears from the Contacts list and the corporation's contact count decrements. The removal is a soft delete: deleted_at is set, the row remains in the DB for audit/recovery, and the contact's portal login no longer resolves. The primary contact's Remove button is disabled with a hint to mark another contact as primary first (one-primary invariant preserved). | Pass | PASS. Front Desk (Pieter), Customers -> Contacts. Remove on non-primary contact (Marius Test, ACME Industries) showed the inline two-step 'Remove this contact?' confirmation (no browser dialog); Confirm remove soft-deleted the contact — removed from the Contacts list, ACME contact count decremented. Primary contacts' Remove buttons verified disabled across ALL corporations (Johan/ACME, John Doe/AgrTest, John/Agri-IT, Pat/Retest Customer One) — one-primary invariant preserved; server-side backstop in deleteContact. Soft delete: deleted_at set, row retained in DB. | Lucia | Enhancement built 22 Jul 2026 (Lucia request, with TC-INT-34): deleteContact in lib/people/people.ts, deleteContactAction, inline confirm UI in CustomersManager.tsx. Supabase auth-user disablement deferred to D-09. |
| TC-WF-01 | Workflow & State Machine | Job moves through all 10 defined stages in order | FR-WF-01, Sec 10 | Functional | High | A received job. | 1. Advance stage sequentially: Received -> Diagnosed -> Quoted -> Approved -> In Progress -> QC -> Boxed -> Ready for Collection -> Collected -> Closed. | Each forward transition is accepted in order and the stage timeline reflects the current stage. | Not Run |  |  |  |
| TC-WF-02 | Workflow & State Machine | Each transition records timestamp, actor, prev/new stage, notes | FR-WF-02 | Functional | High | A job at any stage. | 1. Advance the stage and add an optional note.<br>2. Open Stage history / History tab. | History entry shows timestamp, the staff user who triggered it, previous and new stage, and the note. | Not Run |  |  |  |
| TC-WF-03 | Workflow & State Machine | Assign and reassign technician (logged) | FR-WF-03 | Functional | Medium | A diagnosed job. | 1. Assign technician Naledi.<br>2. Reassign to another technician.<br>3. Open history. | Both assignment and reassignment are saved and logged with actor and timestamp. | Not Run |  |  |  |
| TC-WF-04 | Workflow & State Machine | Diagnosis note internal-only; customer-facing note visible in portal | FR-WF-04 | RBAC | High | A job with both a technician diagnosis note and a customer-facing note. | 1. As staff, view both notes on Job detail.<br>2. As the agent/main customer, view the job in the portal. | Internal diagnosis note is hidden from the portal; only the customer-facing note appears externally. | Not Run |  |  |  |
| TC-WF-05 | Workflow & State Machine | Attach multiple photos at any stage with caption and visibility flag | FR-WF-05 | Functional | Medium | A job in progress. | 1. Upload photos with captions.<br>2. Set one to internal-only and one to customer-facing.<br>3. View as staff and as customer. | Photos save with captions; internal-only photos are hidden in the portal while customer-facing ones are visible. | Not Run |  |  |  |
| TC-WF-06 | Workflow & State Machine | Not Repairable requires a structured reason from the defined set | FR-WF-06 | Functional | High | A diagnosed job. | 1. Move the job to Not Repairable.<br>2. Inspect the reason field. | A structured reason must be selected from: lightning damage, secondary lightning damage, water damage, component failure, power supply failure, chip-to-probe communication failure, other (free text). Save blocked without a reason. | Not Run |  |  |  |
| TC-WF-07 | Workflow & State Machine | Boxed stage auto-triggers ready-for-collection notification to agent | FR-WF-07, FR-COM-01 | Notification | High | A job reaching the Boxed stage; agent has consent. | 1. Mark the job Boxed. | A 'ready for collection' notification is sent automatically to the agent (WhatsApp + email). | Not Run |  |  |  |
| TC-WF-08 | Workflow & State Machine | Quoted to Approved is the only customer-triggerable transition | Sec 10 | State Machine | High | A job in Quoted stage. | 1. As the main customer, approve the quote (portal/WhatsApp/email). | Job auto-transitions Quoted -> Approved; no other transition can be initiated by a customer. | Not Run |  |  |  |
| TC-WF-09 | Workflow & State Machine | Not Repairable is a terminal state with assessment fee | Sec 10, FR-QUO-09 | State Machine | High | A diagnosed job set to Not Repairable with a reason. | 1. Complete the Not Repairable transition.<br>2. Attempt any further forward transition. | State is terminal (no further advance), and an assessment-fee proforma invoice is generated automatically. | Not Run |  |  |  |
| TC-WF-10 | Workflow & State Machine | Rejected is terminal with NO assessment fee | Sec 10, FR-QUO-09 | State Machine | High | A job in Quoted stage. | 1. As main customer, reject the quote. | Job moves to Rejected (terminal); NO assessment fee is generated. | Not Run |  |  |  |
| TC-WF-11 | Workflow & State Machine | Cancelled is admin-only and incurs no fee | Sec 10 | State Machine | High | A received job before any work began. | 1. As Admin, cancel the job.<br>2. Attempt cancel as a non-admin. | Admin can cancel (terminal, no fee); non-admin cannot cancel. | Not Run |  |  |  |
| TC-WF-12 | Workflow & State Machine | Invalid stage skips are prevented | FR-WF-01, Sec 10 | Negative | High | A job in Received stage. | 1. Attempt to jump directly from Received to Boxed. | Disallowed transition is rejected; only valid next-state transitions are permitted. | Not Run |  |  |  |
| TC-WF-13 | Workflow & State Machine | Stage timeline highlights done/current/future stages | Wireframe Job detail | UI | Low | A job in Quoted stage. | 1. Open Job detail and inspect the stage timeline pips. | Completed stages appear 'done', the current stage is highlighted 'now', and later stages are 'future'. | Not Run |  |  |  |
| TC-WF-14 | Workflow & State Machine | Print jobcard from Job detail | FR-INT-10, FR-ADM-07 #6, Wireframe | Functional | Medium | Open Job detail. | 1. Click 'Print jobcard'. | A single-page A4 jobcard renders with the Gentick header sourced from company-details settings. | Not Run |  |  |  |
| TC-COM-01 | Communication & Notifications | Notifications fire at the four key milestones | FR-COM-01 | Notification | High | A job with consenting parties. | 1. Trigger item received, quote ready, ready for collection, and not repairable events. | A communication is sent at each milestone to the relevant party per the routing matrix. | Not Run |  |  |  |
| TC-COM-02 | Communication & Notifications | WhatsApp primary when consent given; email always sent | FR-COM-02 | Notification | High | Party with WhatsApp consent. | 1. Trigger an 'item received' event. | WhatsApp message is sent (primary) and email is also sent as fallback/record. | Not Run |  |  |  |
| TC-COM-03 | Communication & Notifications | Email-only when WhatsApp consent is absent | FR-COM-02, FR-INT-04 | Notification | High | Party who has opted out of WhatsApp. | 1. Trigger a milestone event for that party. | Only email is sent; no WhatsApp message goes to the opted-out party. | Not Run |  |  |  |
| TC-COM-04 | Communication & Notifications | Quote approval via WhatsApp 'YES' | FR-COM-03 | Functional | High | Main customer received a quote-ready WhatsApp. | 1. Reply 'YES' on WhatsApp. | Reply is classified as approval; job transitions to Approved and a confirmation is logged. | Not Run |  |  |  |
| TC-COM-05 | Communication & Notifications | Quote approval via email reply / portal click | FR-COM-03, FR-QUO-03 | Functional | High | Main customer has a quote awaiting approval. | 1. Click 'Approve' in the portal (or reply to the approval email). | Quote is approved, job transitions to Approved, and the action is logged with channel and actor. | Not Run |  |  |  |
| TC-COM-06 | Communication & Notifications | Only the main customer's approval is accepted | FR-COM-03, FR-QUO-02 | Negative | High | Agent attempts to approve via WhatsApp YES from agent number. | 1. Send 'YES' from the agent's WhatsApp number for a quoted job. | Approval is NOT applied from the agent; the message is treated as non-authoritative / escalated, and the job stays Quoted. | Not Run |  |  |  |
| TC-COM-07 | Communication & Notifications | Every inbound and outbound message is logged against the job | FR-COM-04 | Functional | High | A job with message activity. | 1. Open the Communications log tab. | Each message shows channel, timestamp, sender/recipient, delivery status, and content. | Not Run |  |  |  |
| TC-COM-08 | Communication & Notifications | Unclassified inbound replies escalate to Michael | FR-ADM-09, Sec 7.1, Wireframe | Notification | Medium | Inbound WhatsApp reply that is not YES/NO. | 1. Send a free-text question via WhatsApp in reply to a notification. | Message is routed to the WhatsApp escalation contact (default Michael, 082 374 9842) and logged against the job. | Not Run |  |  |  |
| TC-COM-09 | Communication & Notifications | Message templates are configurable and versioned | FR-COM-05, FR-ADM-05 | Functional | Medium | Admin editing templates. | 1. Edit an outbound template and save a new version.<br>2. Send a message using the new version.<br>3. View a historical message sent under the prior version. | New messages use the current template; historical messages retain the exact wording version sent at the time. | Not Run |  |  |  |
| TC-COM-10 | Communication & Notifications | Notification routing matrix - quote ready | Sec 8 | Notification | High | A quote-ready event. | 1. Trigger 'quote ready'. | Main customer is notified (email primary + WhatsApp for approval); the agent is NOT notified until approved. | Not Run |  |  |  |
| TC-COM-11 | Communication & Notifications | Notification routing matrix - quote approved | Sec 8, FR-QUO-02 | Notification | High | A quote-approved event. | 1. Approve the quote. | Both main customer and agent are notified (WhatsApp + email) and the agent now sees the full price. | Not Run |  |  |  |
| TC-COM-12 | Communication & Notifications | Notification routing matrix - item not repairable | Sec 8, FR-WF-06, FR-QUO-09 | Notification | High | A unit marked Not Repairable. | 1. Complete the Not Repairable transition. | Both main customer and agent receive WhatsApp + email with the reason report and assessment-fee invoice. | Not Run |  |  |  |
| TC-COM-13 | Communication & Notifications | Notification routing matrix - agent invited | Sec 8, FR-INT-11 | Notification | Medium | An agent invitation is issued. | 1. Main customer invites a new agent. | Only the agent receives a welcome/invite email; the main customer is not separately notified. | Not Run |  |  |  |
| TC-COM-14 | Communication & Notifications | Notification routing matrix - job closed | Sec 8, FR-QUO-04 | Notification | Medium | A job moves to Closed. | 1. Close the job. | Main customer receives an email with the proforma invoice; the agent is not notified at close. | Not Run |  |  |  |
| TC-COM-15 | Communication & Notifications | Item received notifies both parties on any origin | Sec 8, FR-COM-01 | Notification | Medium | A unit received from any origin path. | 1. Mark the unit received. | Both main customer and agent receive WhatsApp + email 'item received'. | Not Run |  |  |  |
| TC-QUO-01 | Quoting & Billing | Quote contains required line items and totals | FR-QUO-01 | Functional | High | A diagnosed job. | 1. Create a quote with labour estimate, parts line items, notes, validity period, and currency.<br>2. Save. | Quote stores labour, parts line items, notes, validity, currency code, and a VAT-inclusive total. | Not Run |  |  |  |
| TC-QUO-02 | Quoting & Billing | Quote total calculation incl. VAT is correct | FR-QUO-01, FR-QUO-08 | Functional | High | A ZAR quote with subtotal 3,090.00. | 1. Add lines totalling 3,090.00 ZAR.<br>2. Apply default 15% VAT.<br>3. Inspect totals. | VAT = 463.50 and Total = 3,553.50 ZAR (matches wireframe Q-GT-ACME-0184-1). | Not Run |  |  |  |
| TC-QUO-03 | Quoting & Billing | Quote is issued to the main customer regardless of originator | FR-QUO-02 | Functional | High | An agent-initiated job now quoted. | 1. Issue the quote. | Quote email goes to the main customer; the agent is informed but not asked to approve. | Not Run |  |  |  |
| TC-QUO-04 | Quoting & Billing | Agent sees full quote details (incl. R-value) after approval | FR-QUO-02, Sec 8 | RBAC | High | A quote just approved by the main customer. | 1. Log in as the agent and open the job. | Agent now sees the full quote including the R-value/price. | Not Run |  |  |  |
| TC-QUO-05 | Quoting & Billing | Agent does NOT see price before approval | FR-QUO-02, Sec 8 | RBAC | High | A quoted job not yet approved. | 1. Log in as the agent and open the job. | Agent sees the job is Quoted/awaiting approval but the price is not yet exposed as approved detail (status shows 'Waiting for [main customer] to approve'). | Not Run |  |  |  |
| TC-QUO-06 | Quoting & Billing | Approval auto-transitions job to Approved | FR-QUO-03 | Functional | High | Quote awaiting approval. | 1. Main customer approves. | Job stage becomes Approved automatically. | Not Run |  |  |  |
| TC-QUO-07 | Quoting & Billing | Proforma invoice PDF generated on job close | FR-QUO-04 | Functional | High | A job ready to close. | 1. Close the job.<br>2. Download the proforma invoice. | A downloadable PDF proforma invoice is generated. | Not Run |  |  |  |
| TC-QUO-08 | Quoting & Billing | Sage Pastel Online CSV/XML export produced | FR-QUO-05, Sec 7.2 | Functional | High | Closed jobs with invoices. | 1. Generate the Sage export batch.<br>2. Open the exported file. | A CSV/XML file compatible with Sage Pastel Online's import format is produced. | Not Run |  |  |  |
| TC-QUO-09 | Quoting & Billing | Invoiced / Not invoiced status reflects export only | FR-QUO-06 | Functional | Medium | A closed job before and after export. | 1. Before export, check status.<br>2. Generate export.<br>3. Re-check status. | Status shows 'Not invoiced' until an export is generated, then 'Invoiced'; settlement/payment status is not mirrored from Sage. | Not Run |  |  |  |
| TC-QUO-10 | Quoting & Billing | Multi-currency quote in USD stays native end-to-end | FR-QUO-07 | Functional | High | A job for a foreign customer. | 1. Create a quote in USD.<br>2. Generate invoice and reports. | Quote and invoice remain in USD throughout; no ZAR conversion is performed in MVP. | Not Run |  |  |  |
| TC-QUO-11 | Quoting & Billing | ZAR quote defaults to 15% VAT | FR-QUO-08 | Functional | High | New ZAR quote. | 1. Create a quote with currency ZAR. | VAT rate defaults to 15%. | Not Run |  |  |  |
| TC-QUO-12 | Quoting & Billing | Non-ZAR quote defaults to 0% VAT (export zero-rating) | FR-QUO-08, Sec 13 | Functional | High | New USD quote. | 1. Create a quote with currency USD. | VAT rate defaults to 0%. | Not Run |  |  |  |
| TC-QUO-13 | Quoting & Billing | VAT override requires a captured reason | FR-QUO-08 | Functional | Medium | A quote with default VAT. | 1. Override the VAT rate.<br>2. Attempt to save without a reason.<br>3. Then add a reason and save. | Override is blocked until a reason is captured; the reason is stored with the quote. | Not Run |  |  |  |
| TC-QUO-14 | Quoting & Billing | Not Repairable auto-generates assessment-fee proforma (default R350) | FR-QUO-09 | Functional | High | Assessment fee setting = R350. | 1. Mark a unit Not Repairable with a reason. | An assessment-fee proforma invoice for R350 is generated automatically. | Not Run |  |  |  |
| TC-QUO-15 | Quoting & Billing | Rejected quote does NOT generate an assessment fee | FR-QUO-09, Sec 10 | Negative | High | A quoted job. | 1. Main customer rejects the quote. | No assessment fee or invoice is generated; job is Rejected. | Not Run |  |  |  |
| TC-QUO-16 | Quoting & Billing | Assessment fee configurable per job and waivable by admin with reason | FR-QUO-09 | Functional | Medium | A Not Repairable job with an assessment fee. | 1. As admin, change the per-job fee, then waive it with a reason. | Per-job fee can be changed; waiver requires and stores a reason; audit reflects the change. | Not Run |  |  |  |
| TC-QUO-17 | Quoting & Billing | Company legal details appear on quotes and invoices | FR-QUO-10, FR-ADM-08 | Functional | High | Company-details setting populated. | 1. Generate a quote PDF and a proforma invoice PDF. | Both show Gentick Electronics, 169 Haupt Street, Sidwell, Port Elizabeth, 6001, and VAT 4840244414. | Not Run |  |  |  |
| TC-QUO-18 | Quoting & Billing | Expired quote (past validity) auto-expires | Sec 14 #4 | Functional | Medium | Quote validity = 14 days; a quote issued 15 days ago. | 1. Open the quote after the validity window. | Quote is marked expired and cannot be approved without reissue. | Not Run |  |  |  |
| TC-QUO-19 | Quoting & Billing | Per-job parts consumption feeds the quote | FR-ADM-03 | Functional | Medium | A job with logged parts from the catalogue. | 1. Log a part (e.g., PSU-4G-A) against the job.<br>2. Generate the quote. | The logged part appears as a quote line item with the catalogue cost and currency. | Not Run |  |  |  |
| TC-REP-01 | Reporting & Dashboard | Active jobs dashboard shows per-stage counts and aging | FR-REP-01, Wireframe Admin | Functional | High | Jobs distributed across stages. | 1. Open the Admin dashboard. | KPI tiles show active jobs, aged >14 days, avg turnaround, revenue; the stages strip shows counts per stage; aged jobs are highlighted. | Not Run |  |  |  |
| TC-REP-02 | Reporting & Dashboard | Technician workload report | FR-REP-02 | Functional | Medium | Multiple technicians with open jobs. | 1. Open the technician workload report. | Open jobs per technician with average time-in-stage are shown. | Not Run |  |  |  |
| TC-REP-03 | Reporting & Dashboard | Turnaround time report sliceable by month and repair type | FR-REP-03 | Functional | Medium | Closed jobs across months and repair types. | 1. Open the turnaround report.<br>2. Filter by month and by repair type. | Average days Received->Collected are shown and update correctly per slice. | Not Run |  |  |  |
| TC-REP-04 | Reporting & Dashboard | Revenue report shows currency alongside values (no ZAR roll-up) | FR-REP-04, FR-QUO-07, Sec 14 #5 | Functional | Medium | Invoiced jobs in ZAR and USD. | 1. Open the revenue report. | Values are shown by period/repair type/main customer with their currency; ZAR and USD are not combined into a single total. | Not Run |  |  |  |
| TC-REP-05 | Reporting & Dashboard | Intake source report splits by origin | FR-REP-08 | Functional | Medium | Jobs from all three origins. | 1. Open the intake source report. | Report shows the split of jobs by main customer / agent / front desk origin. | Not Run |  |  |  |
| TC-REP-06 | Reporting & Dashboard | All reports exportable to CSV | FR-REP-07 | Functional | Medium | Any report open. | 1. Click Export CSV. | A CSV download is produced with the report's data. | Not Run |  |  |  |
| TC-REP-07 | Reporting & Dashboard | Common faults summary on dashboard | FR-REP-06 (Phase 2), Wireframe Admin | UI | Low | Faults classified across jobs this month. | 1. Open the Admin dashboard 'Common faults this month' card. | Fault classifications are listed with counts; 'Open full report' link is present (full report is Phase 2). | Not Run |  |  |  |
| TC-REP-08 | Reporting & Dashboard | Today's priorities surfaces approvals, boxed units, and Sage batch | Wireframe Admin | UI | Low | Pending approvals, boxed units, and a ready Sage batch exist. | 1. Open the Admin dashboard. | 'Today's priorities' lists quotes awaiting approval, boxed units awaiting collection notification, and the Sage export ready for download. | Not Run |  |  |  |
| TC-REP-09 | Reporting & Dashboard | Jobs table customer filter works | Wireframe Admin | UI | Medium | Jobs for multiple customers. | 1. On the dashboard jobs table, filter by 'Acme Agritech'. | Only Acme jobs are listed; clearing the filter restores all. | Not Run |  |  |  |
| TC-ADM-01 | Admin & Settings | Staff user management across the three roles | FR-ADM-01, Sec 4.1 | Functional | High | Logged in as Admin. | 1. Create staff users as Admin, Technician, and Front Desk.<br>2. Save. | Users are created with the correct role and corresponding access. | Not Run |  |  |  |
| TC-ADM-02 | Admin & Settings | Parts catalogue management | FR-ADM-02 | Functional | Medium | Admin in parts catalogue. | 1. Add a part with code, description, cost, and currency.<br>2. Save. | Part is added and becomes selectable when technicians log usage. | Not Run |  |  |  |
| TC-ADM-03 | Admin & Settings | Repair type taxonomy management | FR-ADM-04 | Functional | Low | Admin in settings. | 1. Add a repair category.<br>2. Use it on a job; check it appears in reports. | New repair type is available for jobs and appears as a slice in reporting. | Not Run |  |  |  |
| TC-ADM-04 | Admin & Settings | Audit log viewer shows consent, logins, role and data changes | FR-ADM-06 | Functional | Medium | Activity has occurred (consent change, login, role change, data edit). | 1. Open the audit log viewer. | All four event categories are visible with actor and timestamp. | Not Run |  |  |  |
| TC-ADM-05 | Admin & Settings | System settings editable by admin only | FR-ADM-07 | RBAC | High | Logged in as Admin, then as non-admin. | 1. As admin, edit assessment fee, currencies, default VAT, photo limits, quote validity.<br>2. As non-admin, attempt to open settings. | Admin can edit all listed settings; non-admin is denied access. | Not Run |  |  |  |
| TC-ADM-06 | Admin & Settings | Company details setting is versioned for historical documents | FR-ADM-08 | Functional | High | A historical invoice issued under prior company details. | 1. Change the company VAT/address setting.<br>2. Re-open a historical invoice.<br>3. Generate a new invoice. | Historical document retains the company details in effect at issue; new document reflects updated details. | Not Run |  |  |  |
| TC-ADM-07 | Admin & Settings | Operational contacts setting (escalation + champion) | FR-ADM-09, Sec 14 #9-10 | Functional | Medium | Admin in operational contacts. | 1. Reassign the WhatsApp escalation contact and the internal champion to a new person.<br>2. Save.<br>3. Trigger an unclassified inbound. | Defaults are Michael; after reassignment the new contact receives escalations - no code change needed. | Not Run |  |  |  |
| TC-ADM-08 | Admin & Settings | Photo upload limit enforced (20 per job, 5MB each) | FR-ADM-07, Sec 14 #3 | Negative | Medium | A job with 20 photos already attached. | 1. Attempt to upload a 21st photo.<br>2. Attempt to upload a 6MB image. | 21st photo and oversized file are rejected with a clear message. | Not Run |  |  |  |
| TC-ADM-09 | Admin & Settings | Assessment fee default change applies to new Not Repairable jobs | FR-ADM-07, FR-QUO-09 | Functional | Medium | Admin changes default assessment fee from R350 to R400. | 1. Save the new default.<br>2. Mark a new unit Not Repairable. | New assessment-fee invoice uses R400; previously generated invoices are unchanged. | Not Run |  |  |  |
| TC-ADM-10 | Admin & Settings | Admin can reset any user's password from the Security page; non-admins cannot | FR-ADM-01 / NFR-04 / enhancement #11 | Functional | High | Admin logged in. At least one staff, one customer contact and one agent with logins exist. | 1. As Admin open /account/security and locate 'Reset a user's password (admin)'.<br>2. Select a user and click 'Send reset email'; check Mailpit (:54324) for the reset email.<br>3. Select a user, enter a new temp password (>= 8 chars) and click 'Set password now'; verify the user can sign in with it (incognito).<br>4. Try 'Set password now' with a 7-character password.<br>5. Open the Audit log as Admin and look for the password events.<br>6. Log in as Technician or Front Desk and open /account/security. | The admin card lists all login-bearing users grouped by Staff / Customer contacts / Agents. Reset email arrives in Mailpit and links to the app's reset page on ONE origin (NEXT_PUBLIC_APP_URL). Direct set succeeds and the user can sign in with the temp password. Short password rejected inline (min 8). Both actions write audit events (password_set_by_admin / password_reset_email_by_admin). Non-admin staff see only the MFA card — no reset section — and the server actions independently reject non-admins. | Not Run |  |  | Enhancement #11 built 22 Jul 2026 (Lucia request; D-12 lockout was the driving case). Files: AdminPasswordReset.tsx, account/security/actions.ts (+page), lib/admin/supabase-admin.ts, listLoginUsers in people.ts. NOTE: if "Set password now" then sign-in fails, that reproduces open defect D-12 — record evidence there. |
| TC-UI-01 | UI / Wireframe | Technician 'Today's focus' shows prioritised cards | Wireframe Technician | UI | Low | Logged in as Technician with an aged job and parts arrivals. | 1. Open 'My queue'. | Focus cards show e.g. aged job to escalate, parts-arrived resume, and ready-for-QC, each with a primary action. | Not Run |  |  |  |
| TC-UI-02 | UI / Wireframe | Technician full queue sorting | Wireframe Technician | UI | Low | Technician with 12 jobs. | 1. Sort by oldest first, by stage, and by customer. | Queue reorders correctly for each sort option. | Not Run |  |  |  |
| TC-UI-03 | UI / Wireframe | Technician quick-log note/part/photo against active job | FR-WF-04, FR-ADM-03, Wireframe | Functional | Medium | Technician working on a job. | 1. In Quick log, select the working job.<br>2. Add a note, a part, and a photo.<br>3. Save log entry. | Note, part, and photo are recorded against the selected job. | Not Run |  |  |  |
| TC-UI-04 | UI / Wireframe | Agent portal KPIs reflect agent's own jobs | Wireframe Agent | UI | Low | Agent with 3 active, 1 awaiting approval, 1 ready for collection. | 1. Open the agent portal. | KPI tiles match the agent's job counts and statuses. | Not Run |  |  |  |
| TC-UI-05 | UI / Wireframe | Agent 'My units' pre-fills a new repair request | FR-INT-09, Wireframe Agent | Functional | Medium | Agent has prior units. | 1. In 'My units', pick SN-21A847 to start a new request. | New repair form is pre-filled with that unit's type/serial and prior context. | Not Run |  |  |  |
| TC-UI-06 | UI / Wireframe | Agent active/closed repairs toggle | FR-INT-10, Wireframe Agent | UI | Low | Agent has active and closed jobs. | 1. Toggle between 'Active only' and 'Include closed'. | List updates to show active-only vs all jobs accordingly. | Not Run |  |  |  |
| TC-UI-07 | UI / Wireframe | Main customer 'Action required' approve/reject controls | FR-QUO-02, FR-COM-03, Wireframe | Functional | High | Main customer with 3 quotes awaiting approval. | 1. Open the main customer portal.<br>2. Use 'View quote PDF', 'Approve', and 'Reject' on a quote. | PDF opens; Approve transitions the job to Approved; Reject moves it to Rejected (no fee). | Not Run |  |  |  |
| TC-UI-08 | UI / Wireframe | Main customer spend-this-quarter and CSV export | FR-REP-04, FR-REP-07, Wireframe | Functional | Low | Main customer with invoiced jobs. | 1. Open the portal.<br>2. Click 'Export spend report (CSV)'. | Spend summary shown; CSV export downloads the spend data. | Not Run |  |  |  |
| TC-UI-09 | UI / Wireframe | Main customer all-active-jobs agent filter | Wireframe Main customer | UI | Low | Jobs across several agents. | 1. Filter the all-active-jobs table by a single agent. | Only that agent's jobs are listed. | Not Run |  |  |  |
| TC-UI-10 | UI / Wireframe | Branding/header renders Gentick identity | Wireframe, FR-ADM-08 | UI | Low | Any portal page. | 1. Load a page and inspect the header/sidebar. | Gentick Electronics branding and 'Experts in Telemetry Systems' tagline render correctly. | Not Run |  |  |  |
| TC-NFR-01 | Non-Functional | Staff page loads under 2s on 4G; portal under 3s | NFR-01 | Performance | Medium | Throttled 4G connection. | 1. Load staff dashboard and customer portal pages; measure load time. | Staff pages load < 2s; customer portal < 3s. | Not Run |  |  |  |
| TC-NFR-02 | Non-Functional | Job list handles 1,000 jobs without pagination performance issues | NFR-01 | Performance | Medium | 1,000 jobs seeded. | 1. Open the active jobs list and scroll/filter. | List remains responsive with 1,000 jobs. | Not Run |  |  |  |
| TC-NFR-03 | Non-Functional | All traffic served over HTTPS | NFR-04 | Security | High | Any page. | 1. Attempt to access over HTTP.<br>2. Inspect TLS on HTTPS. | HTTP redirects to HTTPS; valid TLS in use throughout. | Not Run |  |  |  |
| TC-NFR-04 | Non-Functional | Secrets are not exposed in code or workflow JSON | NFR-04, Sec 7.1 | Security | High | Access to repo and n8n workflow exports. | 1. Inspect code and exported n8n workflow JSON for credentials. | No secrets in code or workflow JSON; credentials live in n8n's encrypted store. | Not Run |  |  |  |
| TC-NFR-05 | Non-Functional | POPIA data residency - no cross-border transfer | NFR-05, Sec 13 | Compliance | High | Production hosting. | 1. Verify DB and file storage region.<br>2. Verify backup region. | Data hosted in Hetzner Cape Town (SA); off-site backup in a second SA region; no cross-border transfer. | Not Run |  |  |  |
| TC-NFR-06 | Non-Functional | Subject access request answerable within 30 days | NFR-05 | Compliance | Medium | A data subject request. | 1. Export all personal data held for a given contact/agent. | A complete personal-data export can be produced to satisfy a SAR within 30 days. | Not Run |  |  |  |
| TC-NFR-07 | Non-Functional | Customer portal is mobile-responsive | NFR-06, Sec 2.1 | UI | High | Agent on a phone-width viewport (<=1100px). | 1. Open the agent portal and book a repair on a narrow viewport. | Layout reflows to a single column and intake is fully usable on mobile. | Not Run |  |  |  |
| TC-NFR-08 | Non-Functional | Supported browsers render correctly | NFR-06 | Compatibility | Medium | Latest 2 versions of Chrome, Edge, Firefox, Safari. | 1. Open key screens in each supported browser. | Screens render and function correctly with no browser-specific breakage. | Not Run |  |  |  |
| TC-NFR-09 | Non-Functional | Customer portal meets WCAG 2.1 AA basics | NFR-07 | Accessibility | Medium | Customer portal pages. | 1. Run an accessibility audit (contrast, labels, keyboard nav, focus order). | Portal passes WCAG 2.1 AA checks for contrast, form labels, and keyboard navigation. | Not Run |  |  |  |
| TC-NFR-10 | Non-Functional | Daily backups exist and are restorable | NFR-03, Sec 14 #11 | Reliability | High | Backup schedule configured. | 1. Verify a recent daily DB + file backup.<br>2. Perform a test restore. | Daily backups exist (30-day retention; monthly snapshots 12 months) and a restore succeeds. | Not Run |  |  |  |
| TC-NFR-11 | Non-Functional | Automation failures raise error alerts to admin | NFR-10, Sec 7.1 | Reliability | Medium | An n8n workflow configured to fail (e.g., bad WhatsApp send). | 1. Force a workflow failure. | The n8n error workflow captures it and an error alert is sent to admin email. | Not Run |  |  |  |
| TC-INTG-01 | Integrations | RMS emits event to n8n webhook on status change | Sec 7.1 | Integration | High | n8n webhook reachable. | 1. Change a job status to trigger job.status_changed. | RMS posts the event to the n8n webhook with the correct payload. | Not Run |  |  |  |
| TC-INTG-02 | Integrations | n8n logs outbound WhatsApp back to the RMS | Sec 7.1, FR-COM-04 | Integration | High | A WhatsApp send completes via Meta Cloud API. | 1. Trigger a WhatsApp notification end-to-end. | n8n calls back the RMS API and the outbound message is logged against the job. | Not Run |  |  |  |
| TC-INTG-03 | Integrations | Inbound Meta webhook classified and routed | Sec 7.1 | Integration | Medium | Inbound WhatsApp message arrives. | 1. Send a YES, then a free-text reply. | YES routes to quote approval; free-text routes to staff/escalation; both are logged. | Not Run |  |  |  |
| TC-INTG-04 | Integrations | Sage export file matches Sage Pastel Online import format | FR-QUO-05, Sec 7.2 | Integration | High | An export batch generated. | 1. Import the exported file into Sage Pastel Online (or validate against its schema). | File imports without format errors; customer and invoice fields map correctly. | Not Run |  |  |  |
| TC-INTG-05 | Integrations | Transactional emails authenticated (SPF/DKIM/DMARC) | Sec 7.3 | Integration | Medium | Email sending configured on gentick.co.za. | 1. Send a jobcard/quote email and inspect headers. | Email passes SPF, DKIM, and DMARC checks; from-address is repairs@gentick.co.za. | Not Run |  |  |  |
| TC-REP-10 | Reporting & Dashboard | Customer / unit repair history view | FR-REP-05, Wireframe Job detail | Functional | Medium | A unit (e.g., SN-21A847) with multiple prior jobs. | 1. Open Job detail for the unit.<br>2. Open the History / 'This unit's history' section.<br>3. Also open the per-customer history view. | All historical jobs for the unit/customer are listed with quick view to fault patterns (e.g., '3 times', prior lightning/component faults). | Not Run |  |  |  |
| TC-NFR-12 | Non-Functional | Availability target and maintenance notice | NFR-02 | Reliability | Medium | Uptime monitoring (e.g., UptimeRobot) configured. | 1. Review uptime monitoring over a measured month.<br>2. Verify planned maintenance windows are announced 48h in advance. | Monthly uptime meets the 99% target; maintenance windows are announced at least 48 hours ahead. | Not Run |  |  |  |
| TC-NFR-13 | Non-Functional | Scalability headroom for 10x growth | NFR-08 | Performance | Low | Baseline of hundreds of jobs/month. | 1. Load-test the system with ~10x the expected monthly job volume.<br>2. Measure response times and resource use. | System handles 10x volume without architectural change and within acceptable performance. | Not Run |  |  |  |
| TC-NFR-14 | Non-Functional | Maintainability standards in place | NFR-09 | Maintainability | Low | Access to the codebase and migration history. | 1. Confirm TypeScript is used throughout.<br>2. Confirm README documents code conventions.<br>3. Confirm DB schema is versioned via migrations. | Codebase is TypeScript, conventions are documented, and schema changes are tracked as migrations. | Not Run |  |  |  |
| TC-AUTH-11 | Authentication & RBAC | Front Desk can add an agent | RBAC invite_agent / §4.1 | Functional | Medium | Front Desk account exists; at least one main customer exists. | 1. Log in as Front Desk.<br>2. Open Agents.<br>3. Select a main customer, complete agent details + a temporary password.<br>4. Submit. | Agent is created and appears in the Agents list. The new agent can log in to the agent portal. Front Desk reaches Agents from its own sidebar. | Pass | Logged in as Front Desk (frontdesk@gentick.co.za, 'Pieter'); landed on New intake with sidebar New intake / Customers / Agents. Opened Agents from the sidebar and completed the 'Add an agent' form (Main customer = ACME Industries; name 'FD Test Agent 0624'; email fd-test-agent-0624@example.com; address + city; temporary password) and submitted. Agent created and appears at the top of the Agents list (now 6) as Active under ACME Industries. Signed out and logged in as the new agent (fd-test-agent-0624@example.com): lands on the External Agent portal ('My repairs - FD Test Agent 0624'). Front Desk reached Agents from its own sidebar. | Lucia | PASS. Note: 'Address line 1' is a required field on the agent form - the first submit was rejected with 'addressLine1: required' and succeeded once provided. Test agent 'FD Test Agent 0624' left in place (Active, ACME). |
| TC-AUTH-12 | Authentication & RBAC | Main customer primary contact can add an agent for own corporation | RBAC invite_agent / §4.1 | Functional | High | A primary main-customer contact account exists (e.g. johan@acme.co.za, isPrimary = true). | 1. Log in as the primary contact.<br>2. On the portal 'Your agents' card, click '+ Add an agent'.<br>3. Complete the agent details + a temporary password.<br>4. Submit. | Agent is created under the contact's OWN corporation only and appears under 'Your agents'; the new agent can log in. The mainCustomerId is taken from the session, so the contact cannot create agents for any other corporation (enforced by invite_agent). | Pass | Logged in as a primary main-customer contact ('TC12 Primary Contact', tc12-primary@example.com, isPrimary=true, corporation Test Co Z072693). The main-customer portal shows a 'Your agents' card with a '+ Add an agent' button. Clicked it; the inline form has NO main-customer selector (corporation is taken from the session). Completed agent details + temporary password and submitted. New agent 'Portal Test Agent 0624' created under the contact's OWN corporation (Test Co Z072693) and now appears under 'Your agents' (Active); the header card updated to '2 agents'. Signed out and logged in as the new agent: lands on the External Agent portal ('My repairs - Portal Test Agent 0624'). With no control to choose another corporation, mainCustomerId is session-bound. | Lucia | PASS. The seeded primary johan@acme.co.za could NOT be used: its password is no longer Password123! (login rejected 'Invalid login credentials'); admin and front-desk still authenticate with Password123!. A fresh primary contact 'TC12 Primary Contact' (Test Co Z072693, temp pw TestPass123!) was created to exercise the test - consistent with earlier runs where tech/front-desk accounts were created during testing. Test agent 'Portal Test Agent 0624' left in place. Follow-up: reset/confirm johan@acme.co.za's password if it should remain Password123!. |
| TC-AUTH-13 | Authentication & RBAC | Non-primary contact cannot add agents | RBAC invite_agent / §4.1 | Negative | Medium | A non-primary main-customer contact account exists. | 1. Log in as a non-primary main-customer contact.<br>2. View the portal 'Your agents' card.<br>3. (If reachable) attempt the add-agent action. | No '+ Add an agent' control is shown to a non-primary contact, and the add-agent action is denied server-side (invite_agent requires isPrimary). | Pass | Logged in as a non-primary main-customer contact ('TC13 NonPrimary Contact', tc13-nonprimary@example.com, isPrimary=false, ACME Industries). The 'Your agents' card lists ACME's agents (FD Test Agent 0624, Sipho Field) but shows NO '+ Add an agent' button. A DOM scan of the whole portal page returned 0 'Add an agent' controls. The add-agent control is correctly hidden for a non-primary contact. | Lucia | PASS on observable behaviour (UI control correctly hidden). No non-primary contact existed in seed data, so 'TC13 NonPrimary Contact' (ACME, isPrimary=false, temp pw TestPass123!) was created for this test. Server-side enforcement via invite_agent (requires isPrimary) is the documented control but was NOT independently exercised, because the portal renders no add-agent entry point for a non-primary contact - recommend a direct server-action/RLS test to fully prove the negative path. Out-of-scope observation: a non-primary contact still sees quote Approve/Reject controls - confirm whether quote approval should be restricted to the primary contact. |

---

## Authentication and RBAC

13 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-AUTH-01 | Authentication & RBAC | Gentick Admin can log in to internal portal | Sec 4.1, NFR-04 | Functional | High | Admin staff account exists in Supabase Auth. | 1. Navigate to the staff portal login page.<br>2. Enter valid admin email and password.<br>3. Submit. | Login succeeds over HTTPS and the Admin dashboard loads with full navigation (Admin dashboard, Technician queue, Job detail, New intake, settings). | Pass | Login succeeded; redirected to Admin dashboard. Full staff navigation present: Admin dashboard, Technician queue, New intake, Reports, Customers, Agents, Technicians & staff, Parts catalogue, Repair types, Message templates, Audit log, Settings. | Lucia |  |
| TC-AUTH-02 | Authentication & RBAC | Technician sees only technician-scoped navigation | Sec 4.1 | RBAC | High | Technician account exists. | 1. Log in as a Technician.<br>2. Inspect the landing page and sidebar navigation.<br>3. Attempt to open /dashboard and /reports directly. | Technician lands on the Technician queue ('My queue'). Sidebar under Internal-Staff shows ONLY Technician queue (no Admin dashboard, New intake or Reports). Repair initiation is unavailable (Can initiate repairs = No). Direct navigation to /dashboard and /reports redirects to /queue. No admin/management screens accessible. | Pass | RETEST after fix. Technician logs in and lands on '/queue' (My queue), not the Admin dashboard. Sidebar under 'Internal - Staff' shows only Technician queue - no Admin dashboard, no New intake, no Reports. Direct navigation to /dashboard and /reports both redirect to /queue (routes blocked, not just hidden). Admin/management screens remain hidden. Repair initiation still correctly blocked. | Lucia | PASS on retest. Original D-01 fix (queue landing + New intake hidden) confirmed. Additional fix applied during this retest: Admin dashboard + Reports links hidden for technicians AND routes blocked (redirect to /queue) - edits in components/ui/Sidebar.tsx, app/(staff)/dashboard/page.tsx, app/(staff)/reports/page.tsx. Closes the residual admin-data exposure. |
| TC-AUTH-03 | Authentication & RBAC | Front Desk can perform intake and dispatch | Sec 4.1 | RBAC | High | Front Desk account exists. | 1. Log in as Front Desk.<br>2. Open New intake and complete the required fields.<br>3. Open Customers and manage a record.<br>4. Open Agents and add an agent.<br>5. Attempt /settings, /dashboard, /reports and /queue directly. | Front Desk can create intake jobs, manage customer records, AND add agents. Sidebar shows New intake, Customers and Agents only; lands on New intake. Admin dashboard, Reports and Technician queue are hidden and their routes redirect to New intake. No access to admin-only Settings or staff management. | Pass | RETEST after fix. Front Desk (frontdesk@gentick.co.za) can now manage customer records: the Customers screen loads with an 'Add a customer' form and the customer list (6 customers). New intake works (form opens, required fields completable). Front Desk nav scoped to New intake + Customers only; Admin dashboard, Technician queue and Reports removed and their routes (/dashboard, /reports, /queue) redirect Front Desk to New intake. Settings and other admin-only screens remain blocked. Front Desk now lands on New intake after login. | Lucia | PASS on retest. D-02 fix confirmed (manage_customers permission + Customers UI). Additional scoping applied during retest at Lucia's request: removed Admin dashboard/Reports/Technician queue from Front Desk nav and blocked those routes; Front Desk home set to /intake. Edits in Sidebar.tsx, app/page.tsx, app/(staff)/dashboard\|reports\|queue/page.tsx. |
| TC-AUTH-04 | Authentication & RBAC | Main Customer Contact sees all jobs across their agents | Sec 4.1, FR-INT-10 | RBAC | High | Main customer with multiple agents and existing jobs. | 1. Log in as Main Customer Contact.<br>2. Open the repair overview. | All active jobs across all of the corporation's agents are visible; jobs belonging to other corporations are not visible. | Pass | Logged in as johan@acme.co.za - correct External Main customer portal ('Johan - repair overview'). Header cards: 'Active repairs across your agents' = 163 (1 agent), 'Awaiting your approval' = 6, 'Ready for collection' = 0. 'Action required' lists 6 quotes awaiting approval, all GT-ACME-* (Agent: Sipho Field) with View/Approve/Reject controls. All visible jobs belong to ACME; no other corporation's jobs shown. Correct portal (not staff dashboard). | Lucia | PASS on observable behaviour. Caveat: seed data has only ACME with a single agent (Sipho) and no second corporation, so 'all agents' and cross-corporation isolation are only partially exercised. Recommend adding a second ACME agent + a second corporation to fully prove TC-AUTH-04/05 isolation. |
| TC-AUTH-05 | Authentication & RBAC | Agent sees only their own jobs | Sec 4.1, FR-INT-10 | RBAC | High | Agent with own jobs; sibling agents under same main customer also have jobs. | 1. Log in as an Agent.<br>2. Open 'My repairs'. | Only the agent's own jobs are listed; jobs of sibling agents and other main customers are not shown. | Pass | Logged in as sipho@acme.co.za - correct External Agent portal ('My repairs - Sipho Field'). Cards: My active repairs 163, Awaiting approval 6 (quoted, pending main customer), Ready for collection 0, Completed YTD 37. 'My repairs' list shows only GT-ACME-* jobs with units/stages/submitted dates; 'My units' panel lists Sipho's units. No Approve/Reject controls present in the agent view. Correct agent portal (not staff/main-customer view). | Lucia | PASS on observable behaviour. Caveat: ACME has only one agent (Sipho) and no second corporation in seed data, so exclusion of sibling-agent / other-customer jobs cannot be fully demonstrated. Recommend adding a second ACME agent + second corporation to fully prove isolation. |
| TC-AUTH-06 | Authentication & RBAC | Agent cannot approve a quote | Sec 4.1, FR-COM-03, FR-QUO-02 | RBAC | High | Agent has a job in Quoted stage. | 1. Log in as the Agent.<br>2. Open the quoted job.<br>3. Attempt to find a quote approval control. | No approve/reject control is available to the agent for MVP; quote authority rests with the main customer only. | Pass | As agent Sipho, opened quoted job GT-ACME-0193 (Repair detail). Shows Quote Q-GT-ACME-0193-1 marked 'issued' with text: 'A quote has been prepared and sent to your main customer for approval. Full pricing will appear here once it is approved.' No approve/reject control present; pricing withheld from the agent. Progress timeline visible (Received -> Diagnosed -> Quoted). Quote authority correctly restricted to the main customer. | Lucia |  |
| TC-AUTH-07 | Authentication & RBAC | Agent self-service password reset via email link | FR-INT-13 | Functional | High | Registered agent who has forgotten password. | 1. On login, click 'Forgot password'.<br>2. Enter agent email.<br>3. Open the Supabase Auth reset email and follow the link.<br>4. Set a new password. | A clickable 'Forgot password' link opens a form to enter an email; submitting sends a Supabase reset email (generic confirmation, no account enumeration; in dev it lands in Mailpit :54324). The emailed link opens a 'set new password' page. After setting the password the recovery session is signed out and the user is redirected to the login page with a 'Password updated' confirmation, then signs in with the new password. Fully self-service, no staff intervention. | Pass | RETEST after fix. Login page now has a working 'Forgot your password?' link -> /auth/forgot-password. Submitting an email shows a generic 'if an account exists' confirmation (no enumeration) and sends a Supabase reset email (captured in Mailpit at 127.0.0.1:54324) with a 'Reset password' link and fallback code. The link opens /auth/reset-password ('Choose a new password'); setting a new password succeeds, signs out the one-time recovery session, and redirects to the login page showing 'Password updated - please sign in with your new password'. Login then succeeds with the new password. Fully self-service, no staff intervention. | Lucia | PASS on retest. D-03 fixed (forgot-password + reset pages, login link, Mailpit delivery). Post-reset UX improved during retest at Lucia's request: reset now signs out and redirects to login with a confirmation (edit in app/auth/actions.ts resetPassword + app/auth/login/page.tsx) instead of auto-entering the portal. |
| TC-AUTH-08 | Authentication & RBAC | MFA available for staff users | NFR-04 | Security | Medium | A staff account exists. Local Supabase has MFA TOTP enabled ([auth.mfa.totp] in supabase/config.toml; restart Supabase after enabling). An authenticator app (TOTP) is available. Enrolment UI lives at /account/security. | 1. Log in as staff.<br>2. Open security settings.<br>3. Enable multi-factor authentication.<br>4. Log out and back in. | MFA can be enabled and is enforced on subsequent staff logins. | Pass | RETEST after fix (Supabase restarted to apply [auth.mfa.totp]). Logged in as admin -> Security page (/account/security) offers MFA enrolment: TOTP QR scanned with an authenticator app and verified with a 6-digit code. After enrolment, signing out and back in as admin prompts for the MFA code (/auth/mfa) before granting access; entering a valid code completes login. MFA can be enabled and is enforced on subsequent staff logins. | Lucia | PASS on retest. D-04 fixed (TOTP enrolment at /account/security, login challenge at /auth/mfa, staff layout enforces AAL2). Requires local Supabase restart so config.toml [auth.mfa.totp] takes effect. Note: once enrolled, all subsequent admin logins require the authenticator code. |
| TC-AUTH-09 | Authentication & RBAC | Invalid credentials are rejected | NFR-04 | Negative | High | Any account exists. | 1. Go to login.<br>2. Enter a valid email with a wrong password.<br>3. Submit. | Login is rejected with a generic error; no indication whether email exists; no session created. | Pass | Entered valid email (admin@gentick.co.za) with an incorrect password and submitted. Login rejected with a generic error 'Invalid login credentials' - does not reveal whether the email exists. User remained on the login page; no session created. | Lucia | Generic error wording is correct (no account-enumeration). PASS. |
| TC-AUTH-10 | Authentication & RBAC | Direct URL access to another role's page is blocked | Sec 4.1, NFR-04 | RBAC | High | Logged in as Agent. | 1. While authenticated as an agent, request a staff-only URL (e.g., system settings) directly. | Access is denied (redirect or 403); no staff data is exposed. | Pass | Logged in as agent (sipho@acme.co.za). Entered the staff-only URL localhost:3000/settings directly in the address bar. Access denied - redirected to http://localhost:3000/portal (agent home). The Settings page and its staff/company data were not exposed. | Lucia | Role-based route protection works: agent requesting a staff URL is redirected to the agent portal, no staff data leaked. PASS. |
| TC-AUTH-11 | Authentication & RBAC | Front Desk can add an agent | RBAC invite_agent / §4.1 | Functional | Medium | Front Desk account exists; at least one main customer exists. | 1. Log in as Front Desk.<br>2. Open Agents.<br>3. Select a main customer, complete agent details + a temporary password.<br>4. Submit. | Agent is created and appears in the Agents list. The new agent can log in to the agent portal. Front Desk reaches Agents from its own sidebar. | Pass | Logged in as Front Desk (frontdesk@gentick.co.za, 'Pieter'); landed on New intake with sidebar New intake / Customers / Agents. Opened Agents from the sidebar and completed the 'Add an agent' form (Main customer = ACME Industries; name 'FD Test Agent 0624'; email fd-test-agent-0624@example.com; address + city; temporary password) and submitted. Agent created and appears at the top of the Agents list (now 6) as Active under ACME Industries. Signed out and logged in as the new agent (fd-test-agent-0624@example.com): lands on the External Agent portal ('My repairs - FD Test Agent 0624'). Front Desk reached Agents from its own sidebar. | Lucia | PASS. Note: 'Address line 1' is a required field on the agent form - the first submit was rejected with 'addressLine1: required' and succeeded once provided. Test agent 'FD Test Agent 0624' left in place (Active, ACME). |
| TC-AUTH-12 | Authentication & RBAC | Main customer primary contact can add an agent for own corporation | RBAC invite_agent / §4.1 | Functional | High | A primary main-customer contact account exists (e.g. johan@acme.co.za, isPrimary = true). | 1. Log in as the primary contact.<br>2. On the portal 'Your agents' card, click '+ Add an agent'.<br>3. Complete the agent details + a temporary password.<br>4. Submit. | Agent is created under the contact's OWN corporation only and appears under 'Your agents'; the new agent can log in. The mainCustomerId is taken from the session, so the contact cannot create agents for any other corporation (enforced by invite_agent). | Pass | Logged in as a primary main-customer contact ('TC12 Primary Contact', tc12-primary@example.com, isPrimary=true, corporation Test Co Z072693). The main-customer portal shows a 'Your agents' card with a '+ Add an agent' button. Clicked it; the inline form has NO main-customer selector (corporation is taken from the session). Completed agent details + temporary password and submitted. New agent 'Portal Test Agent 0624' created under the contact's OWN corporation (Test Co Z072693) and now appears under 'Your agents' (Active); the header card updated to '2 agents'. Signed out and logged in as the new agent: lands on the External Agent portal ('My repairs - Portal Test Agent 0624'). With no control to choose another corporation, mainCustomerId is session-bound. | Lucia | PASS. The seeded primary johan@acme.co.za could NOT be used: its password is no longer Password123! (login rejected 'Invalid login credentials'); admin and front-desk still authenticate with Password123!. A fresh primary contact 'TC12 Primary Contact' (Test Co Z072693, temp pw TestPass123!) was created to exercise the test - consistent with earlier runs where tech/front-desk accounts were created during testing. Test agent 'Portal Test Agent 0624' left in place. Follow-up: reset/confirm johan@acme.co.za's password if it should remain Password123!. |
| TC-AUTH-13 | Authentication & RBAC | Non-primary contact cannot add agents | RBAC invite_agent / §4.1 | Negative | Medium | A non-primary main-customer contact account exists. | 1. Log in as a non-primary main-customer contact.<br>2. View the portal 'Your agents' card.<br>3. (If reachable) attempt the add-agent action. | No '+ Add an agent' control is shown to a non-primary contact, and the add-agent action is denied server-side (invite_agent requires isPrimary). | Pass | Logged in as a non-primary main-customer contact ('TC13 NonPrimary Contact', tc13-nonprimary@example.com, isPrimary=false, ACME Industries). The 'Your agents' card lists ACME's agents (FD Test Agent 0624, Sipho Field) but shows NO '+ Add an agent' button. A DOM scan of the whole portal page returned 0 'Add an agent' controls. The add-agent control is correctly hidden for a non-primary contact. | Lucia | PASS on observable behaviour (UI control correctly hidden). No non-primary contact existed in seed data, so 'TC13 NonPrimary Contact' (ACME, isPrimary=false, temp pw TestPass123!) was created for this test. Server-side enforcement via invite_agent (requires isPrimary) is the documented control but was NOT independently exercised, because the portal renders no add-agent entry point for a non-primary contact - recommend a direct server-action/RLS test to fully prove the negative path. Out-of-scope observation: a non-primary contact still sees quote Approve/Reject controls - confirm whether quote approval should be restricted to the primary contact. |

---

## Intake and Customer Mgmt

35 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INT-01 | Intake & Customer Mgmt | Capture mandatory main customer fields at registration | FR-INT-01 | Functional | High | Admin/Front Desk creating a new main customer. | 1. Open new main customer form.<br>2. Enter Corporation Name, Contact Person, Contact Email, Contact Phone.<br>3. Save. | Record saved with all four fields persisted and retrievable. | Pass | RETEST after fix. The 'Add a customer' form now captures all four mandatory fields. Created prefix RETEST / 'Retest Customer One' with Contact person 'Pat Primary', Contact email retest-primary@example.com, Contact phone +27821119999 and a temporary password. Save succeeded: the corporation appears in the Customers list with 1 contact, and the Contacts list shows 'Pat Primary' (Primary) under Retest Customer One, Active (login provisioned). All four mandatory fields are persisted and retrievable from a single registration step. | Lucia | PASS on retest. Fix: customer registration now captures Contact Person/Email/Phone (required) + an optional temporary password and creates the customer AND its primary contact atomically (createCustomerWithContact, withTransaction). Prefix/Corporation alignment fixed by moving to FormGrid. Files: components/feature/CustomersManager.tsx, app/(staff)/customers/actions.ts, lib/people/people.ts, components/ui/Field.tsx. Note: contact email is format-validated only - no verification email is sent. |
| TC-INT-02 | Intake & Customer Mgmt | Main customer registration rejects missing required fields | FR-INT-01 | Negative | High | New main customer form open. | 1. Leave Corporation Name and Contact Email blank.<br>2. Attempt to save. | Save blocked with inline validation on the missing required fields. | Pass | RETEST after fix. Clicking Add customer with all fields blank now blocks the save and shows field-level inline validation on EVERY required field at once: Prefix 'must be 2-10 letters/numbers', Corporation name 'required', Contact person 'required', Contact email 'invalid email', Contact phone 'use E.164 e.g. +27821234567'. Errors render inline beneath each field (no longer a single top-of-form banner) and all missing fields are surfaced simultaneously. No record was created. | Lucia | PASS on retest. Fix: added client-side field-level validation surfacing all missing/invalid required fields together, rendered inline via a new optional 'error' prop on the shared Field component; server-side zod validation retained as backstop. Resolves both original gaps (banner-not-inline and only the first/prefix error being shown). |
| TC-INT-03 | Intake & Customer Mgmt | Reject invalid email/phone formats on customer record | FR-INT-01, FR-INT-02 | Negative | Medium | New customer/agent form open. | 1. Enter 'notanemail' in Contact Email and 'abc' in Phone.<br>2. Attempt to save. | Validation errors are shown for malformed email and phone; record not saved. | Pass | Entered 'notanemail' in Contact Email and 'abc' in Phone on the new customer form and attempted save. Inline validation errors shown on both fields; record not saved. | Lucia |  |
| TC-INT-04 | Intake & Customer Mgmt | Capture agent fields linked to a main customer | FR-INT-02 | Functional | High | An existing main customer. | 1. Create an agent with Agent Name, Phone, Email, Address.<br>2. Link to the main customer.<br>3. Save. | Agent saved and associated to the correct main customer. | Pass | Created an agent with name, phone, email and address, linked to a main customer; save succeeded and the agent appears in the Agents list under the correct customer. DEFECT found during this case (fixed same session): the Add agent form showed only one validation error at a time instead of all invalid fields at once. Fix mirrors the TC-INT-02 customer-form fix — client-side field-level validation surfacing all errors inline via the Field 'error' prop; applied to BOTH the staff Agents form and the portal '+ Add an agent' form. Files: components/feature/AgentsManager.tsx, components/feature/PortalAddAgent.tsx. Server-side zod validation (agentInput) unchanged as backstop. | Lucia |  |
| TC-INT-05 | Intake & Customer Mgmt | Capture WhatsApp opt-in with timestamp, IP, and wording version | FR-INT-03 | Functional | High | First-time registration of a contact/agent via web. | 1. Complete registration and tick the WhatsApp consent box.<br>2. Submit.<br>3. Open the resulting consent record / audit log. | A ConsentRecord is stored with granted timestamp, source IP (web), and the consent wording version shown to the user. | Fail | FAIL — feature not implemented. The consent_records table exists in the DB schema (001_initial_schema.sql) but no application code writes to it: no WhatsApp consent checkbox in any registration/invite flow, so no ConsentRecord (timestamp/IP/wording version) is captured. See defect D-06. | Lucia/Claude (code inspection) |  |
| TC-INT-06 | Intake & Customer Mgmt | Opt-out toggle immediately revokes WhatsApp consent and logs it | FR-INT-04 | Functional | High | Contact/agent with active WhatsApp consent. | 1. Open the profile.<br>2. Switch the WhatsApp opt-out toggle on.<br>3. Save.<br>4. Inspect consent/audit log. | Consent is revoked immediately, a revocation event with timestamp is logged, and no further WhatsApp messages are sent to that party. | Fail | FAIL — feature not implemented. No WhatsApp opt-out toggle exists on any profile screen and no code writes revocation events to consent_records. See defect D-06. | Lucia/Claude (code inspection) |  |
| TC-INT-07 | Intake & Customer Mgmt | Annual consent audit flags consents older than 12 months | FR-INT-05 | Functional | Medium | A consent record dated >12 months ago exists. | 1. Run / open the annual consent audit workflow. | The aged consent is flagged for renewal; consents under 12 months are not flagged. | Fail | FAIL — feature not implemented. No annual consent audit workflow exists in the app or n8n; consent_records is never populated so there is nothing to audit. See defect D-06. | Lucia/Claude (code inspection) |  |
| TC-INT-08 | Intake & Customer Mgmt | Intake via main customer origin path | FR-INT-06 | Functional | High | Logged in as Front Desk / main customer. | 1. Create a job with origin = Main customer (pre-registered).<br>2. Capture unit details.<br>3. Save. | Job created with shipment origin 'main_customer'; identical data fields captured as other paths. | Pass | Created a job via New intake with origin = Main customer (pre-registered), captured unit details and saved. Job created with shipment origin 'main_customer'; same data fields captured as the other intake paths. | Lucia |  |
| TC-INT-09 | Intake & Customer Mgmt | Intake via agent origin path (self-service) | FR-INT-06, Sec 2.1 | Functional | High | Logged in as Agent on mobile. | 1. Use 'Book a new repair'.<br>2. Register a unit and fault.<br>3. Submit. | Job created with origin 'agent'; agent portal is intake-capable, not read-only. | Pass | Logged in as agent (sipho@acme.co.za), used 'Book in a repair', registered a unit and fault, submitted. Job created with origin 'agent'; agent portal confirmed intake-capable. DEFECT found during this case (D-07, fixed same session): incomplete units showed only one banner error and blank unit rows were silently dropped before submit. Fixed in components/feature/AgentIntakeForm.tsx; retested Pass. | Lucia |  |
| TC-INT-10 | Intake & Customer Mgmt | Intake via Gentick front desk for unannounced unit | FR-INT-06 | Functional | High | Front Desk receives an unregistered unit. | 1. Open New intake.<br>2. Select origin = Walk-in / unannounced.<br>3. Identify the owning customer/agent.<br>4. Save. | Job created with origin 'walk_in'; the same data fields are captured as the other two paths. | Pass | Logged in as Front Desk, created a walk-in intake for an identified agent (ACME/Sipho Field) and saved. Job created with origin 'walk_in'; same data fields captured as the other paths. DEFECT found during this case (D-08, fixed same session): incomplete units showed only the first server error in a banner. Fixed in components/feature/IntakeForm.tsx; retested Pass. | Lucia |  |
| TC-INT-11 | Intake & Customer Mgmt | Walk-in requires customer/agent identification before save | FR-INT-06, Wireframe Step 1 | Negative | High | New intake, origin = Walk-in. | 1. Leave the originator/owner unidentified.<br>2. Attempt to save the shipment. | Save is blocked with a prompt to identify the customer/agent (per wireframe hint 'Walk-in needs you to identify customer/agent before save'). | Pass | As Front Desk, attempted to save a walk-in shipment with the agent/owner unidentified. Save blocked with an inline 'select the agent the unit(s) belong to' error on the Agent field (per D-08 fix). Enhancement added same session: selecting an agent now auto-backfills the Main customer field. | Lucia |  |
| TC-INT-12 | Intake & Customer Mgmt | Register multiple units in a single shipment | FR-INT-07, Wireframe Step 2 | Functional | High | New intake open. | 1. Add Unit 1 and Unit 2 via '+ Add another unit'.<br>2. Complete both units.<br>3. Receive shipment. | Each unit receives its own job number while sharing one shipment identifier (e.g., SHP-0438). | Pass | Added two units to one shipment via '+ Add unit', completed both and received the shipment. Each unit received its own job number while sharing a single shipment identifier. | Lucia |  |
| TC-INT-13 | Intake & Customer Mgmt | Job number format and per-customer sequencing | FR-INT-08, Sec 14 #1 | Functional | High | Main customer with prefix 'ACME' and last job GT-ACME-0184. | 1. Create a new job for ACME.<br>2. Inspect the generated job number. | Job number is GT-ACME-0185 - prefix from customer record, sequence per-customer, zero-padded to 4 digits. | Pass | Created a new job for ACME and inspected the generated job number: format GT-ACME-NNNN with the prefix from the customer record, per-customer sequence incremented by one from ACME's previous job, zero-padded to 4 digits. | Lucia |  |
| TC-INT-14 | Intake & Customer Mgmt | Walk-in / unlinked jobs use fallback prefix GT-GEN | FR-INT-08 | Functional | High | Design note: every job links to an agent (jobs.agent_id NOT NULL), so a customer prefix always exists. The GT-GEN fallback is a code-level safety net only (lib/jobs/numbering.ts) with no reachable UI path. | 1. Create a walk-in job without an assigned customer prefix.<br>2. Inspect the job number. | N/A by design — walk-ins require owner identification before save (verified by TC-INT-11), so all jobs are numbered GT-{CUSTOMER_PREFIX}-NNNN. The GT-GEN fallback remains in nextJobNumber() for the data-level edge case of a job with no main customer and would produce e.g. GT-GEN-0001 if ever invoked. | N/A | Marked N/A on 7 Jul 2026 — superseded by design. UI enforces agent identification (TC-INT-11 Pass), making an unlinked job impossible to create; fallback verified present in code (FALLBACK_PREFIX='GEN' in lib/jobs/numbering.ts). | Lucia/Claude |  |
| TC-INT-15 | Intake & Customer Mgmt | Job number sequence is independent per customer | FR-INT-08, Sec 14 #2 | Functional | Medium | Two main customers ACME and KAROO. | 1. Create a job for KAROO, then one for ACME.<br>2. Compare numbers. | KAROO uses its own sequence (e.g., GT-KAROO-0043) independent of ACME's sequence; no cross-customer collisions. | Pass | Created a job for a second main customer, then one for ACME, and compared job numbers: each customer's sequence advanced independently with its own prefix; no cross-customer collisions. | Lucia |  |
| TC-INT-16 | Intake & Customer Mgmt | Capture all per-unit intake fields | FR-INT-09 | Functional | High | New intake, a unit being added. | 1. Enter unit type, serial number, fault description, accessories shipped.<br>2. Attach intake photos.<br>3. Save. | All per-unit fields persist, including at least one intake photo reference. | Pass | New intake completed with all per-unit fields (unit type, serial, fault description, accessories) and a photo attached in the form via 'Photos of condition on arrival'. After receive, the job showed all fields and the photo persisted (customer-visible, caption 'Intake - condition on arrival'). | Lucia | Passed as originally written after the in-form intake photo enhancement (see Intake defect record, enhancement E: photos at intake). Previously would have required a two-step flow via Job detail Photos panel. |
| TC-INT-17 | Intake & Customer Mgmt | Serial number auto-links to prior repair history | FR-INT-09, Wireframe Step 2 | Functional | Medium | Serial SN-21A847 has 2 prior repairs. | 1. Begin a new unit and enter serial SN-21A847. | System detects the existing unit and shows 'This unit has been here before (2 prior repairs) - auto-linked.' | Pass | Entered serial GE1145 (2 prior repairs) on New intake: green banner appeared under the Serial field - 'This unit has been here before (2 prior repairs) - auto-linked' with last job number and last-seen date. Banner clears when the serial is edited. Job detail page shows the new 'Previous repairs for this unit' card. | Lucia | Passed after building the indicator in this session (see Intake defect record, enhancement 5: prior-repair-history indicator). Server-side auto-link by case-insensitive serial already existed; UI feedback and job-detail history panel were added. Staff-only lookup so counts don't leak to portal users. |
| TC-INT-18 | Intake & Customer Mgmt | Jobcard emailable to originator at intake | FR-INT-10, Wireframe Step 3 | Functional | High | Shipment ready to receive. | 1. Complete intake and confirm 'Receive shipment & notify'. | Jobcard(s) are emailed to the originator (e.g., the agent) at the point of intake. | Pass | Intake of GT-TE1-0003 emitted job.received with the full jobcard payload (job number/fault, unit serial/type, agent name+email+phone, main customer + primary contact, shipment origin/tracking) - verified in webhook_events. Delivery to local n8n returned 404 (workflow 01 not imported/active in local n8n) - environment config, not app code; event remains queued for retry. Required a dev-server restart to pick up the new code. | Lucia | Pass on the agreed app-side criterion (see Intake defect record, enhancement 6): createIntake now emits job.received per job post-commit; n8n workflow 01 turns it into the jobcard email. Actual email delivery requires the n8n workflow to be imported/activated and a mail provider (Postmark) configured - to be covered under the Communication module tests. |
| TC-INT-19 | Intake & Customer Mgmt | Both main customer and agent can view jobcards in portal | FR-INT-10 | Functional | Medium | A job exists for an agent under a main customer. | 1. Log in as the agent and open the job.<br>2. Log in as the main customer and open the same job. | Both parties can view the relevant jobcard (current and historical) through their portals. | Pass | Logged in as the agent (sipho@acme.co.za) and opened an ACME job from the agent portal, then as the main customer (johan@acme.co.za) and opened the same job. Both portals showed the jobcard with current status and history. | Lucia |  |
| TC-INT-20 | Intake & Customer Mgmt | Main-customer-led agent invitation flow | FR-INT-11, FR-INT-13 | Functional | High | Logged in as Main Customer Contact. | 1. Click 'Invite new agent'.<br>2. Enter the agent's details and send.<br>3. Open the invitation email as the agent and follow the link.<br>4. Complete the profile incl. WhatsApp opt-in on first login. | Invitation email is sent, agent self-completes profile and consent, and the agent account is linked to the inviting main customer. | Fail | No invitation email flow exists: the portal '+ Add an agent' creates the agent directly with a password set by the main customer contact (createAgent/provisionUnverifiedAuthUser). No email sent, no link, no agent self-completion, no WhatsApp opt-in (D-06). accept-invitation page and invitedAt/invitationAcceptedAt fields exist as scaffolding only. | Lucia | Fail - grouped into the 'agent invitations + WhatsApp consent' work package with D-06 (see defect record D-09). Build scheduled after the test run. |
| TC-INT-21 | Intake & Customer Mgmt | Pending agent invitation shows expiry and resend | FR-INT-11, Wireframe | Functional | Low | An invitation has been sent but not accepted. | 1. As main customer, open 'Your agents'. | Pending invite is shown with an 'Invited' badge and an expiry date plus a 'resend invite' action. | Fail | Not testable - no pending-invitation state exists because no invitation email flow is implemented (see TC-INT-20). No expiry or resend UI anywhere. | Lucia | Fail - grouped into the 'agent invitations + WhatsApp consent' work package with D-06 (see defect record D-09). Build scheduled after the test run. |
| TC-INT-22 | Intake & Customer Mgmt | Expired invitation link cannot be used | FR-INT-11 | Negative | Medium | An invitation token past its expiry. | 1. Open the expired invitation link. | Link is rejected as expired; agent is prompted to request a new invite; no account created. | Fail | Not testable - no invitation links are ever generated (see TC-INT-20), so an expired-link rejection path cannot exist. | Lucia | Fail - grouped into the 'agent invitations + WhatsApp consent' work package with D-06 (see defect record D-09). Build scheduled after the test run. |
| TC-INT-23 | Intake & Customer Mgmt | Extra units discovered - main customer originator routing | FR-INT-12(a) | Notification | Medium | Shipment originated by main customer; extra unit found at intake. | 1. At intake, add the extra unit beyond what was registered.<br>2. Save and notify. | Both the main customer AND the agent are notified about the extra unit. | N/A | N/A by design in this build: main customers cannot pre-register units (no booking form), so a main-customer-origin shipment has no announced baseline against which an 'extra' unit can be detected. Extra-unit detection exists only for agent pre-registrations (TC-INT-24). If main-customer pre-registration is added later, this case becomes testable. | Lucia | Reworded per design decision 8 Jul 2026; see defect record enhancement 7 (extra-unit discovery). Revisit if main-customer pre-registration is built. |
| TC-INT-24 | Intake & Customer Mgmt | Extra units discovered - agent originator routing | FR-INT-12(b) | Notification | Medium | Shipment originated by agent; extra unit found. | 1. Add the extra unit at intake.<br>2. Save and notify. | The agent is notified and the main customer is copied for visibility. | Pass | PASS. Front Desk (Pieter) received a shipment for Sipho (ACME): pre-registered unit GE55432-1 added via 'Add to shipment' (existing job GT-ACME-0212, no duplicate created) + hand-entered extra SN-TEST-24B (new job GT-ACME-0223). webhook_events verified in DB (22 Jul 09:51:17): job.extra_unit_discovered emitted for GT-ACME-0223 ONLY (not for GT-ACME-0212), before both jobs' job.received events, with agent + main customer + originator_type routing payload for n8n (agent notified, main customer copied). delivered_at NULL / 1 attempt = queued pending n8n workflow 01 import — agreed local pass criterion (as TC-INT-18). | Lucia | Evidence: webhook_events query 22 Jul 2026. Full delivery to be re-verified in the Communication module run once n8n workflow 01 is imported. |
| TC-INT-25 | Intake & Customer Mgmt | Extra units discovered - walk-in routing by identified owner | FR-INT-12(c) | Notification | Medium | No pre-registration; extra units found. | 1. Ensure the agent has NO pending pre-registrations.<br>2. Front desk creates the jobs at intake (origin = Walk-in / unannounced), identifying the owning agent.<br>3. Confirm the no-extra-units checkbox and save.<br>4. Inspect webhook_events for the created jobs. | Jobs are created and the job.received notification routes to whichever party the front desk identifies as the owner (payload carries the identified agent + main customer/primary contact). NOTE (agreed 22 Jul 2026): with no pre-registration baseline, job.extra_unit_discovered deliberately does NOT fire for walk-ins (same design reasoning as TC-INT-23 N/A); the pass criterion is correct owner routing on job.received. Local pass criterion is the webhook_events row per job, as agreed for TC-INT-18. | Pass | PASS (per reworded criterion agreed 22 Jul). Front Desk (Pieter) walk-in intake, agent FD Test Agent 0624 (ACME Industries) — agent had NO pending pre-registrations (no pending card shown, precondition held). Unit SN-TEST-25W -> new job GT-ACME-0224. webhook_events verified (10:01:02): single job.received, NO job.extra_unit_discovered (correct — no pre-reg baseline). Routing payload verified: agent='FD Test Agent 0624', main_customer='ACME Industries', originator_type='front_desk' — notification routes to the owner front desk identified. delivered_at NULL = queued pending n8n import (agreed local criterion). | Lucia |  |
| TC-INT-26 | Intake & Customer Mgmt | Confirm-no-extra-units checkbox required before receive | Wireframe Step 3 | UI | Medium | Intake at Step 3. | 1. Leave the 'no extra unannounced units' confirmation unticked.<br>2. Attempt 'Receive shipment & notify'. | Receiving is gated on the confirmation tick; instruction reminds user to add extras above first if any exist. | Pass | PASS. Executed as Front Desk (Pieter) on New intake, agent = Sipho (ACME). (1) Valid unit SN-TEST-26A entered with the 'no extra unannounced units' checkbox unticked -> 'Confirm & notify' blocked with inline red error under the checkbox and top banner; no job created. (2) Checkbox ticked, resubmitted -> intake succeeded, job GT-ACME-0222 created. Serial-history banner subsequently confirmed the job (re-confirms TC-INT-17). Server backstop (confirmedNoExtraUnits in CreateIntakeSchema/createIntake) verified in code review 22 Jul. | Lucia | Side effect noted: Sipho had 15 pending pre-registrations at the time, so SN-TEST-26A was treated as an unannounced extra — a job.extra_unit_discovered event is expected for GT-ACME-0222 in webhook_events; to be verified during the TC-INT-24/25 DB check. VERIFIED 22 Jul: webhook_events shows job.extra_unit_discovered + job.received for GT-ACME-0222 at 09:48:20. |
| TC-INT-27 | Intake & Customer Mgmt | Save draft preserves an in-progress intake | Wireframe Step 1 | Functional | Low | Partial intake entered. | 1. Click 'Save draft'.<br>2. Navigate away and return. | Draft is retained with previously entered data; no job numbers issued until receive. | Fail | FAIL — feature not implemented. No 'Save draft' control exists on staff New intake (only Cancel / Book in repair), and no draft route, state or persistence exists anywhere in the app (no match for 'draft' in application code). See defect D-10; build deferred to after the test run per the D-06/D-09 pattern (priority Low). | Lucia/Claude (code inspection) | Logged 22 Jul 2026 by code inspection. D-10 in Gentick_RMS_Intake_Defects_2026-07-07.md. |
| TC-INT-28 | Intake & Customer Mgmt | Add an additional (non-primary) contact to a corporation | FR-INT-01 | Functional | Medium | A corporation exists with a primary contact (captured at registration). Logged in as Admin or Front Desk. | 1. Open Customers.<br>2. In 'Add another contact', select the corporation.<br>3. Enter Contact person and Email (login); optionally a Phone and a temporary password; leave 'Mark as the primary contact' unchecked.<br>4. Click Add contact. | The additional contact is created and listed under the corporation in Contacts WITHOUT a Primary badge; the corporation's contact count increments. If a temporary password was supplied, the new contact can log in to the main-customer portal. | Pass | PASS (both parts). Front Desk (Pieter), Customers -> 'Add another contact', ACME Industries. (A) Thandi Test (thandi@acme.co.za, password set) added with 'Mark as primary' UNTICKED -> listed under ACME without Primary badge, Login Active, contact count incremented; Johan initially retained Primary. (B) D-11 retest: Marius Test (marius@acme.co.za) added with 'Mark as primary' TICKED -> Marius shows the Primary badge and Johan's badge is REMOVED — exactly one primary per corporation, demote-in-transaction fix verified in UI. Johan restored as primary via SQL immediately after (test data restored). Typecheck (npx tsc --noEmit) clean on the D-11 change. | Lucia | Adjacent defect D-11 found by code inspection 22 Jul 2026: creating a contact with 'Mark as primary' did not demote the existing primary (two primaries possible; primary drives quote-approval routing and invite_agent). Fixed same day in lib/people/people.ts (createContact demotes existing primary in-transaction). Retest during this case: also verify the primary-tick path demotes the old primary. |
| TC-INT-29 | Intake & Customer Mgmt | 'Add another contact' rejects missing customer and invalid email/phone | FR-INT-01, FR-INT-02 | Negative | Medium | Customers page open as Admin or Front Desk. | 1. In 'Add another contact', click Add contact with no customer selected.<br>2. Select a customer, then enter 'notanemail' as Email and 'abc' as Phone.<br>3. Attempt to save. | Save is blocked: 'Select a customer' is shown when none is chosen; validation errors are shown for the malformed email and phone; no contact is created. | Pass | PASS. Front Desk (Pieter), Customers -> 'Add another contact' submitted with Customer unselected, Contact person blank, email 'notanemail', phone 'abc': all four errors flagged inline simultaneously (select a customer / required / invalid email / E.164 format), no server call, no contact created. Validation from the 7 Jul proactive sweep confirmed in UI. | Lucia |  |
| TC-INT-30 | Intake & Customer Mgmt | Add agent (staff) surfaces all invalid/missing fields at once; no record saved | FR-INT-02 / D-05 | Negative | High | Logged in as Admin or Front Desk; Agents screen open. | 1. Leave all fields blank and click 'Add agent'.<br>2. Then enter 'notanemail' in Email, 'abc' in Phone, a 3-char password, leaving Address/City blank.<br>3. Click 'Add agent' again. | Step 1: inline errors shown simultaneously under Main customer ('select a customer'), Agent name ('required'), Email ('invalid email'), Address line 1 ('required') and City ('required'). Step 2: inline errors for invalid email, phone ('use E.164 e.g. +27821234567') and password ('min 8 characters') shown together. No agent record is created in either step. | Pass | Verified 7 Jul 2026 during D-05 retest: blank/invalid submits flag ALL missing/invalid fields at once, inline beneath each field; no record saved. | Lucia | Added after D-05 (agent form showed only one error at a time). Fix in components/feature/AgentsManager.tsx; server-side zod (agentInput) is the backstop. |
| TC-INT-31 | Intake & Customer Mgmt | Portal '+ Add an agent' surfaces all invalid/missing fields at once; no record saved | FR-INT-02, FR-INT-11 / D-05 | Negative | Medium | Logged in as a primary main-customer contact (e.g. johan@acme.co.za); 'Your agents' card open. | 1. Click '+ Add an agent', leave all fields blank and click 'Add agent'.<br>2. Then enter invalid email/phone and a short password.<br>3. Click 'Add agent' again. | All missing/invalid fields are flagged simultaneously with inline errors (name required, invalid email, E.164 phone if entered, address/city required, password min 8 if entered). No agent record is created. | Pass | PASS. Executed as Thandi Test (thandi@acme.co.za), promoted to ACME primary contact via the new edit-contact feature (Johan's login was blocked by an open auth issue — see defect log). Portal 'Your agents' -> '+ Add an agent' submitted with name/address/city blank and email 'notanemail': ALL invalid/missing fields flagged inline simultaneously (name required, invalid email, address required, city required); phone and temp password correctly optional; no server call made and no agent record created (agent list unchanged). Bonus verifications en route: non-primary contact saw NO '+ Add an agent' control before promotion (re-confirms TC-AUTH-13), and unverified-contact first login correctly demanded a 6-digit email OTP (delivered via Mailpit). | Lucia | Companion to TC-INT-30; same D-05 fix applied to components/feature/PortalAddAgent.tsx. |
| TC-INT-32 | Intake & Customer Mgmt | Agent 'Book in a repair' flags all missing unit fields on all units; blank units not silently dropped | FR-INT-06, FR-INT-09 / D-07 | Negative | High | Logged in as an Agent; 'Book in a repair' open with at least two unit rows, one incomplete/blank. | 1. Complete Unit 1 fully; leave Unit 2 entirely blank.<br>2. Click 'Book in repair' / 'Confirm & notify'.<br>3. Observe validation.<br>4. Remove or complete Unit 2 and resubmit. | Step 2: submission is blocked; ALL missing required fields (Serial number, Unit type, fault description) are flagged inline on EVERY incomplete unit simultaneously, with a banner instructing to complete highlighted fields or remove unused units. The blank unit is NOT silently dropped. Step 4: submission succeeds and each completed unit gets its own job with origin 'agent'. | Pass | Verified 7 Jul 2026 during D-07 retest: blank unit row flagged on all three required fields inline; nothing silently dropped; after completing/removing the row the booking succeeded. | Lucia | Added after D-07. Fix in components/feature/AgentIntakeForm.tsx (per-unit inline validation, no silent filtering, stale-error clearing on add/remove). Server-side IntakeUnitSchema is the backstop. |
| TC-INT-33 | Intake & Customer Mgmt | Staff New intake flags all missing unit fields on all units and agent selection inline | FR-INT-06, FR-INT-09 / D-08 | Negative | High | Logged in as Admin or Front Desk; New intake open with two unit rows, one complete and one blank; no agent selected. | 1. Complete Unit 1 fully; leave Unit 2 entirely blank; leave 'Agent (owner of the unit)' unselected.<br>2. Click 'Confirm & notify'.<br>3. Observe validation.<br>4. Select the agent, remove or complete Unit 2, resubmit. | Step 2: submission blocked; the agent field shows an inline 'select the agent' error AND all missing required fields (Serial number, Unit type, Fault description) are flagged inline on every incomplete unit simultaneously. Step 4: shipment saves; each unit gets its own job. | Pass | Verified 7 Jul 2026 during D-08 retest: blank unit flagged on all three required fields inline; after completing, the walk-in intake saved successfully. | Lucia | Added after D-08. Fix in components/feature/IntakeForm.tsx. The 'Add another contact' form (TC-INT-29) was proactively given the same fix in components/feature/CustomersManager.tsx during this session. |
| TC-INT-34 | Intake & Customer Mgmt | Edit contact details and reassign primary via Contacts table | FR-INT-01 / D-11 follow-up | Functional | Medium | A corporation with a primary and at least one non-primary contact. Logged in as Admin or Front Desk. | 1. Open Customers -> Contacts and click Edit on a non-primary contact.<br>2. Change the person name and phone (E.164) and Save.<br>3. Click Edit on the same contact, tick 'Primary contact', Save.<br>4. Click Edit on the current primary and inspect the Primary checkbox.<br>5. Attempt invalid input (blank name, malformed phone) and Save. | Edits persist (name/phone; login email not editable). Promoting a contact demotes the previous primary in the same transaction - exactly one Primary badge per corporation at all times (DB backstop uq_mcc_primary). The current primary's checkbox is disabled with a hint to promote another contact instead (a corporation can never be left with no primary). Invalid input is flagged inline, all errors at once; no server call is made. | Pass | PASS (all five steps). (1) Edited Thandi Test's phone to +27821110001 — persisted. (2) Ticked Primary on Thandi -> she gained the Primary badge and Johan was demoted (demote-in-transaction). (3) Reverse promotion verified (Johan re-promoted, later Thandi promoted again for TC-INT-31 after Johan's login issue surfaced). (4) Current primary's checkbox disabled with hint. (5) Negative: blank name + phone 'abc' -> both errors inline at once, nothing saved. Login email correctly not editable. | Lucia | Enhancement built 22 Jul 2026 (Lucia request during TC-INT-28): lib/people/people.ts (updateContact), app/(staff)/customers/actions.ts (updateContactAction), components/feature/CustomersManager.tsx (inline row edit). |
| TC-INT-35 | Intake & Customer Mgmt | Remove (soft-delete) a contact; primary contact cannot be removed | FR-INT-01 / D-11 follow-up | Functional | Medium | A corporation with a primary and at least one non-primary contact. Logged in as Admin or Front Desk. | 1. Open Customers -> Contacts and click Remove on a non-primary contact.<br>2. Confirm via the inline 'Confirm remove' prompt.<br>3. Inspect the Contacts list and the corporation's contact count.<br>4. Inspect the Remove button on the current primary contact.<br>5. (DB) select deleted_at for the removed contact. | Inline two-step confirmation shown (no browser dialog). On confirm, the contact disappears from the Contacts list and the corporation's contact count decrements. The removal is a soft delete: deleted_at is set, the row remains in the DB for audit/recovery, and the contact's portal login no longer resolves. The primary contact's Remove button is disabled with a hint to mark another contact as primary first (one-primary invariant preserved). | Pass | PASS. Front Desk (Pieter), Customers -> Contacts. Remove on non-primary contact (Marius Test, ACME Industries) showed the inline two-step 'Remove this contact?' confirmation (no browser dialog); Confirm remove soft-deleted the contact — removed from the Contacts list, ACME contact count decremented. Primary contacts' Remove buttons verified disabled across ALL corporations (Johan/ACME, John Doe/AgrTest, John/Agri-IT, Pat/Retest Customer One) — one-primary invariant preserved; server-side backstop in deleteContact. Soft delete: deleted_at set, row retained in DB. | Lucia | Enhancement built 22 Jul 2026 (Lucia request, with TC-INT-34): deleteContact in lib/people/people.ts, deleteContactAction, inline confirm UI in CustomersManager.tsx. Supabase auth-user disablement deferred to D-09. |

---

## Workflow and State Machine

14 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-WF-01 | Workflow & State Machine | Job moves through all 10 defined stages in order | FR-WF-01, Sec 10 | Functional | High | A received job. | 1. Advance stage sequentially: Received -> Diagnosed -> Quoted -> Approved -> In Progress -> QC -> Boxed -> Ready for Collection -> Collected -> Closed. | Each forward transition is accepted in order and the stage timeline reflects the current stage. | Not Run |  |  |  |
| TC-WF-02 | Workflow & State Machine | Each transition records timestamp, actor, prev/new stage, notes | FR-WF-02 | Functional | High | A job at any stage. | 1. Advance the stage and add an optional note.<br>2. Open Stage history / History tab. | History entry shows timestamp, the staff user who triggered it, previous and new stage, and the note. | Not Run |  |  |  |
| TC-WF-03 | Workflow & State Machine | Assign and reassign technician (logged) | FR-WF-03 | Functional | Medium | A diagnosed job. | 1. Assign technician Naledi.<br>2. Reassign to another technician.<br>3. Open history. | Both assignment and reassignment are saved and logged with actor and timestamp. | Not Run |  |  |  |
| TC-WF-04 | Workflow & State Machine | Diagnosis note internal-only; customer-facing note visible in portal | FR-WF-04 | RBAC | High | A job with both a technician diagnosis note and a customer-facing note. | 1. As staff, view both notes on Job detail.<br>2. As the agent/main customer, view the job in the portal. | Internal diagnosis note is hidden from the portal; only the customer-facing note appears externally. | Not Run |  |  |  |
| TC-WF-05 | Workflow & State Machine | Attach multiple photos at any stage with caption and visibility flag | FR-WF-05 | Functional | Medium | A job in progress. | 1. Upload photos with captions.<br>2. Set one to internal-only and one to customer-facing.<br>3. View as staff and as customer. | Photos save with captions; internal-only photos are hidden in the portal while customer-facing ones are visible. | Not Run |  |  |  |
| TC-WF-06 | Workflow & State Machine | Not Repairable requires a structured reason from the defined set | FR-WF-06 | Functional | High | A diagnosed job. | 1. Move the job to Not Repairable.<br>2. Inspect the reason field. | A structured reason must be selected from: lightning damage, secondary lightning damage, water damage, component failure, power supply failure, chip-to-probe communication failure, other (free text). Save blocked without a reason. | Not Run |  |  |  |
| TC-WF-07 | Workflow & State Machine | Boxed stage auto-triggers ready-for-collection notification to agent | FR-WF-07, FR-COM-01 | Notification | High | A job reaching the Boxed stage; agent has consent. | 1. Mark the job Boxed. | A 'ready for collection' notification is sent automatically to the agent (WhatsApp + email). | Not Run |  |  |  |
| TC-WF-08 | Workflow & State Machine | Quoted to Approved is the only customer-triggerable transition | Sec 10 | State Machine | High | A job in Quoted stage. | 1. As the main customer, approve the quote (portal/WhatsApp/email). | Job auto-transitions Quoted -> Approved; no other transition can be initiated by a customer. | Not Run |  |  |  |
| TC-WF-09 | Workflow & State Machine | Not Repairable is a terminal state with assessment fee | Sec 10, FR-QUO-09 | State Machine | High | A diagnosed job set to Not Repairable with a reason. | 1. Complete the Not Repairable transition.<br>2. Attempt any further forward transition. | State is terminal (no further advance), and an assessment-fee proforma invoice is generated automatically. | Not Run |  |  |  |
| TC-WF-10 | Workflow & State Machine | Rejected is terminal with NO assessment fee | Sec 10, FR-QUO-09 | State Machine | High | A job in Quoted stage. | 1. As main customer, reject the quote. | Job moves to Rejected (terminal); NO assessment fee is generated. | Not Run |  |  |  |
| TC-WF-11 | Workflow & State Machine | Cancelled is admin-only and incurs no fee | Sec 10 | State Machine | High | A received job before any work began. | 1. As Admin, cancel the job.<br>2. Attempt cancel as a non-admin. | Admin can cancel (terminal, no fee); non-admin cannot cancel. | Not Run |  |  |  |
| TC-WF-12 | Workflow & State Machine | Invalid stage skips are prevented | FR-WF-01, Sec 10 | Negative | High | A job in Received stage. | 1. Attempt to jump directly from Received to Boxed. | Disallowed transition is rejected; only valid next-state transitions are permitted. | Not Run |  |  |  |
| TC-WF-13 | Workflow & State Machine | Stage timeline highlights done/current/future stages | Wireframe Job detail | UI | Low | A job in Quoted stage. | 1. Open Job detail and inspect the stage timeline pips. | Completed stages appear 'done', the current stage is highlighted 'now', and later stages are 'future'. | Not Run |  |  |  |
| TC-WF-14 | Workflow & State Machine | Print jobcard from Job detail | FR-INT-10, FR-ADM-07 #6, Wireframe | Functional | Medium | Open Job detail. | 1. Click 'Print jobcard'. | A single-page A4 jobcard renders with the Gentick header sourced from company-details settings. | Not Run |  |  |  |

---

## Communication and Notifications

15 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-COM-01 | Communication & Notifications | Notifications fire at the four key milestones | FR-COM-01 | Notification | High | A job with consenting parties. | 1. Trigger item received, quote ready, ready for collection, and not repairable events. | A communication is sent at each milestone to the relevant party per the routing matrix. | Not Run |  |  |  |
| TC-COM-02 | Communication & Notifications | WhatsApp primary when consent given; email always sent | FR-COM-02 | Notification | High | Party with WhatsApp consent. | 1. Trigger an 'item received' event. | WhatsApp message is sent (primary) and email is also sent as fallback/record. | Not Run |  |  |  |
| TC-COM-03 | Communication & Notifications | Email-only when WhatsApp consent is absent | FR-COM-02, FR-INT-04 | Notification | High | Party who has opted out of WhatsApp. | 1. Trigger a milestone event for that party. | Only email is sent; no WhatsApp message goes to the opted-out party. | Not Run |  |  |  |
| TC-COM-04 | Communication & Notifications | Quote approval via WhatsApp 'YES' | FR-COM-03 | Functional | High | Main customer received a quote-ready WhatsApp. | 1. Reply 'YES' on WhatsApp. | Reply is classified as approval; job transitions to Approved and a confirmation is logged. | Not Run |  |  |  |
| TC-COM-05 | Communication & Notifications | Quote approval via email reply / portal click | FR-COM-03, FR-QUO-03 | Functional | High | Main customer has a quote awaiting approval. | 1. Click 'Approve' in the portal (or reply to the approval email). | Quote is approved, job transitions to Approved, and the action is logged with channel and actor. | Not Run |  |  |  |
| TC-COM-06 | Communication & Notifications | Only the main customer's approval is accepted | FR-COM-03, FR-QUO-02 | Negative | High | Agent attempts to approve via WhatsApp YES from agent number. | 1. Send 'YES' from the agent's WhatsApp number for a quoted job. | Approval is NOT applied from the agent; the message is treated as non-authoritative / escalated, and the job stays Quoted. | Not Run |  |  |  |
| TC-COM-07 | Communication & Notifications | Every inbound and outbound message is logged against the job | FR-COM-04 | Functional | High | A job with message activity. | 1. Open the Communications log tab. | Each message shows channel, timestamp, sender/recipient, delivery status, and content. | Not Run |  |  |  |
| TC-COM-08 | Communication & Notifications | Unclassified inbound replies escalate to Michael | FR-ADM-09, Sec 7.1, Wireframe | Notification | Medium | Inbound WhatsApp reply that is not YES/NO. | 1. Send a free-text question via WhatsApp in reply to a notification. | Message is routed to the WhatsApp escalation contact (default Michael, 082 374 9842) and logged against the job. | Not Run |  |  |  |
| TC-COM-09 | Communication & Notifications | Message templates are configurable and versioned | FR-COM-05, FR-ADM-05 | Functional | Medium | Admin editing templates. | 1. Edit an outbound template and save a new version.<br>2. Send a message using the new version.<br>3. View a historical message sent under the prior version. | New messages use the current template; historical messages retain the exact wording version sent at the time. | Not Run |  |  |  |
| TC-COM-10 | Communication & Notifications | Notification routing matrix - quote ready | Sec 8 | Notification | High | A quote-ready event. | 1. Trigger 'quote ready'. | Main customer is notified (email primary + WhatsApp for approval); the agent is NOT notified until approved. | Not Run |  |  |  |
| TC-COM-11 | Communication & Notifications | Notification routing matrix - quote approved | Sec 8, FR-QUO-02 | Notification | High | A quote-approved event. | 1. Approve the quote. | Both main customer and agent are notified (WhatsApp + email) and the agent now sees the full price. | Not Run |  |  |  |
| TC-COM-12 | Communication & Notifications | Notification routing matrix - item not repairable | Sec 8, FR-WF-06, FR-QUO-09 | Notification | High | A unit marked Not Repairable. | 1. Complete the Not Repairable transition. | Both main customer and agent receive WhatsApp + email with the reason report and assessment-fee invoice. | Not Run |  |  |  |
| TC-COM-13 | Communication & Notifications | Notification routing matrix - agent invited | Sec 8, FR-INT-11 | Notification | Medium | An agent invitation is issued. | 1. Main customer invites a new agent. | Only the agent receives a welcome/invite email; the main customer is not separately notified. | Not Run |  |  |  |
| TC-COM-14 | Communication & Notifications | Notification routing matrix - job closed | Sec 8, FR-QUO-04 | Notification | Medium | A job moves to Closed. | 1. Close the job. | Main customer receives an email with the proforma invoice; the agent is not notified at close. | Not Run |  |  |  |
| TC-COM-15 | Communication & Notifications | Item received notifies both parties on any origin | Sec 8, FR-COM-01 | Notification | Medium | A unit received from any origin path. | 1. Mark the unit received. | Both main customer and agent receive WhatsApp + email 'item received'. | Not Run |  |  |  |

---

## Quoting and Billing

19 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-QUO-01 | Quoting & Billing | Quote contains required line items and totals | FR-QUO-01 | Functional | High | A diagnosed job. | 1. Create a quote with labour estimate, parts line items, notes, validity period, and currency.<br>2. Save. | Quote stores labour, parts line items, notes, validity, currency code, and a VAT-inclusive total. | Not Run |  |  |  |
| TC-QUO-02 | Quoting & Billing | Quote total calculation incl. VAT is correct | FR-QUO-01, FR-QUO-08 | Functional | High | A ZAR quote with subtotal 3,090.00. | 1. Add lines totalling 3,090.00 ZAR.<br>2. Apply default 15% VAT.<br>3. Inspect totals. | VAT = 463.50 and Total = 3,553.50 ZAR (matches wireframe Q-GT-ACME-0184-1). | Not Run |  |  |  |
| TC-QUO-03 | Quoting & Billing | Quote is issued to the main customer regardless of originator | FR-QUO-02 | Functional | High | An agent-initiated job now quoted. | 1. Issue the quote. | Quote email goes to the main customer; the agent is informed but not asked to approve. | Not Run |  |  |  |
| TC-QUO-04 | Quoting & Billing | Agent sees full quote details (incl. R-value) after approval | FR-QUO-02, Sec 8 | RBAC | High | A quote just approved by the main customer. | 1. Log in as the agent and open the job. | Agent now sees the full quote including the R-value/price. | Not Run |  |  |  |
| TC-QUO-05 | Quoting & Billing | Agent does NOT see price before approval | FR-QUO-02, Sec 8 | RBAC | High | A quoted job not yet approved. | 1. Log in as the agent and open the job. | Agent sees the job is Quoted/awaiting approval but the price is not yet exposed as approved detail (status shows 'Waiting for [main customer] to approve'). | Not Run |  |  |  |
| TC-QUO-06 | Quoting & Billing | Approval auto-transitions job to Approved | FR-QUO-03 | Functional | High | Quote awaiting approval. | 1. Main customer approves. | Job stage becomes Approved automatically. | Not Run |  |  |  |
| TC-QUO-07 | Quoting & Billing | Proforma invoice PDF generated on job close | FR-QUO-04 | Functional | High | A job ready to close. | 1. Close the job.<br>2. Download the proforma invoice. | A downloadable PDF proforma invoice is generated. | Not Run |  |  |  |
| TC-QUO-08 | Quoting & Billing | Sage Pastel Online CSV/XML export produced | FR-QUO-05, Sec 7.2 | Functional | High | Closed jobs with invoices. | 1. Generate the Sage export batch.<br>2. Open the exported file. | A CSV/XML file compatible with Sage Pastel Online's import format is produced. | Not Run |  |  |  |
| TC-QUO-09 | Quoting & Billing | Invoiced / Not invoiced status reflects export only | FR-QUO-06 | Functional | Medium | A closed job before and after export. | 1. Before export, check status.<br>2. Generate export.<br>3. Re-check status. | Status shows 'Not invoiced' until an export is generated, then 'Invoiced'; settlement/payment status is not mirrored from Sage. | Not Run |  |  |  |
| TC-QUO-10 | Quoting & Billing | Multi-currency quote in USD stays native end-to-end | FR-QUO-07 | Functional | High | A job for a foreign customer. | 1. Create a quote in USD.<br>2. Generate invoice and reports. | Quote and invoice remain in USD throughout; no ZAR conversion is performed in MVP. | Not Run |  |  |  |
| TC-QUO-11 | Quoting & Billing | ZAR quote defaults to 15% VAT | FR-QUO-08 | Functional | High | New ZAR quote. | 1. Create a quote with currency ZAR. | VAT rate defaults to 15%. | Not Run |  |  |  |
| TC-QUO-12 | Quoting & Billing | Non-ZAR quote defaults to 0% VAT (export zero-rating) | FR-QUO-08, Sec 13 | Functional | High | New USD quote. | 1. Create a quote with currency USD. | VAT rate defaults to 0%. | Not Run |  |  |  |
| TC-QUO-13 | Quoting & Billing | VAT override requires a captured reason | FR-QUO-08 | Functional | Medium | A quote with default VAT. | 1. Override the VAT rate.<br>2. Attempt to save without a reason.<br>3. Then add a reason and save. | Override is blocked until a reason is captured; the reason is stored with the quote. | Not Run |  |  |  |
| TC-QUO-14 | Quoting & Billing | Not Repairable auto-generates assessment-fee proforma (default R350) | FR-QUO-09 | Functional | High | Assessment fee setting = R350. | 1. Mark a unit Not Repairable with a reason. | An assessment-fee proforma invoice for R350 is generated automatically. | Not Run |  |  |  |
| TC-QUO-15 | Quoting & Billing | Rejected quote does NOT generate an assessment fee | FR-QUO-09, Sec 10 | Negative | High | A quoted job. | 1. Main customer rejects the quote. | No assessment fee or invoice is generated; job is Rejected. | Not Run |  |  |  |
| TC-QUO-16 | Quoting & Billing | Assessment fee configurable per job and waivable by admin with reason | FR-QUO-09 | Functional | Medium | A Not Repairable job with an assessment fee. | 1. As admin, change the per-job fee, then waive it with a reason. | Per-job fee can be changed; waiver requires and stores a reason; audit reflects the change. | Not Run |  |  |  |
| TC-QUO-17 | Quoting & Billing | Company legal details appear on quotes and invoices | FR-QUO-10, FR-ADM-08 | Functional | High | Company-details setting populated. | 1. Generate a quote PDF and a proforma invoice PDF. | Both show Gentick Electronics, 169 Haupt Street, Sidwell, Port Elizabeth, 6001, and VAT 4840244414. | Not Run |  |  |  |
| TC-QUO-18 | Quoting & Billing | Expired quote (past validity) auto-expires | Sec 14 #4 | Functional | Medium | Quote validity = 14 days; a quote issued 15 days ago. | 1. Open the quote after the validity window. | Quote is marked expired and cannot be approved without reissue. | Not Run |  |  |  |
| TC-QUO-19 | Quoting & Billing | Per-job parts consumption feeds the quote | FR-ADM-03 | Functional | Medium | A job with logged parts from the catalogue. | 1. Log a part (e.g., PSU-4G-A) against the job.<br>2. Generate the quote. | The logged part appears as a quote line item with the catalogue cost and currency. | Not Run |  |  |  |

---

## Reporting and Dashboard

10 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REP-01 | Reporting & Dashboard | Active jobs dashboard shows per-stage counts and aging | FR-REP-01, Wireframe Admin | Functional | High | Jobs distributed across stages. | 1. Open the Admin dashboard. | KPI tiles show active jobs, aged >14 days, avg turnaround, revenue; the stages strip shows counts per stage; aged jobs are highlighted. | Not Run |  |  |  |
| TC-REP-02 | Reporting & Dashboard | Technician workload report | FR-REP-02 | Functional | Medium | Multiple technicians with open jobs. | 1. Open the technician workload report. | Open jobs per technician with average time-in-stage are shown. | Not Run |  |  |  |
| TC-REP-03 | Reporting & Dashboard | Turnaround time report sliceable by month and repair type | FR-REP-03 | Functional | Medium | Closed jobs across months and repair types. | 1. Open the turnaround report.<br>2. Filter by month and by repair type. | Average days Received->Collected are shown and update correctly per slice. | Not Run |  |  |  |
| TC-REP-04 | Reporting & Dashboard | Revenue report shows currency alongside values (no ZAR roll-up) | FR-REP-04, FR-QUO-07, Sec 14 #5 | Functional | Medium | Invoiced jobs in ZAR and USD. | 1. Open the revenue report. | Values are shown by period/repair type/main customer with their currency; ZAR and USD are not combined into a single total. | Not Run |  |  |  |
| TC-REP-05 | Reporting & Dashboard | Intake source report splits by origin | FR-REP-08 | Functional | Medium | Jobs from all three origins. | 1. Open the intake source report. | Report shows the split of jobs by main customer / agent / front desk origin. | Not Run |  |  |  |
| TC-REP-06 | Reporting & Dashboard | All reports exportable to CSV | FR-REP-07 | Functional | Medium | Any report open. | 1. Click Export CSV. | A CSV download is produced with the report's data. | Not Run |  |  |  |
| TC-REP-07 | Reporting & Dashboard | Common faults summary on dashboard | FR-REP-06 (Phase 2), Wireframe Admin | UI | Low | Faults classified across jobs this month. | 1. Open the Admin dashboard 'Common faults this month' card. | Fault classifications are listed with counts; 'Open full report' link is present (full report is Phase 2). | Not Run |  |  |  |
| TC-REP-08 | Reporting & Dashboard | Today's priorities surfaces approvals, boxed units, and Sage batch | Wireframe Admin | UI | Low | Pending approvals, boxed units, and a ready Sage batch exist. | 1. Open the Admin dashboard. | 'Today's priorities' lists quotes awaiting approval, boxed units awaiting collection notification, and the Sage export ready for download. | Not Run |  |  |  |
| TC-REP-09 | Reporting & Dashboard | Jobs table customer filter works | Wireframe Admin | UI | Medium | Jobs for multiple customers. | 1. On the dashboard jobs table, filter by 'Acme Agritech'. | Only Acme jobs are listed; clearing the filter restores all. | Not Run |  |  |  |
| TC-REP-10 | Reporting & Dashboard | Customer / unit repair history view | FR-REP-05, Wireframe Job detail | Functional | Medium | A unit (e.g., SN-21A847) with multiple prior jobs. | 1. Open Job detail for the unit.<br>2. Open the History / 'This unit's history' section.<br>3. Also open the per-customer history view. | All historical jobs for the unit/customer are listed with quick view to fault patterns (e.g., '3 times', prior lightning/component faults). | Not Run |  |  |  |

---

## Admin and Settings

10 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-ADM-01 | Admin & Settings | Staff user management across the three roles | FR-ADM-01, Sec 4.1 | Functional | High | Logged in as Admin. | 1. Create staff users as Admin, Technician, and Front Desk.<br>2. Save. | Users are created with the correct role and corresponding access. | Not Run |  |  |  |
| TC-ADM-02 | Admin & Settings | Parts catalogue management | FR-ADM-02 | Functional | Medium | Admin in parts catalogue. | 1. Add a part with code, description, cost, and currency.<br>2. Save. | Part is added and becomes selectable when technicians log usage. | Not Run |  |  |  |
| TC-ADM-03 | Admin & Settings | Repair type taxonomy management | FR-ADM-04 | Functional | Low | Admin in settings. | 1. Add a repair category.<br>2. Use it on a job; check it appears in reports. | New repair type is available for jobs and appears as a slice in reporting. | Not Run |  |  |  |
| TC-ADM-04 | Admin & Settings | Audit log viewer shows consent, logins, role and data changes | FR-ADM-06 | Functional | Medium | Activity has occurred (consent change, login, role change, data edit). | 1. Open the audit log viewer. | All four event categories are visible with actor and timestamp. | Not Run |  |  |  |
| TC-ADM-05 | Admin & Settings | System settings editable by admin only | FR-ADM-07 | RBAC | High | Logged in as Admin, then as non-admin. | 1. As admin, edit assessment fee, currencies, default VAT, photo limits, quote validity.<br>2. As non-admin, attempt to open settings. | Admin can edit all listed settings; non-admin is denied access. | Not Run |  |  |  |
| TC-ADM-06 | Admin & Settings | Company details setting is versioned for historical documents | FR-ADM-08 | Functional | High | A historical invoice issued under prior company details. | 1. Change the company VAT/address setting.<br>2. Re-open a historical invoice.<br>3. Generate a new invoice. | Historical document retains the company details in effect at issue; new document reflects updated details. | Not Run |  |  |  |
| TC-ADM-07 | Admin & Settings | Operational contacts setting (escalation + champion) | FR-ADM-09, Sec 14 #9-10 | Functional | Medium | Admin in operational contacts. | 1. Reassign the WhatsApp escalation contact and the internal champion to a new person.<br>2. Save.<br>3. Trigger an unclassified inbound. | Defaults are Michael; after reassignment the new contact receives escalations - no code change needed. | Not Run |  |  |  |
| TC-ADM-08 | Admin & Settings | Photo upload limit enforced (20 per job, 5MB each) | FR-ADM-07, Sec 14 #3 | Negative | Medium | A job with 20 photos already attached. | 1. Attempt to upload a 21st photo.<br>2. Attempt to upload a 6MB image. | 21st photo and oversized file are rejected with a clear message. | Not Run |  |  |  |
| TC-ADM-09 | Admin & Settings | Assessment fee default change applies to new Not Repairable jobs | FR-ADM-07, FR-QUO-09 | Functional | Medium | Admin changes default assessment fee from R350 to R400. | 1. Save the new default.<br>2. Mark a new unit Not Repairable. | New assessment-fee invoice uses R400; previously generated invoices are unchanged. | Not Run |  |  |  |
| TC-ADM-10 | Admin & Settings | Admin can reset any user's password from the Security page; non-admins cannot | FR-ADM-01 / NFR-04 / enhancement #11 | Functional | High | Admin logged in. At least one staff, one customer contact and one agent with logins exist. | 1. As Admin open /account/security and locate 'Reset a user's password (admin)'.<br>2. Select a user and click 'Send reset email'; check Mailpit (:54324) for the reset email.<br>3. Select a user, enter a new temp password (>= 8 chars) and click 'Set password now'; verify the user can sign in with it (incognito).<br>4. Try 'Set password now' with a 7-character password.<br>5. Open the Audit log as Admin and look for the password events.<br>6. Log in as Technician or Front Desk and open /account/security. | The admin card lists all login-bearing users grouped by Staff / Customer contacts / Agents. Reset email arrives in Mailpit and links to the app's reset page on ONE origin (NEXT_PUBLIC_APP_URL). Direct set succeeds and the user can sign in with the temp password. Short password rejected inline (min 8). Both actions write audit events (password_set_by_admin / password_reset_email_by_admin). Non-admin staff see only the MFA card — no reset section — and the server actions independently reject non-admins. | Not Run |  |  | Enhancement #11 built 22 Jul 2026 (Lucia request; D-12 lockout was the driving case). Files: AdminPasswordReset.tsx, account/security/actions.ts (+page), lib/admin/supabase-admin.ts, listLoginUsers in people.ts. NOTE: if "Set password now" then sign-in fails, that reproduces open defect D-12 — record evidence there. |

---

## UI - Wireframe

10 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-UI-01 | UI / Wireframe | Technician 'Today's focus' shows prioritised cards | Wireframe Technician | UI | Low | Logged in as Technician with an aged job and parts arrivals. | 1. Open 'My queue'. | Focus cards show e.g. aged job to escalate, parts-arrived resume, and ready-for-QC, each with a primary action. | Not Run |  |  |  |
| TC-UI-02 | UI / Wireframe | Technician full queue sorting | Wireframe Technician | UI | Low | Technician with 12 jobs. | 1. Sort by oldest first, by stage, and by customer. | Queue reorders correctly for each sort option. | Not Run |  |  |  |
| TC-UI-03 | UI / Wireframe | Technician quick-log note/part/photo against active job | FR-WF-04, FR-ADM-03, Wireframe | Functional | Medium | Technician working on a job. | 1. In Quick log, select the working job.<br>2. Add a note, a part, and a photo.<br>3. Save log entry. | Note, part, and photo are recorded against the selected job. | Not Run |  |  |  |
| TC-UI-04 | UI / Wireframe | Agent portal KPIs reflect agent's own jobs | Wireframe Agent | UI | Low | Agent with 3 active, 1 awaiting approval, 1 ready for collection. | 1. Open the agent portal. | KPI tiles match the agent's job counts and statuses. | Not Run |  |  |  |
| TC-UI-05 | UI / Wireframe | Agent 'My units' pre-fills a new repair request | FR-INT-09, Wireframe Agent | Functional | Medium | Agent has prior units. | 1. In 'My units', pick SN-21A847 to start a new request. | New repair form is pre-filled with that unit's type/serial and prior context. | Not Run |  |  |  |
| TC-UI-06 | UI / Wireframe | Agent active/closed repairs toggle | FR-INT-10, Wireframe Agent | UI | Low | Agent has active and closed jobs. | 1. Toggle between 'Active only' and 'Include closed'. | List updates to show active-only vs all jobs accordingly. | Not Run |  |  |  |
| TC-UI-07 | UI / Wireframe | Main customer 'Action required' approve/reject controls | FR-QUO-02, FR-COM-03, Wireframe | Functional | High | Main customer with 3 quotes awaiting approval. | 1. Open the main customer portal.<br>2. Use 'View quote PDF', 'Approve', and 'Reject' on a quote. | PDF opens; Approve transitions the job to Approved; Reject moves it to Rejected (no fee). | Not Run |  |  |  |
| TC-UI-08 | UI / Wireframe | Main customer spend-this-quarter and CSV export | FR-REP-04, FR-REP-07, Wireframe | Functional | Low | Main customer with invoiced jobs. | 1. Open the portal.<br>2. Click 'Export spend report (CSV)'. | Spend summary shown; CSV export downloads the spend data. | Not Run |  |  |  |
| TC-UI-09 | UI / Wireframe | Main customer all-active-jobs agent filter | Wireframe Main customer | UI | Low | Jobs across several agents. | 1. Filter the all-active-jobs table by a single agent. | Only that agent's jobs are listed. | Not Run |  |  |  |
| TC-UI-10 | UI / Wireframe | Branding/header renders Gentick identity | Wireframe, FR-ADM-08 | UI | Low | Any portal page. | 1. Load a page and inspect the header/sidebar. | Gentick Electronics branding and 'Experts in Telemetry Systems' tagline render correctly. | Not Run |  |  |  |

---

## Non-Functional

14 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-NFR-01 | Non-Functional | Staff page loads under 2s on 4G; portal under 3s | NFR-01 | Performance | Medium | Throttled 4G connection. | 1. Load staff dashboard and customer portal pages; measure load time. | Staff pages load < 2s; customer portal < 3s. | Not Run |  |  |  |
| TC-NFR-02 | Non-Functional | Job list handles 1,000 jobs without pagination performance issues | NFR-01 | Performance | Medium | 1,000 jobs seeded. | 1. Open the active jobs list and scroll/filter. | List remains responsive with 1,000 jobs. | Not Run |  |  |  |
| TC-NFR-03 | Non-Functional | All traffic served over HTTPS | NFR-04 | Security | High | Any page. | 1. Attempt to access over HTTP.<br>2. Inspect TLS on HTTPS. | HTTP redirects to HTTPS; valid TLS in use throughout. | Not Run |  |  |  |
| TC-NFR-04 | Non-Functional | Secrets are not exposed in code or workflow JSON | NFR-04, Sec 7.1 | Security | High | Access to repo and n8n workflow exports. | 1. Inspect code and exported n8n workflow JSON for credentials. | No secrets in code or workflow JSON; credentials live in n8n's encrypted store. | Not Run |  |  |  |
| TC-NFR-05 | Non-Functional | POPIA data residency - no cross-border transfer | NFR-05, Sec 13 | Compliance | High | Production hosting. | 1. Verify DB and file storage region.<br>2. Verify backup region. | Data hosted in Hetzner Cape Town (SA); off-site backup in a second SA region; no cross-border transfer. | Not Run |  |  |  |
| TC-NFR-06 | Non-Functional | Subject access request answerable within 30 days | NFR-05 | Compliance | Medium | A data subject request. | 1. Export all personal data held for a given contact/agent. | A complete personal-data export can be produced to satisfy a SAR within 30 days. | Not Run |  |  |  |
| TC-NFR-07 | Non-Functional | Customer portal is mobile-responsive | NFR-06, Sec 2.1 | UI | High | Agent on a phone-width viewport (<=1100px). | 1. Open the agent portal and book a repair on a narrow viewport. | Layout reflows to a single column and intake is fully usable on mobile. | Not Run |  |  |  |
| TC-NFR-08 | Non-Functional | Supported browsers render correctly | NFR-06 | Compatibility | Medium | Latest 2 versions of Chrome, Edge, Firefox, Safari. | 1. Open key screens in each supported browser. | Screens render and function correctly with no browser-specific breakage. | Not Run |  |  |  |
| TC-NFR-09 | Non-Functional | Customer portal meets WCAG 2.1 AA basics | NFR-07 | Accessibility | Medium | Customer portal pages. | 1. Run an accessibility audit (contrast, labels, keyboard nav, focus order). | Portal passes WCAG 2.1 AA checks for contrast, form labels, and keyboard navigation. | Not Run |  |  |  |
| TC-NFR-10 | Non-Functional | Daily backups exist and are restorable | NFR-03, Sec 14 #11 | Reliability | High | Backup schedule configured. | 1. Verify a recent daily DB + file backup.<br>2. Perform a test restore. | Daily backups exist (30-day retention; monthly snapshots 12 months) and a restore succeeds. | Not Run |  |  |  |
| TC-NFR-11 | Non-Functional | Automation failures raise error alerts to admin | NFR-10, Sec 7.1 | Reliability | Medium | An n8n workflow configured to fail (e.g., bad WhatsApp send). | 1. Force a workflow failure. | The n8n error workflow captures it and an error alert is sent to admin email. | Not Run |  |  |  |
| TC-NFR-12 | Non-Functional | Availability target and maintenance notice | NFR-02 | Reliability | Medium | Uptime monitoring (e.g., UptimeRobot) configured. | 1. Review uptime monitoring over a measured month.<br>2. Verify planned maintenance windows are announced 48h in advance. | Monthly uptime meets the 99% target; maintenance windows are announced at least 48 hours ahead. | Not Run |  |  |  |
| TC-NFR-13 | Non-Functional | Scalability headroom for 10x growth | NFR-08 | Performance | Low | Baseline of hundreds of jobs/month. | 1. Load-test the system with ~10x the expected monthly job volume.<br>2. Measure response times and resource use. | System handles 10x volume without architectural change and within acceptable performance. | Not Run |  |  |  |
| TC-NFR-14 | Non-Functional | Maintainability standards in place | NFR-09 | Maintainability | Low | Access to the codebase and migration history. | 1. Confirm TypeScript is used throughout.<br>2. Confirm README documents code conventions.<br>3. Confirm DB schema is versioned via migrations. | Codebase is TypeScript, conventions are documented, and schema changes are tracked as migrations. | Not Run |  |  |  |

---

## Integrations

5 test cases.

| Test ID | Module | Title | Requirement Ref | Type | Priority | Preconditions | Test Steps | Expected Result | Status | Actual Result | Tester | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INTG-01 | Integrations | RMS emits event to n8n webhook on status change | Sec 7.1 | Integration | High | n8n webhook reachable. | 1. Change a job status to trigger job.status_changed. | RMS posts the event to the n8n webhook with the correct payload. | Not Run |  |  |  |
| TC-INTG-02 | Integrations | n8n logs outbound WhatsApp back to the RMS | Sec 7.1, FR-COM-04 | Integration | High | A WhatsApp send completes via Meta Cloud API. | 1. Trigger a WhatsApp notification end-to-end. | n8n calls back the RMS API and the outbound message is logged against the job. | Not Run |  |  |  |
| TC-INTG-03 | Integrations | Inbound Meta webhook classified and routed | Sec 7.1 | Integration | Medium | Inbound WhatsApp message arrives. | 1. Send a YES, then a free-text reply. | YES routes to quote approval; free-text routes to staff/escalation; both are logged. | Not Run |  |  |  |
| TC-INTG-04 | Integrations | Sage export file matches Sage Pastel Online import format | FR-QUO-05, Sec 7.2 | Integration | High | An export batch generated. | 1. Import the exported file into Sage Pastel Online (or validate against its schema). | File imports without format errors; customer and invoice fields map correctly. | Not Run |  |  |  |
| TC-INTG-05 | Integrations | Transactional emails authenticated (SPF/DKIM/DMARC) | Sec 7.3 | Integration | Medium | Email sending configured on gentick.co.za. | 1. Send a jobcard/quote email and inspect headers. | Email passes SPF, DKIM, and DMARC checks; from-address is repairs@gentick.co.za. | Not Run |  |  |  |

---
